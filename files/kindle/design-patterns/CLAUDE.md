# CLAUDE.md — デザインパターンで学ぶ設計思考
# design-patterns プロジェクト固有の設定ファイル

---

## このプロジェクトが作るもの

GoFデザインパターンの「名前を覚える」ではなく、
「なぜその構造に辿り着くのか」という思考プロセスを身につけることを目的とした
技術書（Kindle本）。

読者：コードは書けるが設計に自信がない中級エンジニア
目標：現場の変更要求をきっかけに、設計パターンを自力で導ける思考を自然に身につける

---

## 必ず最初に読むファイル

エージェントは作業開始前に以下を必ずこの順番で読むこと：

1. `../../shared/skills/author-voice.md` — 著者の人格（最重要）
2. `design-philosophy.md` — 設計思想カタログ（読者の全疑問への回答定義）
3. `../../shared/personas/reader-profiles.md` — 読者ペルソナ
4. `../../shared/skills/markdown-checker.md` — 品質チェック基準
5. このCLAUDE.md の残りのルール

---

## 著者の人格（核心を一言で）

「ポンコツだった自分が設計の楽しさに気づいた。
 同じように悩んでいる人に、一つの参考として届けたい。」

詳細は `../../shared/skills/author-voice.md` を参照。

---

## 章の構造ルール

### 章の骨格

この本の各章は以下のステップで進む。順序を崩してはいけない。
テンプレートの最新版は `templates/chapter-template.md` を参照。

| ステップ | 内容 | H2見出し |
|:---|:---|:---|
| 0. 視点のチューニング | 全パターン共通の問いをセット・変動と不変の仮説 | `## ステップ0：視点のチューニング` |
| 1. 現状把握 | 依存グラフ（Mermaid）・変更前クラス図・起点コード・責任チェック表 | `## ステップ1：現状把握` |
| 2. 変動と不変の峻別 | 仮説をコードで検証 → **関係者ヒアリング** → 変動/不変を表で確定 | `## ステップ2：変動と不変の峻別` |
| 3. 課題分析 | 変更影響グラフ（Mermaid）・痛みのシナリオ | `## ステップ3：課題分析` |
| 4. 原因分析 | なぜなぜ分析（表） | `## ステップ4：原因分析` |
| 5. 対策案の検討 | 段階的試行（責任チェック×N回）→インターフェース定義・変更後クラス図 | `## ステップ5：対策案の検討` |
| 6. 天秤にかける | 比較表・適用判断フローチャート（Mermaid） | `## ステップ6：天秤にかける` |
| 7. 決断と未来 | 最終コード全体・変更シナリオ表・変更影響グラフ（改善後） | `## ステップ7：決断と、手に入れた未来` |

### 全パターン共通の問い（ステップ0）

すべての章でこの1つの問いを使う。パターンごとに変えない。

> 「このコードの中に、**『変わる理由』が異なる2つのものが、同じ場所に混在していないか？」**

「変わる理由」は「誰の判断で変わるか」で見分ける。答えが2人以上なら混在している。

### 図のルール（必須）

各ステップで以下の図を必ず入れる。

| ステップ | 図の種類 | 目的 |
|:---|:---|:---|
| ステップ1 | `graph TD` 依存グラフ | 問題の広がりをマクロで示す |
| ステップ1 | `classDiagram`（変更前） | 問題の構造を可視化 |
| ステップ3 | `graph LR` 変更影響グラフ（改善前） | 変更が飛び火する様子 |
| ステップ5 | `classDiagram`（変更後） | 解決後の構造を可視化 |
| ステップ6 | `flowchart TD` 判断フロー | パターン適用の判断基準 |
| ステップ7 | `graph LR` 変更影響グラフ（改善後） | 変更が局所化された様子 |

変更前後のクラス図は対比して示し、矢印の数の変化で構造改善を説明する。

### コードの専門知識排除ルール

コード例に専門知識（ファイルI/O・通信・特定フレームワーク・DB操作）を入れない。
`processOrder` / `INotifier` / `notify()` のような抽象的な名前を使い、読者が構造にだけ集中できるようにする。

### コードの文法レベルルール

基本的なオブジェクト指向の文法のみを使う。読者が構文の解読に引っかからないようにするため。

使ってよい文法：
- `class` / `virtual` / コンストラクタ / メソッド呼び出し / `if` / `for`
- インターフェース相当の純粋仮想クラス（`virtual void foo() = 0`）
- ポインタ（生ポインタで十分。所有権の議論はしない）

使わない文法：
- templateメタプログラミング・lambda式・スマートポインタ（`unique_ptr`等）
- C++17以降の機能（`std::optional`・構造化束縛等）
- 複雑な演算子オーバーロード

### main()と実行結果のルール

コードは「全体の動きが一目で追える」ことを優先する。

**main()の責任**：
- `main()` はプログラムの入り口。機能をキックするだけ。
- 依存の組み立て（どのクラスをどう注入するか）は `main()` の責任ではない。
- 組み立ての責任は `BatchApplication`（または相当するクラス）が持つ。

```cpp
// 正しいmain()の形
int main() {
    BatchApplication app;
    app.run(year, month);
    return 0;
}
```

**変更前コード（ステップ1）**：
- 起点コードに `main()` を含め、動作をエンドツーエンドで見せる
- `main()` の直後に実行結果を載せる
- 目的：「このコードは正しく動く。問題は構造にある」を明示する

**変更後コード（ステップ7）**：
- `BatchApplication` + `main()` の最終形を示す
- テストコードで「変更前と同じ動作を保証」する
- 変更シナリオ別に「変わるクラス・変わらないクラス」の一覧を示す

### インターフェース命名原則

インターフェースの名前は「**ビジネス上の責任**」で付ける。実装手段で付けない。

| ❌ 実装手段で命名 | ✅ ビジネス責任で命名 | 理由 |
|---|---|---|
| `IPdfService` | `IPaySlipOutputService` | 出力先がExcelに変わっても名前は変わらない |
| `IEmailNotifier` | `INotifier` | 手段がSMSに変わっても名前は変わらない |
| `IMySqlRepository` | `IOrderRepository` | DB変更も名前は変わらない |

インターフェース名が実装手段を含んでいたら、「実装が変わったときに名前も嘘になる」サイン。

**実装クラス名は技術手段で付けてよい（インターフェースとの使い分け）：**

| クラスの種類 | 命名方針 | 例 |
|---|---|---|
| インターフェース | ビジネス責任で命名 | `IPaySlipOutputService` |
| 実装クラス | 技術手段で命名してよい | `PdfServiceImpl` / `ExcelServiceImpl` |

`PdfServiceImpl` → `ExcelServiceImpl` への差し替えは BatchApplication の1行だけで完結する。インターフェースは変わらない——これが「名前の差し替え」ではなく「設計の変更耐性の実証」になる。

### 契約パラメータの隠蔽原則

インターフェースのパラメータ型が「変わるかもしれない」ときは、型をそのまま露出させるのではなく**隠蔽を検討する**。これはカプセル化の応用。

| 手段 | 内容 | 向いている場面 |
|---|---|---|
| 型を合意・固定 | 関係者と合意して型を決める | 型の変更リスクが低い・チームで合意できる |
| DTO（データ転送オブジェクト） | `EmployeeContext` 等の独自型を1つ定義 | 複数フィールドの変更をDTOに閉じ込めたい |
| 基底クラスで受け取る | 共通基底クラスを用意して渡す | OOP的に型ヒエラルキーが自然に存在する |
| void\* / 不完全型 | 型情報をインターフェースに持たせない | 型が全く予測できない・型安全より変更耐性を優先 |

**各章のストーリーで言及すべき場所：**
〇.3（関係者ヒアリング）で「この引数の型は変わりますか？」を問い、型安定性のリスクを確認する。
型変更リスクがある場合は〇.10（耐久テスト）でその選択肢を示す。

### 関係者ヒアリング原則

インターフェースの**契約内容（メソッドシグネチャ・型・責任境界）は1人では決められない**。

変動/不変を確定する前に、関係するサービスのオーナーに必ずヒアリングする：

- 「このAPIは将来変わる予定はありますか？」
- 「このIDの型（int）は今後変更になりますか？」
- 「出力先のフォーマットは固定ですか、それとも変わりえますか？」

ヒアリングの結果が変動/不変テーブルの根拠になる。
**「設計に絶対の正解はありません」——だからこそチームで合意してから境界線を引く。**

各章のストーリーでは、変動/不変を確定する場面（〇.3）でこのヒアリングの会話を入れる。
詳細はchapter-template.mdの「関係者ヒアリング」節を参照。

**ヒアリングで挙がったリスクは〇.10（耐久テスト）で必ず実演する：**
ヒアリングで「将来〇〇に変わるかもしれない」と言及した変更は、〇.10の深化シナリオとして登場させる。
「ステップ2で予告した変化がここで現れた」という伏線回収の構造にする。
（例：ヒアリングで「PDF→Excelの可能性」→〇.10で実際にExcel切り替えを実演）

### 最終コードの設計要件

各章の最終コード（ステップ7）は以下の要件を満たす：

1. **各サービスにインターフェース**：変動する実装クラスは必ずインターフェースを持つ
2. **呼び出し元は契約だけを知る**：具体クラスへの依存を呼び出し元に残さない
3. **BatchApplicationが組み立てを担う**：具体クラスを知っているのは1箇所だけ
4. **main()はキックのみ**：`BatchApplication` を起動するだけ
5. **変更シナリオ表**：どのシナリオで何が変わり何が変わらないかを表で示す
6. **インターフェース名はビジネス責任で命名**：実装手段の名前を含めない

### 責任チェックルール（現状把握の核心）

現状把握（ステップ1）では「各クラスの責任」を定義し、
「責任外の知識が混在していないか」を確認する。

```
手順：
① 各クラスの責任を1文で定義する
② 各クラスの「知るべき知識」を列挙する
③ 実際のコードで「知っていること」を1行ずつ確認する
④ 「知るべきでない知識」を持っている行を特定する
⑤ その行が「誰の責任か」を明示する
```

責任外の知識が混在していれば「設計上の問題がある」と判定できる。

---

## 章の独立性ルール（最重要）

第0章を読んでいれば、どの章から読んでも理解できる完全自己完結の構成。

- 「前章で学んだように」「第〇章と同様に」→ **使わない**
- 「次章では〜」「次章で〜を扱います」などの**次章予告は一切書かない**
  （どの章から読んでもよい設計方針と矛盾するため）

---

## コードラベルのルール

| ラベル | 用途 |
|:---|:---|
| `// ← 知らなくていい` | 責任外の知識が染み出している行を示す |
| `// ← ここだけ変わる` | 変更影響が局所化された行を示す |
| 【深化コード】 | より難しい変化への対応 |
| 【過剰コード】 | 使いすぎた失敗例 |

コードへの気づきはコード外の散文で説明する。
コードはクリーンに保ち、「読む対象」として読者が自分で観察できるようにする。
`// 💭` は使わない（気づきを植え付けるのではなく、読者が自分で気づける構造を作る）。

---

## コードとテストの順序ルール

1コード（本体）＋テストで完結してから次のコードへ進む。テスト後回し禁止。

---

## 執筆品質ルール（フィードバック反映）

### 禁止表現

| 禁止する表現 | 理由 | 代替 |
|:---|:---|:---|
| 文頭・段落冒頭の「まず〜」 | 上から目線に聞こえる | 「最初に〜」「最初の試みとして〜」 |
| 「まず率直に認めたい」「まず認めたい」等 | 「まず」を含む上から目線表現 | 「素直に認めたいと思います」 |
| 「ここで答えは出さない」「まだ答えを出しません」「ここで答えは出しません」 | 読者を試す・偉そう | 削除。問いを立てるだけでよい |
| 「次章では〜」「次章で〜を扱います」等の次章予告 | どの章から読んでもよい設計方針と矛盾 | 書かない |
| 「悪いコード」「スパゲッティ」 | コードへの否定的ラベル | 「この構造では〇〇が難しい」等の事実で |
| 「〜すべき」「〜べきです」 | 命令口調 | 「〜が合うかもしれません」等 |

### 核心一言ルール

各章の **〇.1 見出しの直後**（システム概要の前）に、
以下の形式で**この章の核心を1文**で示す：

```
**要するに[このパターンが何をするか]というパターン。**
```

例（Facadeの場合）：
> **要するに変わりやすい機能を切り出して中継役を立てるパターン。**

特徴：
- 「要するに〜するパターン」という形を必ず使う
- 読者が最初の一文でパターンの本質をつかめる長さに収める
- 章の冒頭に置くことで、以降の説明が「その一文の根拠を積み上げる」構造になる

H1直下の `> **この章の核心**` 形式も引き続き使用してよいが、
〇.1の直後の核心一文が必須。

### 仕様説明ルール

現状の仕様を**文字だけで箇条書き**にするのは禁止。
必ず以下のいずれかで構造を見せる：

1. 入力→処理→出力の3列テーブル
2. 機能一覧テーブル（機能名・入力・出力 の列）
3. 実行結果をコードコメントで示す（`// → 給与明細PDF: payslip_2024_12.pdf`）

両方（テーブル ＋ 起点コードのコメントに実行例）を使うとなお良い。

### 数字根拠ルール

「〇箇所」「〇ファイル」という数字を出すときは、**根拠を必ず添える**。
方法：ファイル名列挙、grepの出力例、または「MonthlyBatch・PayslipExporter・OvertimeAlertJobの3ファイル」のように対象を列挙する。

```
// 根拠の示し方の例（grepの出力例を本文に記載）
$ grep -r "payroll-v1.example.com" .
attendance/MonthlyBatch.cpp:55
payslip/PayslipExporter.cpp:32
job/OvertimeAlertJob.cpp:28
// → 3か所ヒット
```

### 行レベル明示ルール

「使う側が知らなくていいこと」を**コードの該当行に `// ← 知らなくていい` コメント**で明示する。
テーブルで示す場合も行・フィールド名レベルで対応させる。

```cpp
// 良い例：どの行が「知らなくていいこと」かが一目でわかる
req.setHeader(
    "Authorization", "Bearer " + apiToken_  // ← 認証方式を知らなくていい
);
auto json = Json::parse(raw);
double v = json["base_salary"];  // ← レスポンスフィールド名を知らなくていい
```

### 依存注入コードルール

インターフェースを使う実装（解決コード）では、**依存注入のコードを必ず示す**。

示すべきもの：
1. コンストラクタでの受け取り（クラス定義）
2. 本番コードでの組み立て例
3. テストでの差し替え例（テストコードで自然に示す）

### テスト文法説明ルール

テストコードで**スタブ・モック・アサーションマクロが初めて登場する箇所**に、
1〜2行の日本語コメントを添える。

```cpp
// スタブ：本物のAPIを呼ばずに「あらかじめ決めた値を返す」差し替えクラス。
// インターフェースを継承することで本番クラスと入れ替えられる。
class StubPayrollFacade : public IPayrollFacade { ... };

// EXPECT_DOUBLE_EQ(期待値, 実際の値)：
// 「等しければテスト通過」という検証（アサーション）。Google Test のマクロ。
EXPECT_DOUBLE_EQ(350000.0, mockDb.lastSaved());
```

---

## Kindle本固有のチェック項目

### Kindle制限
- コードブロック：1行80文字以内（超える場合は折り返しコメントを入れる）
- Mermaid図：テキストでも意味が伝わる説明を必ず添える
- 脚注：使わない
- 表：列数は最大4列まで

### 読みやすさ
- 1節の長さ：印刷換算で2〜4ページ
- 1段落の長さ：5〜7行以内
- コードブロックの前後：必ず説明文を置く（コードの孤立禁止）
- 箇条書き：5項目以上続く場合は散文に変換することを検討

---

## 品質チェックリスト（生成後に必ず確認）

### 構造チェック
- [ ] ステップ0〜7のH2見出しが揃っているか
- [ ] ステップ0で全パターン共通の問いを使っているか（パターン固有の問いにしていないか）
- [ ] 変更前クラス図（ステップ1）と変更後クラス図（ステップ7）が両方あるか
- [ ] 依存グラフ・変更影響グラフ・判断フローチャートが揃っているか
- [ ] コードに専門知識が混入していないか（ファイルI/O・通信・フレームワーク禁止）
- [ ] コードがtemplateメタプログラミング・lambda・スマートポインタなどの高度な文法を使っていないか
- [ ] ステップ1の起点コードにmain()と実行結果が載っているか
- [ ] ステップ7の最終コードにBatchApplication＋main()の形があるか
- [ ] ステップ7にテスト（スタブを使った動作保証）があるか
- [ ] ステップ7に変更シナリオ別の「変わるクラス・変わらないクラス」表があるか
- [ ] 1コード＋テストで完結してから次に進んでいるか
- [ ] 〇.1の直後に「要するに〜するパターン」の核心一文があるか

### 責任チェック（現状把握の核心）
- [ ] ステップ1に各クラスの責任を1文で定義する表があるか
- [ ] ステップ1に「責任チェック表」（コードの行・持っている知識・責任内か）があるか
- [ ] 責任外の知識が「誰の責任か」まで明示されているか
- [ ] 最終設計の各クラスに「責任」と「変わる理由」の表があるか（ステップ7またはまとめ）

### 関係者ヒアリング・契約設計チェック
- [ ] ステップ2（〇.3）に関係者ヒアリングの会話シーンがあるか（変動/不変を確定する前）
- [ ] ヒアリングの結果が変動/不変テーブルの根拠になっているか
- [ ] ヒアリングで言及したリスクが〇.10の耐久テストシナリオとして登場しているか（伏線回収）
- [ ] インターフェース名がビジネス責任で命名されているか（実装手段の名前を含んでいないか）
- [ ] 実装クラス名は技術手段で命名していて問題ないことが理解できるストーリーになっているか
- [ ] 「インターフェースの契約はチームで合意した」という根拠がストーリー内に示されているか
- [ ] 契約パラメータの型変更リスクに触れているか（〇.3または〇.10で選択肢を示しているか）

### 最終設計チェック
- [ ] 変動する実装クラスは全てインターフェースを持っているか
- [ ] 呼び出し元は具体クラスではなくインターフェースに依存しているか
- [ ] BatchApplication（相当するクラス）が具体クラスの組み立てを担っているか
- [ ] main()はBatchApplicationをキックするだけか
- [ ] 各クラスの「変わる理由」が1つに絞られているか

### 執筆品質チェック
- [ ] 禁止表現（文頭「まず」「まず率直に認めたい」「ここで答えは出しません」「次章では〜」）がないか
- [ ] `// 💭` コードコメントを使っていないか（気づきは散文で説明する）
- [ ] 仕様説明がテーブルまたはコード実行例で示されているか
- [ ] 数字（「3箇所」等）に根拠が添えられているか
- [ ] 試行コードで「残る課題」が明示され、解決コードへの動機づけがあるか
- [ ] テスト文法（スタブ・アサーション）の説明が初出箇所にあるか
- [ ] 次章予告が書かれていないか

### Kindle品質チェック
- [ ] コードが1行80文字以内か
- [ ] 表の列数が4列以内か
- [ ] 脚注を使っていないか
- [ ] Mermaid図に説明文が添えられているか

### 著者の人格チェック
- [ ] 「〜すべき」「〜べきです」がないか
- [ ] 否定的なラベルがないか
- [ ] 口癖が3〜5箇所散りばめられているか
- [ ] 読者と同じ目線で書かれているか

### 読者視点チェック（一貫性・矛盾・疑問）
- [ ] 列挙・比較で「なぜこれだけ？」と思わせる不完全な例示がないか（全パターン・全ケースを揃えるか、代表例であることを明示するか）
- [ ] 見出しと本文の内容が一致しているか（見出しが約束していないことを書いていないか）
- [ ] クラス図・コード・本文で、クラス名・メソッド名・用語が統一されているか
- [ ] 変更前クラス図（ステップ1）とステップ1のコードが同じ構造を表しているか
- [ ] 変更後クラス図（ステップ5）とステップ7の解決コードが同じ構造を表しているか
- [ ] ステップ2の「変動/不変」の仕分けと、ステップ5のインターフェース設計が矛盾していないか
- [ ] 前章の知識を前提にしていないか（第0章だけ読めば各章が理解できるか）
- [ ] 抽象的な説明の直後に具体例があるか（「つまり〇〇のようなことだ」という橋渡し）
- [ ] 専門用語（インターフェース・ポリモーフィズム等）の初出時に説明があるか
- [ ] 根拠なしに断言している箇所がないか（「〇〇です」と書く場合、直前に根拠があるか）

### Markdown品質チェック
- [ ] 太字・コードブロックの閉じ忘れがないか
- [ ] 執筆指示コメント（`<!-- -->`）が出力に残っていないか

---

## エージェントの作業ルール

### 反論・提案ルール（鵜呑み禁止）

著者の指示を受けたとき、**より良い方法があれば必ず提案する**。
指示を単純に実行するだけでは、良い本にならない。

- 著者の意図を尊重しつつ、改善案・代替案があれば先に述べてから作業する
- 「この方向で行くとXXという問題が出ます。代わりにYYはどうでしょう」という形で提案する
- 提案しても著者が元の方針を選んだ場合は、その判断を尊重して実行する
- 何も提案がない場合でも「この判断で問題ありません」と一言添える（無条件服従との区別）

### 指摘・修正の影響範囲ルール

指摘を受けたとき、**影響が及ぶ全ファイルを自分で特定して修正する**。
「テンプレートを変えたのにCLAUDE.mdは古いまま」は禁止。
修正前に影響ファイルを洗い出し、1回の作業でまとめて直す。

### トークン節約ルール

- 大量のトークンを使う作業（ファイル全体の書き換え・大規模な調査）は**事前に確認してから実行**する
- 確認なしに進めてよいのは、影響範囲が明確な小さな修正のみ
- 同じ内容を複数回確認しない。1回読んだら使い切る

### 保存・同期ルール

- ファイルを変更したら**その都度すぐに保存**する（まとめて最後に保存しない）
- デスクトップ（`C:\Users\kumac\OneDrive\デスクトップ\Claude`）と GitHub は**常に同期**させる
- 片方だけ更新することは禁止。変更したら必ず両方に反映する
- git commit は 1つの論理的な変更につき 1コミット。まとめすぎない

---

## 本の構成（第一部・第二部）

### 第一部：デザインパターンの考え方を学ぶ（第0章〜第8章）

各章は1パターンを扱い、8ステップの思考プロセスを体験する。
第0章は共通導入。第1章以降は各パターン。

### 第二部：応用編 ── 複数パターンのコラボ（第9章以降）

第一部で身につけた思考プロセスを、より複雑なコードに適用する。

**第二部の特徴：**
- 1章の中に「変わる理由が異なるもの」が複数混在している複雑なコードを扱う
- 同じ8ステップで問題を分析するが、解決策として複数のパターンが組み合わさる
- 「このパターンを使え」という結論ありきではなく、問題を分析した結果として複数パターンが自然に選ばれる
- 第一部の全パターンを網羅する必要はない。読者が「複数の問題が同時にある」「どこから手をつけるか」を学べる題材を選ぶ

**第二部の章テンプレート（第一部との差異）：**
- ステップ0〜7の構造は同じ。ただし複数の「変わる理由の混在」が登場する
- ステップ5（対策案）で「1つのパターンでは解決しきれない部分」が現れ、2つ目のパターンが登場する
- 各パターンの詳細説明は省略し「第〇章で学んだ〇〇パターンを使います」という形で参照する
  （※ 章の独立性ルールの例外：第二部は第一部の知識を前提としてよい）
- まとめは「使ったパターン × 解消した問題」の対応表で示す

**第二部で扱う題材の選定基準：**
- 現実のシステムで「設計が複雑になりがち」な場面（状態管理＋通知、外部連携＋生成など）
- 読者が「あ、これは現場でよく見る」と思える題材
- コードに専門知識（フレームワーク等）を持ち込まないルールは第二部でも同じ

---

## ファイル構成

```
design-patterns/
├── CLAUDE.md                    ← このファイル
├── design-philosophy.md         ← 設計思想カタログ（全設計問いの唯一の正。新しい問いはここに追加）
├── toc.md
├── agents/
│   ├── orchestrator.md
│   ├── chapter-agent.md
│   ├── review-agent.md
│   ├── logic-check-agent.md
│   └── readability-agent.md
├── skills/
│   └── consistency-checker.md
├── templates/
│   └── chapter-template.md
├── patterns/
│   ├── facade.yaml
│   ├── state.yaml
│   ├── template-method.yaml
│   ├── command.yaml
│   ├── decorator.yaml
│   ├── observer.yaml
│   └── factory-method.yaml
└── output/
    ├── chapter00.md
    ├── chapter02.md
    └── ...
```
