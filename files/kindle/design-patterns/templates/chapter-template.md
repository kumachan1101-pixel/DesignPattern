# 第〇章　[テーマ名]：[サブタイトル]
―― 今回の変化：「[変化の種類を一言で]」

> **この章の核心**：[このパターンが「何を・どこに・なぜ」するのかを1文で。
> 「要するに〇〇するパターン」という形が目安。複雑にしない。]

> **第0章との対応**：この章では「[変化の種類]」という問題に、
> 5ステップの思考プロセスを適用します。
> どの章から読んでいただいても、同じ手順で考えを進められます。

---

## ステップ1：現状把握
> 今のシステムと、今日届いた変更要求を正確に把握する

### 〇.1 今のシステムの仕様とコードの構造

**要するに[このパターンが何をするか]というパターン。**

[〇.1の直後に、章全体の核心を「要するに〜するパターン」という一文で置く。
 読者がこの一文を読んだ段階でパターンの本質をつかめるようにする。
 以降の説明は「この一文の根拠を積み上げる」構造になる。]

#### このシステムが何をするか

[システムの概要を2〜3文で。業務用語はここで定義する。
 読者がこの機能を初めて見た新任エンジニアという前提で書く。]

#### 現在の仕様

[文字だけの箇条書きは禁止。必ずテーブルで構造を見せる。]

| 機能 | 入力 | 出力 |
|:---|:---|:---|
| [機能名1] | [入力] | [出力] |
| [機能名2] | [入力] | [出力] |

#### 【起点コード】

[「このコードを書いた担当者が当時どんな状況だったか」を
 コメントに込める。コードの説明ではなく背景を語る。
 著者の口癖を1箇所入れる。
 「まず〜」「まず率直に認めたい」という書き出しは使わない。]

```cpp
// 【起点コード】
// [ファイル名]
// コメント＝「なぜこう書いたかの背景」
// 実行すると何が起きるかをコメントで示す。
// 例：// → 戻り値: {"base_salary": 300000, "overtime_pay": 50000}
```

[このコードが今日まで動き続けてきた事実を認める。
 「今日まで現場を支えてきた事実を、素直に認めたいと思います」のような形。
 「まず率直に認めたい」「まず認めたい」は使わない。]

---

### 〇.2 届いた変更要求

[誰から・何の要求が・いつまでに、という形式で書く。
 読者が「自分がこの担当者だったら」と感じられる具体的な状況にする。
 著者の口癖を1箇所入れる。]

[変更要求を受けて検索をかけるシーンを書く。
 「〇箇所ヒットした」という数字を出す場合は、grep等の根拠を示す。]

```
// 根拠の示し方の例：
$ grep -r "[検索キーワード]" .
[ファイルパス1]:[行番号]
[ファイルパス2]:[行番号]
[ファイルパス3]:[行番号]
// → [N]か所ヒット
```

---

## ステップ2：課題の発見
> 変更要求を受けて「何が難しいのか」を具体化する

### 〇.3 変更しようとしたときに現れる困難

[変更要求を頭の中で試してみる。
 素朴に手をつけようとしたとき、何が立ちはだかるかを描く。
 観察は「起きていること」を動詞文で書く。評価・断定は混ぜない。
 読者が「あ、それは自分も詰まる」と感じられる具体性で。]

- **困難1**：[具体的に何が難しいか（事実として）]
- **困難2**：[別の角度からの困難（事実として）]

> 「[困難を受けて生まれる問い。なぜこんなに難しいのか？]」

[問いを立てるだけでよい。
 「まだ答えを出しません」「ここで答えは出しません」「ここで答えは出さない」は使わない。
 問いを置いたら次のステップへ進む。]

---

## ステップ3：原因特定
> 「なぜ難しいのか」の根本を突き止める

### 〇.4 困難の根本にあるもの

[以下の切り口から、このパターンに最も自然なものを1つ選んで展開する。
 使わない切り口のブロックは出力から削除する。]

<!-- ────────────────────────────────────────
  切り口A：変化の混在（Strategy / Observer / Template Method / Command / Decorator）
  「変わり続けるもの」と「変わってほしくないもの」が同じ場所に混在している。

  切り口B：依存の過多（Facade）
  使う側が「知らなくていいこと」まで知っている。

  切り口C：状態と振る舞いの混在（State）
  「状態が何か」と「そのときに何をするか」が同じ場所にある。

  切り口D：生成と利用の混在（Factory Method）
  「何を作るか」の判断と「どう使うか」の処理が同じ場所にある。
──────────────────────────────────────── -->

#### [原因の見出し（この章に即した具体的なタイトル）]

[選んだ切り口に沿って原因を説明する。
 「〇〇と△△が同じ場所にいる」という形式で原因を表現する。
 〇.3の困難と直接結びつけて書く。]

[「知らなくていいこと」を特定する場合は、起点コードの該当行を引用して
 `// ← 知らなくていい` コメントで明示する。
 または以下のテーブルで行・フィールド名レベルで示す。]

| 本来知っていればよいこと | 知らなくていいのに知っていること（行・箇所） |
|:---|:---|
| [意図・業務的な目的] | [具体的なコードの行・フィールド名] |

> **ここで立ち止まって考える**
>
> 「[原因となっている構造]を解消できたら、何が変わるでしょうか？」
>
> [「〇〇の要求が来ても、△△には触れずに済む」という形で具体的に書く。]

---

## ステップ4：対策案の検討
> 原因から論理的に案を導く

### 〇.5 最初の試み：【試行コード】

[〇.4で特定した原因を受けて、人が自然に最初に思いつくアプローチを示す。
 著者の口癖を1箇所入れる。
 「最初の試みとして〜」という導入にする（「まず〜」は使わない）。]

[このアプローチが原因にどう対処するかを導入文で示す。]

```cpp
// 【試行コード】① [クラス名]
// [ファイル名]
// コメント＝「なぜこの構造にするのか」
// コメント＝「何を解消して、何が残るか」
```

[試行コードを使った呼び出し元の変化を示す。]

```cpp
// 【試行コード】② 呼び出し元の変化
// [ファイル名]（変更後）
// コメント＝「何が変わって、何がまだ残っているか」
```

[依存注入のコードを示す（コンストラクタで受け取る形）。]

```cpp
// 依存注入：[クラス名] はコンストラクタで [インターフェース名] を受け取る。
// 本番では [実装クラス] を、テストでは Stub を渡す。
class [呼び出し元クラス] {
public:
    explicit [呼び出し元クラス](
        [インターフェース名]& [フィールド名],
        [依存クラス]& dep
    ) : [フィールド名]_([フィールド名]), dep_(dep) {}
    ...
};
```

[このアプローチで解消されること・残る課題を率直に書く。
 何が「まだ残っているか」を次の解決コードへの橋渡しとして示す。]

#### 試行コードのテスト

```cpp
// スタブ：本物の[外部サービス名]を呼ばずに「あらかじめ決めた値を返す」差し替えクラス。
// [インターフェース名] を継承することで [呼び出し元クラス名] に渡せる。
class Stub[クラス名] : public [インターフェース名] {
public:
    // テスト用の戻り値をここで決める
    [戻り値の型] [メソッド名](...) override {
        return [固定の戻り値];
    }
};

// MockDatabase は「[メソッド名] が呼ばれたかどうか」と
// 「渡された値」を記録するだけのクラス（モック）。本物のDBには一切アクセスしない。
class MockDatabase { ... };

TEST([テスト名], [テストケース名]) {
    Stub[クラス名] stub;
    MockDatabase mockDb;
    [呼び出し元クラス名] target(stub, mockDb);
    target.[メソッド名](...);

    // EXPECT_[マクロ名](期待値, 実際の値)：
    // 「等しければテスト通過」という検証（アサーション）。Google Test のマクロ。
    EXPECT_[マクロ名]([期待値], mockDb.[記録値]());

    // 試行コードの残課題がここに現れる：
    // [何がまだテストに漏れているかをコメントで示す]
}
```

---

### 〇.6 発想の転換：【解決コード】

[試行コードで「残った課題」を受けて、発想の転換を示す。
 「〇〇だけでなく、△△まで一か所に集めると何が変わるか」という橋渡しを書く。
 ここで初めてパターン名を出す。著者の口癖を1箇所入れる。]

```cpp
// 【解決コード】① インターフェース
// [ファイル名]
// コメント＝「呼び出し元が知る必要があるのはこの形だけ」
```

```cpp
// 【解決コード】② 実装クラス
// [ファイル名]
// コメント＝「[外部サービス]への全知識がここに収まる」
```

```cpp
// 【解決コード】③ 呼び出し元の変化
// [ファイル名]（変更後）
// コメント＝「[外部サービス]についての知識がゼロになった」
```

[依存注入：本番とテストでの差し替え方を示す。]

```cpp
// 依存注入：[呼び出し元クラス] はコンストラクタで [インターフェース名] を受け取る。
// インターフェース経由なので、本番では [実装クラス] を、テストでは Stub を渡せる。
class [呼び出し元クラス] {
public:
    explicit [呼び出し元クラス](
        [インターフェース名]& [フィールド名],
        [依存クラス]& dep
    ) : [フィールド名]_([フィールド名]), dep_(dep) {}
    ...
};

// 本番コードでの組み立て例
[実装クラス名] impl;
[依存クラス名] dep;
[呼び出し元クラス名] target(impl, dep);
target.[メソッド名](...);
```

```mermaid
classDiagram
    %% 解決コードの構造を図示する
```

*図が表示されない環境のために補足します。*
[Mermaid図の構造を1〜2文で説明する。]

> 「この構造」を、先人たちは **[パターン名]パターン** と呼んでいます。
> 名前は、論理的に辿り着いた構造へのラベルです。
> 覚えることが目的ではありません。

#### 解決コードのテスト

[試行コードのテストと何が変わったかが伝わるように書く。]

```cpp
// [テストで[外部サービス]の知識が消えたことをコメントで示す]
class Stub[解決クラス名] : public [インターフェース名] {
public:
    [戻り値の型] [メソッド名](...) override {
        // URLもフィールド名もここには登場しない
        return [固定の戻り値];
    }
};

TEST([テスト名], [テストケース名]) {
    Stub[解決クラス名] stub;
    MockDatabase mockDb;
    [呼び出し元クラス名] target(stub, mockDb);
    target.[メソッド名](...);

    // [呼び出し元クラス]は[外部サービス]の詳細を何も知らない
    // 「[業務的な意図]したか」だけをテストできる
    EXPECT_[マクロ名]([期待値], mockDb.[記録値]());
}
```

---

## ステップ5：天秤にかける・決断する
> 基準を先に宣言し、各案を等価に比較した上で決断する

### 〇.7 比較の基準を先に宣言する

[比較を始める前に「何を重視するか」を明示する。
 基準を後から決めると、結論ありきの比較になってしまう。
 〇.2の変更要求・〇.3の困難と結びついた基準を選ぶ。]

今回の状況で私が判断に使う基準は次の通りです。

| 基準 | なぜこの状況で重要か |
|:---|:---|
| [基準1] | [理由] |
| [基準2] | [理由] |
| [基準3] | [理由] |

---

### 〇.8 試行コードと解決コードをテストで比較する

[〇.7の基準それぞれについて、試行コードと解決コードがどう違うかを示す。
 コードの差（何がどう変わったか）から判断の根拠を導く。]

#### 比較のまとめ

| 基準 | 試行コード | 解決コード |
|:---|:---|:---|
| [基準1] | [○ / △ / ×] 理由 | [○ / △ / ×] 理由 |
| [基準2] | [○ / △ / ×] 理由 | [○ / △ / ×] 理由 |
| 実装コスト | 少ない（既存の小修正） | 多い（設計が必要） |
| **この状況に合うか** | [合う条件] | [合う条件] |

*この比較はあくまで「今回の状況と基準」に対するものです。
別の状況・別の基準であれば、違う選択が正解になります。*

---

### 〇.9 より難しい変化への耐久テスト

#### 新たな状況

[同じドメイン・同じシステムへの、より難しい変更要求。
 〇.2と同じ形式（誰から・何・いつまで）で書く。別ドメインは使わない。]

> **〇.5〜〇.6で導いた構造は、この変化にも通用するでしょうか？**
>
> 少し立ち止まって、考えてみてください。

[試行コードと解決コードがそれぞれこの変化にどう対応するかを示す。]

#### 【深化コード】

```cpp
// 【深化コード】
// [ファイル名]
// コメント＝「どこをどう拡張したか」
```

#### 深化コードのテスト

```cpp
// 深化コードのテスト
// [呼び出し元は新しい外部サービスについても何も知らずに書けることを示す]
```

*この耐久テストを経て、この状況では解決コードが合っていると判断できます。
〇.5〜〇.6で論理的に導いた構造が、難しい変化にも通用することが確認できました。*

---

### 〇.10 使う場面・使わない場面

[「では、解決コードを常に選べばいいのか？」という問いでつなぐ。
 著者の口癖を1箇所入れる。]

#### 【過剰コード】：解決コードを使いすぎた例

```cpp
// 【過剰コード】
// コメント＝「何を入れすぎているか・なぜこれが問題か」
```

[何が問題かを言語化する。責めるトーンにしない。]

#### 状況ごとの選択指針

| 状況 | 適切な選択 | 理由 |
|:---|:---|:---|
| [解決コードが適する状況] | 解決コード | [理由] |
| [試行コードが十分な状況] | 試行コード | [コスト効率] |

> **どの形を選んでも守る一線**
>
> [[原因を放置したまま変化を直書きで追加し続けることだけは避ける] という形で書く。]

---

## この章で踏んだ思考の整理

### 〇.11 5ステップとこの章でやったこと

| ステップ | この章でやったこと |
|:---|:---|
| **1. 現状把握** | [仕様・起点コード・変更要求をひとつの状況として把握した] |
| **2. 課題の発見** | [変更しようとして現れた具体的な困難を言語化した] |
| **3. 原因特定** | [困難の根本にある構造上の問題を突き止めた] |
| **4. 対策案検討** | [試行コードで部分解消し、解決コードで根本を解消した] |
| **5. 天秤・決断** | [基準を宣言し、テストで比較し、この状況に合う形を選んだ] |

この思考の結果として辿り着いた構造を、
先人たちは **[パターン名]パターン** と呼んでいます。
一つの参考として受け取っていただければと思います。
