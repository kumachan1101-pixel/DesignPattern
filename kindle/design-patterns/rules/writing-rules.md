# rules/writing-rules.md
# 執筆品質ルール詳細
# CLAUDE.md から参照される。
# 対応フレームワーク：7フェーズ・接続点レビュー

---

## 基本姿勢

テンプレートの項目を箇条書きで埋めるだけでは足りない。
各項目は、読者が「ここまで読んだので次へ進める」と感じる水準まで展開する。

各項目では、次の流れを意識する。

1. なぜ今これを確認するか。
2. 何を材料に見るか。
3. 何が分かったか。
4. 次の項目で何に使うか。

## 目的と達成基準

各標準見出しの直下に、本文へ出さない執筆用コメントとして `目的` と `達成基準` を置く。

- 目的：この項目は読者の理解を何のために進めるのか。
- 達成基準：読み終えた読者は何を自分の言葉で説明・予測・判断できるべきか。

目的を達成しない説明、図、表、コード、実行結果は置かない。
達成基準に関係しない一般論や先取りは削る。

## 短段落で積み重ねる

1段落は1〜3文に収める。
長い段落は分割する。
「この文で何かを伝えたら、次の段落で別のことを伝える」という積み重ね方をする。

## 章の独立性

第0章を読んでいれば、どの章から読んでも理解できる完全自己完結の構成にする。

- 「前章で学んだように」は使わない。
- 「第〇章と同様に」は使わない。
- 次章予告は書かない。

## 図解ルール

文章だけで全体像を説明しきろうとしない。
次の箇所は、できるだけ図または表で示す。

- 仕様で、何を突き合わせて何を作り、何を返すかという業務処理の流れ。
- クラス責任と依存関係。
- 変更影響の広がり。
- 接続点で流れる値・型・操作・イベント。
- 改善前後の差分。
- 責任移動。

図を置くときは、次を守る。

- 図の前に、読者が図を見る目的を1文で書く。
- 図に出るクラス名・値・状態名は、本文または表で説明済みにする。
- 図の後に、図から読み取るべき結論を1〜3文で回収する。
- 図だけで新しい仕様や判断を初出ししない。
- 責任は、変更理由に結びつく仕事の束として導く。4-1では「変更理由の一覧 → 変更理由×修正場所のマトリクス → 責任」の順に書き、責任名から書き始めない。修正箇所が複数あること自体は問題とせず、一つの場所へ複数の変更理由が集まっていることを混在として扱う。
- flowchart / graph の`［変更］`は場所の印として使い、同じノードへ変更内容を具体的に書く。数や方法が変わるものは`変更前→変更後`、項目が加わるものは変更後の全項目を並べて加わった項目へ「（追加）」を付ける。
- 本文には図を採用した編集上の理由を書かず、変更前後のシステム事実と図で確認する対象を書く。

## 仕様説明ルール

現状の仕様を文字だけで箇条書きにしない。
1-1の最初の具体物は、代表操作の`main()`抜粋と対応する実行結果の一組とする。複数回呼ぶ場合は、各呼び出しコードの直後へその結果を置き、「全呼び出し→全結果」と分離しない。コードより前に、本書共通の模擬環境（コンストラクタがサンプルデータをメモリへ作る、`main()`が操作を直接呼ぶ、画面や外部サービスの代わりに標準出力へ出す）を章の具体クラス名で短く確認する。結果の読み取りでは、出力に文字として現れた事実と、コード・状態表から分かる内部処理を区別する。

仕様の処理順は、`何と何を突き合わせる → 何を決める → 何を作る／更新する → 何を返す`を章の業務語でつないだ図で見せる。`入力・判定・加工・出力`という分類名を見出し・表の軸・箱の名前にせず、箱だけを読んでも具体的な情報、判断、生成物、更新結果が分かるようにする。1-1の現状図と1-5の変更後図は同じ骨格・同じ語彙を使い、差分表を重ねない。
仕様変更で外部API、タイマー、永続化、非同期受信、画面などが新たに必要になる場合は、現状の簡略化とは分けて1-5に`実システムの変更を、掲載コードではどう再現するか`を置く。実システムの変更対象、掲載コードの代替、この章で省くものを対応づけ、省略してもコードで動かす処理を明記する。簡略化自体が仕様変更へ加わるように読める見出しは使わない。

## コード提示ルール

コードブロックの前に、読む目的と注目点を書く。
コードブロックの後に、コードから分かったことを1〜3文で回収する。

コードは責任の固まり（入力データ・計算・組み立て・実行など）ごとに分割して掲載する。
各コードブロックの直後に、そのブロックが仕様のどの部分を担い、何をしているかを箇条書きで補う。
プログラミングに不慣れな読者が、ブロック単位で「どの仕様か・何をするか」を追える水準にする。
長いメソッド（計算・判定）は、処理の固まりにコメントや区切りを入れ、各固まりを仕様のルールへ対応づける。

専用の組み立てクラスは、部品の生成・所有・寿命管理・登録・配線と、組み立て済みの公開入口を渡すことだけを担う。実行時の入力検証、動作例の進行、ドメイン状態の更新、結果表示を同居させない。登録順が業務上の競合方針を表す場合は、その順序を構成として保持してよいが、実行時の判断や処理まで広げない。

コード内の空行は、行数を増やす装飾ではなく**処理の段階を示す境界**として使う。関数内では、次の段階が切り替わる場所に1行だけ空ける。

1. 入力・依存先の取得
2. 妥当性判定と早期return（連続するガード条件は一群にする）
3. 実行対象の選択・計算
4. 状態変更・保存
5. 通知・表示・結果返却

同じ段階の代入や連続するガード条件の間には空行を入れない。閉じ波括弧の直後に別の判定・実行・返却が続く場合、または準備した値を使って新しい判定へ入る場合は空行を入れる。短い関数でも「判定」と「正常時の実行」が切り替わるなら省略しない。

現状コードの説明では、原因や責任外の判断を先取りしない。
特に1-4の実装コード説明では、構造分析や問題提起をせず、コード上の事実と仕様との対応だけを説明する。
原因分析の後で初めて、どの行にどの判断や前提が漏れているかを説明する。

3-1の冒頭では、フェーズ2から引き継ぐ変更IDと維持する動作を一、二文で示し、変更途中コードへ進む。`変更ID / 修正対象 / 変更内容`のようにコードを予告するだけの表は置かない。一方、状態遷移や外部APIの入出力などを共通の軸で比較し、コードから問題を発見する表は利用できる。

### 掲載単位（1定義1ブロック）

長いコードを一度に見せない。**掲載の最小単位は「1つの定義」**である。第9章 `output/chapter09_2.md` の1-4・3-1・7-1を基準実装とする。適用範囲は1-4（現状コード）、3-1（変更を試みる）、フェーズ6の断片、7-1（完成コード）で、この4か所は同じ書き方でそろえる。

#### 単位の形

各単位は必ずこの順で置く。

1. 見出し（定義名だけの太字）
2. 1行の導入 ―― このブロックで何を見るか
3. コードブロック
4. 説明
5. `---` の区切り線

区切り線を省くと、説明が上のコードのものか下のコードのものかが読者に分からなくなる。導入を省くと、読者は目的を持たずにコードへ入ることになる。

#### どこで分けるか

**1ブロック＝トップレベルの1定義。** 行数では分けない。「長いから割る」「短いからまとめる」はどちらも誤りである。`class`、`struct`、通常の`enum`、`enum class`、`namespace`は、それぞれ1つずつ別のコードブロックへ置く。同じ契約を実装する短い兄弟クラスも同居させず、共通の導入の下へ1クラスずつ隣接して置いて比較する。見出しには直下のブロックで定義する1つの名前だけを書き、複数クラスをまとめた見出しを最初のクラスへ流用しない。共通ヘッダー、自由関数、`main()`も、型定義へ詰め込まず役割ごとに分ける。

クラス内に短いメソッドを実装込みで置くことは、そのクラス1つの定義なので分割違反ではない。宣言と定義を分ける基準は次項に従う。

**ブロックはトップレベルの型をまたがない。** 判定に迷ったら次で決める。

> **そのブロックが1つの見出しで説明できないなら、それはまとめすぎである。**

#### 宣言と定義を分ける基準

原則は分けない。分けるのは次の一行で判断する。

> **メンバーを見ないと読めない関数は、メンバーと一緒に置く。**

つまり、**手順を持つ関数（制御フロー・複数ステップの処理）があるクラスだけ**、宣言ブロック（メンバーと公開操作の一覧）と、定義ブロック（関数1つにつき1ブロック）へ分ける。それ以外のクラスは、メンバーと処理を同じブロックへ置く。定義だけを並べると継承関係とメンバーが見えなくなるので、機械的に全クラスを分けてはならない。第9章では19クラス中、分けたのは `TicketService` と `TicketManager` の2つだけである。

#### 掲載ブロックと実ファイルを混同しない

コードブロックは読解単位、`.h`／`.cpp`は依存・変更・ビルドの単位であり、同じ判断ではない。本書は動作確認のため、各章のブロックを上から連結すると1つの`.cpp`になる形を正本とする。一方、実務の配置は次を基本にする。

- `.h`：利用側へ公開する契約、クラス宣言、引数・戻り値に必要な型
- `.cpp`：メソッドの処理本体と外へ公開しない補助処理
- `main.cpp`：実体の生成・所有・登録・注入と公開入口の呼び出し

インターフェースのように処理本体を持たない型は、宣言と短い定義をヘッダーへ置き、対応する`.cpp`を作らない構成もある。小さな値の型は、同じ機能境界で一緒に変更される場合だけ`OrderTypes.h`のような共有ヘッダーへまとめてよい。行数や「同じ画面に収まるか」でファイルを決めない。第0章「掲載ブロックと実ファイルの分け方」で、掲載用1ファイルと実務配置の違いを読者へ説明する。

#### 見出しに書かないこと

見出しは定義名だけにする。クラス名、または `Class::method()` である。

- 「（継承なし）」「`: public Base`」など、コードを見れば分かる情報は書かない。
- 役割の要約を見出しへ足さない。役割は導入の1行で書く。
- 兄弟クラスの片方だけを分けない。`EscalatedPhase` を単独ブロックにするなら、`ResolvedPhase` と `PendingPhase` も単独にする。

#### 説明の深さ

**1行が下限である。** 名前を言い換えるだけの説明（「`assign()`：担当者を割り当てます」）は、コードを読めば分かるので置かない。消すか、意味のある説明へ変えるかのどちらかにする。

1行を超えて書いてよいのは、次のいずれかがあるときだけである。

1. 判断が2つ以上ある
2. 順序に意味がある（入れ替えると壊れる）
3. 失敗の扱いがある
4. 所有権・生存期間・前提がある

#### `main()` と実行結果

`main()` と実行結果は、全定義を説明した後、最後に置く。`main()` を行のまとまりごとに区切り、**各まとまりの直後に対応する出力を置く**。離れた `main()` と出力を読者に往復させない。

掲載したブロックを上から順に連結すれば、そのまま1つのプログラムとして動くようにする。これは `script/check_execution_output.py` が1-4と7-1について機械で確認する。

コンパイル方法、掲載コードを1つの `.cpp` にする理由、`main()` の組み替え方、インメモリ保存の寿命は、第0章「掲載コードを手元で動かす」で一度だけ説明する。実践章へ「手元で動かすには」の囲みを繰り返さない。章固有の入力や操作は、`main()` と対応する実行結果そのものから読めるようにする。

### 接続点コードの略記禁止

接続点（変わる側と守る側の境界）に当たるコードは、**絶対に略さない**。「← 他のelse ifブロックがここに続く」「〜も同様に並ぶ」「…が続く」のような省略は、読者から判断材料そのものを奪う。同じ構造の分岐が並ぶこと自体が接続点の重要情報なので、全分岐を記載する。接続点以外の周辺コード（検証・保存など）を省く場合は、コードブロックの外で維持条件として明記する。

### 関数・データの記述スタイル

- クラスのデータメンバと各関数の先頭には、役割を示すコメントを付ける（全章共通）。
- 同型で並列な極小関数（1行のエラー出力など）は、揃えた1行形式で並べてよい。並列関係が縦に見え、Kindleの80字制限内に収まる場合に限る。
- それ以外の関数は、1行に潰さずブロック（複数行）で書く。Kindleでは横幅が狭く、1行に詰めた実装は読みにくい。
- 同じコードを別の節で再掲するときは、行の折り返し・ブロック構造を変えない。「おさらい」と言いながら形が違うと、読者は別のコードだと誤解する。

### クラス図の記述スタイル

- クラス図で他と線がつながらないクラスを置く場合、「誰が使うか」の注記（note）または直後の本文説明を必ず添える。
- 矢印のラベルは依存の目的説明（「商品情報を取得」など）として許容する。時間順のやり取りはシーケンス図で示す（0章「クラス図の読み方」参照）。
- 一枚で関係を追いにくいクラス図は、クラス数を保ったまま縮小せず、「データと処理」「契約と具体」「生成・所有」「利用側との接続」のように、読者が確認するポイントごとの部分クラス図へ分ける。各図の前に今回見る関係、後に読み取れることを書き、複数図を合わせたときに登場クラスが欠けないことを確認する。

#### クラス図の線種（全章共通）

関係の種類ごとに線種をそろえる。実コードの関係と一致させる。

| 関係 | Mermaid記法 | 見た目 | 使う場面 |
|---|---|---|---|
| 実現（インターフェース実装） | `<\|..` / `..\|>` | 破線＋白三角 | 具象がインターフェース契約を満たす |
| 継承（クラス拡張） | `<\|--` / `--\|>` | 実線＋白三角 | 具象が抽象・基底クラスを継承する |
| 依存（生成・一時利用） | `..>` | 破線矢印 | 生成して一時的に使い、メンバに保持しない |
| 関連・集約・合成（保持） | `-->` / `o--` / `*--` | 実線 | メンバとして保持する |

- C++に`interface`キーワードも`abstract`キーワードもない。純粋仮想関数を1つ以上持つクラスはC++の型として抽象クラスになる。そのうち、データメンバや業務上の共通実装を持たず、公開する業務操作を純粋仮想（`= 0`）で示す型だけを、本書では`<<interface>>`と書く。仮想デストラクタの既定実装は持てる。この場合、具象からの線は**実現**（`<|..`／破線白三角）にする。
- 掲載コードではlambdaとスマートポインタ（`unique_ptr`、`shared_ptr`、`make_unique`）を使わない。生ポインタが所有関係なら所有者のデストラクタで破棄し、借用するだけなら生存期間を本文で説明する。
- 抽象クラス（一部を実装し、一部を純粋仮想で残す）には`<<abstract>>`を書き、基底に継承する線は**継承**（`<|--`／実線白三角）にする。純粋仮想関数がなく直接生成できる通常の基底クラスにはステレオタイプを付けず、同じ実線白三角を使う。
- 派生側のC++構文は、契約の実装でも実装継承でも`: public 基底型`になる。型名の`I`や派生宣言だけで線種を決めず、基底型にデータ・共通実装・純粋仮想関数があるかを確認する。
- 生成メソッド（ファクトリー等）で作って一時的に使い、メンバに保持しない関係は**依存**（`..>`／破線矢印）にする。参照・所有としてメンバに保持する関係は実線（関連・集約・合成）にする。
- `classDiagram`は原則として`direction TB`で統一する。横向きが不可欠な例外は、図の目的と例外理由を本文に明記する。各型は保持・生成・引数・戻り値・呼び出しのいずれかで関係線へ接続し、説明のない孤立型を作らない。

### フェーズ6のコード提示

フェーズ6は、フェーズ5の全課題から**一つの実行可能な構想**を作る場所である。契約だけ、生成だけ、実行だけを別々に完成させない。フェーズ5で決めた責任配置を出発点に、判断の根拠を示しながらコード構造へ具体化する。

フェーズ5の課題カードにある`接続`が、業務上の依頼・入力・結果／反映の正本である。フェーズ6の契約では、それをクラス／インターフェース、メソッド、引数型、戻り値型へ翻訳する。同じ入力・結果を候補表や利用先別一覧で再選別しない。共通の型が必要かは具体実装の数と利用側の扱いから判断し、一実装しかなく共通化の根拠がなければ具体クラスまたは関数で表す。

#### 分離した責任をコードの構造へ変える

冒頭では、フェーズ5で何の責任をどこから分け、何でつなぐと決めたかを短く再確認する。そのうえで、次の順に決めると予告する。

- 契約と代表具体
- 具体の生成者・所有者・生存期間
- 必要な登録・選択と、その判断材料
- 利用側へ渡す実際の操作
- 公開入口から契約・具体・結果までの呼び出し順

冒頭に、まだ説明していない最終クラス名・生成場所・完了判断を表で先出ししない。クラス名、操作名、関係は、それを決める理由を説明する位置の部分クラス図とコードで初めて確定する。

登録は候補を預けること、選択は候補から使う実体を決めること、注入は外で用意した実体を利用側が契約として使う場所へ渡すことである。同じ語でまとめず、章で実際に行う操作名を書く。

#### 構想をコードでつなぐ

本文は次の順に連続させる。

1. `#### 契約：課題の入出力をC++の型と操作にする`
2. `#### 具体：契約の裏へ変わる判断を置く`
3. `#### 生成・選択・受け渡しを決める`
4. `#### 実行骨格：組み立てた実体を契約から呼ぶ`
5. `#### 公開入口から具体の実行まで追う`

変更前コードはフェーズ3「変更を試みる」で確認済みなので、フェーズ6では再掲しない。必要ならクラス名・メソッド名を示してフェーズ3の観測結果を一文で受ける。フェーズ6のコードは契約のシグネチャ、所有・受け渡し、契約経由の実行を証明する最小行に絞り、具体クラスの処理本文、同型の全具体、完成コード、実行結果を置かない。

契約節の冒頭では、フェーズ5の`接続`を一文で受け取り、その項目がどのC++型・操作・引数・戻り値になるかだけを決める。課題定義で確定した業務上の入出力を増減させる必要が生じた場合は、フェーズ5へ戻って課題カードを直してから進む。

フェーズ6のC++断片には、所属する定義・メソッドと、その抜粋で分かることを直前へ置く。既存コードを参照する場合は`変更前の Class::method()：境界候補がある箇所`、構想のコードなら`IContract：境界で約束する操作`のように、定型ラベルではなく内容を直接示す。

一つのC++ブロックにはトップレベルの型を一つだけ置く。入力・取得、判定、選択・計算、保存、通知・返却の意味が切り替わる箇所は空行で区切る。

#### 構想を確定する

最後は、採用する責任配置と原因が残る形を一、二文で確定し、リスクIDに対する変更先と限界だけを確認する。フェーズ6冒頭の責任→クラス対応図を`課題 / 決定した構造 / コード上の実現`の表へ書き直さない。完了条件の判定は、完成コードと実行結果がそろうフェーズ7へ送る。完成可能な構想が一つなら比較せず、責任配置や依存関係が異なる複数構想が残る場合だけ、変更影響・導入コスト・運用規則で比較する。

責任、継承・実現、保持・注入を決める節では、今説明している関係だけの部分`classDiagram`を対応コードと組にしてよい。図の前に対象範囲を書き、フェーズ7で、これらを統合した完成クラス図を一度だけ示す。完成クラス一覧、実行シーケンス、全コード、実行結果はフェーズ7で初めて示す。完成図の直後では、フェーズ4で一つのクラスに混在していた責任が最終的にどのクラスへ分かれ、元のクラスに何が残ったかを、同じ責任名で明記する。ここでいう一つの責任は、一つのメソッドではなく一つの業務上の変更理由である。

問題から導いた構造名を使い、デザインパターン名を対策理由にしない。実装と効果を確認した後で、得られた構造を共有語彙としてパターン名へ結び付ける。

## 実行結果ルール

実行結果は、最終値だけを並べない。
読者が「何を・いくらで・誰が」入力し、「どう変わって」その値になったかを追えるようにする。

- 入力（対象データ・金額・会員種別など）と結果（小計→支払金額、状態遷移など）をセットで出力する。
- 内部の件数・残高・在庫・予約数を変更するときは、対象IDと「変更前→変更後」（必要なら上限・単位）をログへ出す。結果名だけで数値変化を隠さない。
- 将来リスクにはリスクID1から連番を付ける。2-5では、フェーズ3で試す変更IDと今回維持する動作を確定する。責任の分離、境界、差し替え対象、完了条件はフェーズ5に置く。フェーズ3は変更IDを現状構造へ当て、維持する動作を担うコードまで修正・確認が広がるかを観測する。未確定のリスクID自体は変更として持ち込まず、対策検討で同じリスクIDを採用構造へ再適用する。リスクIDを明示的に扱うのはフェーズ2と6だけとし、フェーズ7にも持ち込まない。リスクIDは実装要求ではないため、未確定機能を完成コードや実行結果へ追加しない。
- 現行要求と変更後要求の種別は、要求の利用目的・外部契約・受入条件で決める。今回の計算や状態遷移で出力値が変わるだけなら継続要求であり、表示項目・履歴・操作を設計都合で追加して要求変更にしない。反対に、拒否が待機登録へ変わるなど受入条件が変わるなら変更要求として追う。
- システムが後続して行う昇格・通知・補償・履歴記録を、`main()` や運用者による別操作として実装しない。起点となるユースケースから自動的に呼ぶ。
- 外部システムが後から呼ぶコールバックを掲載コードの `main()` で模擬する場合は、実運用と模擬を対比する。実運用の時間経過・非同期通信・外部受信口と、掲載コードが再現する別呼び出し・受付ID照合・状態遷移を同一視させない。構造説明では、時間表現だけに頼らず「別経路の最終結果」と呼ぶ。
- 状態が変わらないエラーは状態遷移図の矢印に含めず、エラー条件表と実行結果で示す。
- 検証・照会メソッドが成否や結果を返す場合は、呼び出し側で判定・保存・返却のいずれかに使う。戻り値を意図的に持たない命令は`void`とし、結果を捨てているように見える呼び出しを残さない。
- 変更影響グラフは、フェーズ3とフェーズ7で同じ変更ID・同じ構造粒度を使う。入力型、クラス、`main()`／組み立て、生成・登録のうち実際に変えるものを省かず、回帰確認だけの箇所は変更箇所と分ける。改善を枠数の減少へ固定せず、変更理由が安定側から専用部品へ移ったことを説明する。フェーズ3の箱には①から番号を付け、フェーズ4の`図の番号 / その場所が見ている情報 / 該当実装 / 責任としての読み方`表へ直接対応させる。
- 「ここでは〜しません」「まだ〜を決めません」のような執筆手順の否定形を本文へ繰り返さない。現在行う判断と次に使う成果を肯定形で書く。エラー時に状態を変えない、対象外の外部基盤を模擬するなど、仕様・実装範囲を正確にする否定文はこの制限の対象外である。
- 「上の三つのうち一つめ」のように、番号だけで離れた文章を参照しない。図の番号と箱の名前、または参照する具体的な内容をその文に書く。「以降はこの表だけを見る」のような読者の操作を指示する内部メモも本文へ置かない。
- 実行コードの見出しは実際の所有者へ合わせる。`main()` が `Application::run()` を呼ぶだけなら、シナリオ本体と結果は `run()` のものとして示し、最後に `main()` の起動コードを分ける。
- 割引・加算・状態変化など、施策や操作の効果が変更前後で読み取れる形にする（例：`小計 10000円 → 支払金額 8000円`）。
- 実行結果の直前に、実行対象コード・対応する動作例ケース・確認したいことを書く。
- 複数ケースを並べるときは、ケースの区切りと、どのコード（現状／変更後／最終）の結果かを示す。
- 1-1で複数回呼ぶ場合は行数にかかわらず、呼び出しコードをケース単位へ分け、各コードの直後へ対応する結果を置く。ほかの節でも、実行結果が複数ケースにまたがる場合や1ブロック25行以上になる場合は同じペア型にする。一括`main()`の後に全ケースの結果をまとめない。
- 最終値の羅列だけにして、「何が起きたか」を読者の推測に委ねない。

## フェーズ4の問題・責任・原因

- フェーズ3で問題IDと問題箇所を確定し、4-1はその場所を入力として、混在する責任を対策前の部分クラス図へ示す。問題箇所の特定や全処理の再列挙を繰り返さない。
- 責任は、フィールドやメソッドではなく、同じ業務上の理由で一緒に変わる仕事のまとまりとする。`販促施策`、`待機順の管理`、`在庫の更新`のように、実務で通じる短い名前を使う。
- 図の直後に、各責任が担う仕事と、今回直接変わった責任／同居のため巻き込まれた責任を短く説明する。
- 4-1で、責任の同居や具体への直接依存が問題IDを生んだ証拠を確認し、原因IDを付ける。責任が複数あるだけでは原因にしない。重複する4-2は置かない。
- フェーズ5の目標図はフェーズ4の対策前図と同じ責任名・同じ粒度を使う。フェーズ6では同じ責任名をクラス欄に残し、`責任名→C++の型・クラス・操作`へ置き換える。

## コードラベルのルール

| ラベル | 用途 |
|:---|:---|
| `// ← 知らなくていい` | 原因分析後に、呼び出し元へ漏れている判断や前提を示す |
| `// ← ここだけ変わる` | 改善後に、変更影響が局所化された行を示す |
| 【深化コード】 | より難しい変化への対応 |
| 【過剰コード】 | 使いすぎた失敗例 |

`// ← 知らなくていい` は、フェーズ4以降の説明根拠があるコードで使う。
フェーズ1の現状コードには入れない。

## パターン名の扱い

パターン名は、対策を決める前の選択肢ラベルや、対策を指示する動詞の目的語として使わない。「Strategyを適用する」「Stateを使う」のように名前から設計行為を表す文は避け、必要な責任移動・境界・組み立てを述べる。
章タイトルでは、読者の検索性と期待値合わせのためにパターン名を出してよい。ただし「得られること」、ステップ名、要約表、本文中の判断理由では、パターン解説前は本文で導いた構造名を使う。
問題を解いた後、その構造をチームで共有する語彙として紹介する。「ここまで導いた構造は、一般に【パターン名】と呼ばれる」と接続する。
パターン解説の直前に、題材から導いた構造とパターン名を結ぶ移行文を置く。見出しだけで突然パターン名へ切り替えない。

## 章内参照の書き方

本文から別の項目へ読者を移動させるときは、番号だけで参照しない。初回は`フェーズ3「変更を試みる（3-1）」`のようにフェーズ名・項目名・番号を併記し、同じ段落や近接箇所で繰り返すときだけ`変更試行（3-1）`と短縮する。表の列名、要求IDの対応欄、コードの出典ラベルは番号だけでも対象が明らかなため、この規則の対象外とする。

## この章を読むと得られること

- 「〜できるようになる」または「〜がわかるようになる」形式で3〜4項目にする。
- パターン名を前面に出さない。
- 題材固有の業務機能を使えることではなく、別のシステムへ持ち帰れる構造判断を学習成果にする。
- 変動と安定の切り分け、依存・責任の原因特定、契約と生成・所有・受け渡しによる再結合、変更局所化と適用可否の検証を基本の流れとし、章で実際に扱うものだけを書く。
- 全章を同じ一般論にしない。交換可能な振る舞い、状態固有の判断と遷移、発生元と独立した受信側など、その章で導く構造の違いが分かる語を使う。
- 題材固有のクラス名・要求・実行結果は、章末で構造判断を獲得した根拠として使う。学習成果そのものにはしない。
- 章末は冒頭と同じ項目名で一対一に回収する。「どのフェーズに書いたか」だけでなく、どの事実・設計・検証が根拠になったかを書く。

## 禁止表現

| 禁止する表現 | 代替 |
|:---|:---|
| 文頭・段落冒頭の「まず〜」 | 「最初の試みとして〜」 |
| 「ここで答えは出さない」等 | 削除。問いを立てるだけでよい |
| 「次章では〜」等の次章予告 | 書かない |
| 「悪いコード」「スパゲッティ」 | 「この構造では〇〇が難しい」等の事実で書く |
| 「〜すべき」「〜べきです」 | 「〜が合うかもしれません」等 |

## 数字根拠ルール

「〇箇所」「〇ファイル」という数字を出すときは根拠を添える。
方法は、ファイル名列挙、差分表、または検索例とする。

## 依存注入コードルール

インターフェースを使う実装では、依存注入のコードを示す。

1. コンストラクタでの受け取り。
2. 本番コードでの組み立て例。
3. テストでの差し替え例。

## テスト文法説明ルール

スタブ・モック・アサーションマクロが初めて登場する箇所に、1〜2行の日本語コメントを添える。

```cpp
class IPayrollFacade {
public:
    virtual int calculateNetPay(int grossPay) = 0;
    virtual ~IPayrollFacade() = default;
};

// スタブ：本物のAPIを呼ばずに、あらかじめ決めた値を返す差し替えクラス。
class StubPayrollFacade : public IPayrollFacade {
public:
    int calculateNetPay(int grossPay) override {
        return grossPay - 1200;
    }
};

class PayrollCalculator {
    IPayrollFacade& payroll;
public:
    explicit PayrollCalculator(IPayrollFacade& payroll)
        : payroll(payroll) {}

    int calculate(int grossPay) {
        return payroll.calculateNetPay(grossPay);
    }
};

StubPayrollFacade stub;
PayrollCalculator calc(stub); // コンストラクタでスタブを注入する。

// EXPECT_EQ(期待値, 実際の値)：等しければテスト通過。
EXPECT_EQ(8800, calc.calculate(10000));
```

## Kindle品質ルール

- コードブロック：1行80文字以内。出版時の画像分割は、空行・メソッド末尾・クラス末尾を優先し、メソッドの宣言と本体、または閉じ波括弧だけを別画像へ分断しない。
- Mermaid図：テキストでも意味が伝わる説明を必ず添える。図の前は「次の図」、図の後は「直前の図」と対象を明示し、「この図」「上の図」「下の図」だけで参照しない。短い題名を付け、出版時は図番号付きキャプションとして画像と同じページに置く。縦一列の図がA5の1ページを占有する場合は、意味のまとまりを保って横配置または複数段へ組み直す。
- 第0章のクラス図の読み方は、複数の図だけを先に並べて後からまとめて説明しない。線の番号を付けた図の直後に、同じ番号の読み方とコードで確かめる箇所を置き、図と説明を一組にする。PDFでも一組を同じページへ置く凡例には、Mermaid内へ `%% explanation-set` を付ける。
- 第0章で、差分色（新規・変更・維持）と処理フローの形・役割色を、実際の小さな図で一度だけ示す。ここを全章共通の凡例とし、実践章では凡例を繰り返さず、その図固有の着目箇所・番号・差分だけを説明する。
- 図の参照に「上の三つのうち一つめ」のような相対表現を使わない。図中の番号と、箱・線・クラス・実装の具体名で参照先を特定する。
- コード抜粋の見出しへ「ここで確認するコード」という定型句を繰り返さない。`クラス名／関数名：その抜粋で分かること`の形で直接示す。
- 実践章の冒頭は`この章で解く設計課題`とし、題材固有の変更の広がり、原因となる知識の同居、この章で考える構造を2〜3文でつなぐ。`場面`・`兆候`・`判断軸`の定型ラベルで断片化しない。
- 現状システムと掲載用の代役・省略範囲は、`実システムと掲載コードの違い`という題で、代表入力と実行結果の直後にコラム（`> [!NOTE]`）として置く。仕様変更で新たに加わる要素も、同じく`実システムの変更を、掲載コードではどう再現するか`のコラムへまとめる。`この章での簡略化`のように、比較対象が見出しだけでは分からない名前を使わない。
- クラス図に主要属性・操作を書いた直後へ、同じ内容の「主なメンバーと操作」表を重ねない。図の後は、関係線から読み取れる責任・依存・孤立の有無を短い文章で説明する。
- 現状図と変更後図は、`入力・判定・加工・出力`の分類図にせず、何と何を組み合わせ、何を判断し、どの値・状態・通知を作り、何を返すかを業務語で一方向につなぐ。1-5では1-1と同じ骨格を保ち、差分表を重ねず、差分を色だけでなく `［新規］`・`［変更］`・具体的な変更内容でも読めるようにする。
- 責任名は、フェーズ4で変更試行中の処理を変更理由ごとにまとめて初めて付ける。フェーズ1・3へ原因分析後の責任名を先出しせず、フェーズ4では元のコード上の処理と責任名の対応を文章で示す。
- 掲載C++は標準ライブラリ名へ `std::` を明示し、`using namespace std;` を使わない。章や掲載単位によって名前空間の書き方を変えない。
- 出版前の画像埋め込み、外部リンク、発行日、版数、著者名の確認は出版タスクで管理し、読者向け本文へ編集者確認として残さない。
- 仕様図は正常系を先に描き、入力・判定・加工・正常出力を形状または色で区別する。成功側しか描かない入力検証はひし形＋`Yes`にせず、検証済み入力と処理順で表す。ひし形を使うのは正常系にも複数の有効な結果があり、図に全分岐を描く場合だけとする。異常系は同じ図へ混ぜず、エラー条件表で扱う。エラー条件表は正常系の各種仕様表をすべて示したあと、1-1の最後に置く。
- 脚注：使わない。
- 同じ対象を同じ観点で比較する情報は表にする。列名を各項目で繰り返すカード型の箇条書きへ機械変換しない。
- 説明用の表は原則3列以内にする。状態×操作、変更前×変更後、APIの引数差分など、縦横の対応自体を読む表だけ4列まで使ってよい。
- 表へ入れる前に、全行で同じ値になる列、本文と重複する列、読者の判断に使わない列を削る。同じ判定が全行で「合格」なら、判定列を置かず表の後に一度だけ書く。
- 1段落の長さ：5〜7行以内。
- コードブロックの前後：必ず説明文を置く。
- 「おわりに」は、設計判断・実務への持ち帰り・読者への提案を先に置く。著者自身の成長や制作上の苦労は、本編から離れることを一文で区切って終盤へまとめ、読者が先に著者の事情を読まされる構成にしない。

#### 節番号は、フェーズごとの初出で節名を併記する

`前節で決めた` のように対象を名前で示さない参照は、読者が何の話か思い出せず、節を行き来することになる（著者指摘 2026-08-30）。各フェーズで最初に参照するときは、`フェーズ5で確定した課題カード`のように成果物の名前を示す。同じフェーズの2回目以降は、直前に名前を見ているので短縮してよい。

節名は各章の実在する見出しから取る。`実装コード（現状）` のように末尾へ補足の括弧が付く節は、参照時には`現状コード`と短くしてよい。直後に節名と同じ語が続く場合は、重複するので足さない。

反映は `script/annotate_section_refs.py` で機械的に行う。見出し・表・引用・コードブロックは書き換えない。**本文の定型文を文字列一致で検査している箇所があるので、走らせた後は `validate_book.py` を必ず回す。**
