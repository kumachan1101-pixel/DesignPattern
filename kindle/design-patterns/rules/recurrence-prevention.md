# rules/recurrence-prevention.md
# 修正したら原因分析と再発防止を行う仕組み

章・ルール・スクリプトを修正したら、その場を直して終わりにしない。
同じ指摘が別の章や次回の作業で再発しないよう、原因を正本側へ反映する。

## 必ず回す4ステップ

1. **症状を1行で書く** — 何がどう間違っていたか（読者が困る形で）。
2. **原因を1行で書く** — なぜ起きたか。特に「正本（テンプレート／ルール／チェックリスト／Agent／検証スクリプト）の不足・矛盾・陳腐化」から来ていないかを必ず疑う。直した章だけが悪いのか、正本が再発を許しているのかを切り分ける。
3. **正本を直す** — 原因が正本側にあるなら、章だけでなく次を更新する。
   - 構造・見出し・項目の不足や矛盾 → `templates/chapter-template.md`、`output/chapter00_2.md`。
   - 書き方・粒度の基準不足 → `rules/writing-rules.md`、`rules/checklist.md`、`rules/phase-consistency-check.md`。
   - レビュー観点の漏れ → 該当 `agents/*.md`。
   - 機械的に判定できるもの → `script/validate_book.py`（必須見出し・禁止表現・構造）か `script/audit_book.py`（コンパイル・表幅・リスク表現）のチェックへ落とす。「人が毎回気をつける」で終わらせない。
4. **このファイルのログ表へ1行記録する** — 同じ原因が再発したとき、過去の対処を辿れるようにする。

## 正本どうしの結合（変えたら一緒に直す）

- テンプレートの標準見出し・フェーズ名・項目名を変えたら、`script/validate_book.py` の `REQUIRED_PHASES` / `REQUIRED_NUMBERED_SECTIONS` も同じ語へ更新する。片方だけ直すと全章が誤って失敗する。
- テンプレートに必須の図・表・節を足したら、`rules/checklist.md` に対応するチェック行を足す。
- 禁止表現を増やしたら、可能なら `script/validate_book.py` の `BANNED_PATTERNS` へ追加する。

## 2026-09-01 業務上の優先順がmainへ漏れ、否定形の進行説明が再発した

**事象**：第1章で、具体ルールの生成と優先順登録を`main()`へ並べていたため、`main()`が割引同士の競合方針を知っていた。また全章に「ここでは新しい役割を増やしません」「まだ形を決めていません」など、読者へ執筆手順を指示する文が繰り返されていた。

**原因**：Composition Rootの責任を「具体の生成・登録」と一括定義し、単純な配線と業務上の優先方針を区別していなかった。否定形の進行説明は、共通文を全章へ展開した一方、検査が古い数種類の言い回しだけを対象にしていた。

**対応**：第1章へ`DiscountRuleSet`を追加し、5つの具体ルールの所有と競合方針に沿った登録を移した。`main()`は完成したルール集合を生成し、同じSelectorを利用側へ注入するだけにした。全章の共通文は、現在確認する目的や次に追うコードを直接述べる文へ置き換えた。

**再発防止**：第0章、テンプレート、CLAUDE、チェックリスト、フェーズ整合規約、Agent、Skillへ「登録順・組み合わせが業務方針なら専用の組み立てクラスへ置く」を追加した。`check_chapter01_rule_lifecycle_terms`は`main()`内の具体ルール名・`add()`を検出し、`check_author_notes.py`は第0章を含む全章で否定形の進行管理を検出する。負のテストで両方を固定する。

## 2026-08-30 構想を分割して順番に決めようとし、全体の成立条件が見えなくなった

**事象**：フェーズ6を「契約と具体」「生成・所有・受け渡し」「公開入口からの実行」の順に分けたところ、ルールの選択方法や受け渡し方法が未確定のまま実行方法を説明する章になった。読者は離れた断片を自分で再結合しなければならず、完成クラス図もフェーズ6とフェーズ7で重複していた。

**原因**：相互依存する設計判断を、説明上の順番どおり独立に確定できると扱った。また「完成経路の先出しを避ける」という以前の対策を強く適用しすぎ、詳細へ入る前に必要な仮の全体像まで削除した。

**対応**：全12章を、(1)主要なクラス名と処理名を含む構想上の一本の経路、(2)契約・具体・生成・選択や登録・受け渡し・実行を連続して確認する要点コード、(3)課題と将来リスクに対する採用判断、の三部へ再構成した。フェーズ6から完成クラス図をすべて削除し、完成図・完成コード・実行結果はフェーズ7だけに置いた。長い `main()` と実行結果は処理単位で近接させる。

**再発防止**：第0章・テンプレート・執筆ルール・チェックリスト・Agent・validator・負のテストを同じ三部構成へ同期する。validatorはフェーズ6の三見出し、構想上の経路、要点コードの順、フェーズ6のクラス図ゼロ、フェーズ7の完成図を検査する。構想は確定済みコードの説明ではなく、後続コードで成立を検証し、必要なら戻って直す仮置きとして扱う。

## 機械チェックにできるか迷ったときの基準

- 見出し・語句・構造の有無 → 機械チェック向き（validate へ）。
- コンパイル可否・表の列数・特定表現の混入 → 機械チェック向き（audit / validate へ）。
- 「分かりやすいか」「粒度が仕様に紐づくか」「同じ対象を追えているか」 → 人の判断。checklist と phase-consistency-check の観点を使い、Agent レビューで担保する。

## Mermaid検証を動かす（mmdcが無い環境）

`check_mermaid.py` は `mmdc`（Mermaid CLI）をPATHから探す。入っていない環境では、
Playwright用のChromiumが `/opt/pw-browsers/chromium` にあればそれを流用できる。

```sh
# 1. Chromiumの再ダウンロードを避けて mermaid-cli を入れる
PUPPETEER_SKIP_DOWNLOAD=1 npm install @mermaid-js/mermaid-cli

# 2. puppeteer設定（rootで動かすので --no-sandbox が要る）
cat > pptr.json <<'JSON'
{ "executablePath": "/opt/pw-browsers/chromium",
  "args": ["--no-sandbox", "--disable-gpu", "--disable-dev-shm-usage"] }
JSON

# 3. check_mermaid.py は -p を渡さないので、ラッパーで注入する
mkdir -p bin && cat > bin/mmdc <<'SH'
#!/bin/sh
exec "$PWD/node_modules/.bin/mmdc" -p "$PWD/pptr.json" "$@"
SH
chmod +x bin/mmdc

# 4. 実行
PATH="$PWD/bin:$PATH" python3 script/check_mermaid.py
```

`PUPPETEER_EXECUTABLE_PATH` や `PUPPETEER_CONFIG_FILE` を環境変数で渡しても
mermaid-cli には効かない（`-p` でしか読まれない）。ラッパーを挟むのが確実。

## 検査が生きているかの確認

`validate_book.py` は「現在の本文が通るか」しか見ないため、チェックが空振りしていても気づけない。2026-08-12に追加した4系統には負のテストを用意した。

```
python3 script/test_recurrence_checks.py
```

わざと壊した本文を各チェックへ渡し、実際に検出できるかを確認する。チェックを直したら、このテストも一緒に通す。

## 再発防止ログ

> この表は、すべて修正済みの事象を記録する履歴です。最終列の「未」「一部」は、記録時点で機械判定へ落とし切れていなかったという意味であり、本文修正が未完了という意味ではありません。現在の残件は `review-tasks.md` 冒頭を正本とし、構造・実行結果・コンパイル・フェーズ継続は最新の検証スクリプト、意味上の妥当性は全章通読結果で再確認します。

| 日付 | 修正した症状 | 原因（なぜ起きたか） | 正本側の見直し | 記録時の機械チェック化（修正状態ではない） |
|---|---|---|---|---|
| 2026-08-30 | 全12章のフェーズ6で、詳細コードより前の「全体経路」表が`main()`や`Class::method()`を合計73行も断定し、その後の契約・具体・生成・受け渡し・実行で同じ内容を繰り返していた。読者には完成コードから逆算した結論が突然現れ、「どのコードから何を抽出したか」「生成した実体がどこへ渡るか」が見えなかった | 「最初に完成経路を見せると分かりやすい」という規約が、設計順と実行順を混同していた。validatorも序盤の正確な配置表を必須化し、C++ブロックだけを禁止して`main()`・メソッド名・実装文の先出しを許していた | 序盤の完成経路表を全章から削除し、フェーズ6を`契約と具体`→`生成・所有・受け渡し`→`公開入口から具体の実行`へ再構成。序盤は役割レベルの順序だけとし、クラス名・メソッド名・生成場所は根拠とコードを示す節で初めて確定する。変更前抜粋と新規コードのラベルを分け、6-2の重複コードを削除。第0章・テンプレート・規約・Agentを同期した | 済（`check_phase6_point_separation`を新見出し順、序盤のコード位置先出し禁止、コード読解案内、後段の正確なコード経路で更新。負のテストを含む72件がPASS） |
| 2026-08-29 | 全12章のフェーズ6に完成経路はあったが、基準クラス図の変更操作から、所有者・生成時点・選択時点をどう決めたかは「分離／組み立て」の汎用2項目だけで、完成経路が突然現れていた。全体経路表も詳細コード前なのに「実際のコード接続」として実装行を先出ししていた。また原則3の振り返りはコンポジションだけを挙げ、第3・8・9・11章で併用した実装継承の目的を回収していなかった | テンプレートが「確認観点（二行の予告）→全体経路→実際のコード接続」を正本にしており、前工程の事実を設計判断へ変換する成果物がなかった。原則3も「現れ」を書く定型になり、契約実装・実装継承・コンポジションの使い分けを章末で検算する基準がなかった | 全12章へ`前工程で確定した事実／ここで決めること／判断／全体経路への反映`の章固有表を追加し、原因ID・課題ID・守る責任から生成時点・所有者・生存期間・選択条件・境界を導いてから完成経路へ進む形に変更。全体経路表を設計条件として「担う場所／経路で受け渡すもの・起きること」へ変更し、実装行は後続節へ移した。原則3を「判断」とし、全章で契約実装・実装継承・コンポジションを区別。第3・8・9・11章は実装継承を使う骨格と理由も明記した | 済（`check_phase6_point_separation`で判断見出し・4列表・3判断以上・順序・C++先出し禁止・全体経路の列名を検査。負のテスト`DOC-008`を追加。原則3の妥当性はコード関係と理由を要するため、テンプレート・チェックリスト・logic/consistency Agentで人が確認する） |
| 2026-08-29 | 第0章に継承・契約・コンポジションの違いを問う`★`コメントが残り、第3章の`IReservationState`と第9章の`ITicketPhase`は既定実装を持つ通常基底なのに、クラス図で`<<interface>>`・点線白三角としていた。第11章には点線白三角の基底へ`<<interface>>`を書かない完成図もあった | C++では契約実装と実装継承の派生側がどちらも`: public 基底型`になる点を説明せず、型名の`I`からステレオタイプを決めていた。著者メモ検査もフェーズ見出しを持つ方法論12章だけを対象にし、第0章を検査していなかった | 第0章で、C++上はインターフェースも抽象クラスの一種であること、契約だけ／共通実装を持つ抽象基底／通常基底の3区分、具体側の契約実装と利用側のコンポジションは併用できることを基底側コードで説明。第3・9・11章の図を実コードへ合わせ、テンプレート・執筆ルール・チェックリスト・Agentを同期した | 済（`check_class_diagram_type_semantics`でステレオタイプ・白三角・C++基底定義を照合し、`check_author_notes.py`の`★`検査を第0章を含む全原稿へ拡張。負のテスト`DIAGRAM-004`・`DIAGRAM-005`・`EDIT-003`を追加） |
| 2026-08-29 | コード内で判定・実行・保存・返却が空行なしに連続し、処理段階を目で追いにくかった。また「1定義1ブロック」と書きながら複数型を許す検証が残り、短い兄弟クラスを同居させる基準も混在していた。掲載コードを1つの`.cpp`へ連結する説明はあったが、実務で`.h`／`.cpp`／`main.cpp`へどう分けるかが無く、掲載ブロックとファイル単位を混同させた | 空行、読解単位、実ファイル配置という別の三判断を「読みやすさ」の一語で扱い、正本とvalidatorの上限も同期していなかった。動作確認用の1ファイル構成を説明したことで、実務配置まで説明済みと誤認した | 全章のC++を、入力・取得→判定→選択・計算→状態変更・保存→通知・返却の意味境界で整形し、トップレベルの型を1ブロック1つへ統一。第0章へ掲載ブロックと実ファイルの違い、`.h`／`.cpp`／`main.cpp`の役割、第1章を使った配置例を追加。各章の「手元で動かすには」へ1ファイルは動作確認用だと共通注記を追加し、テンプレート・チェックリスト・Agentを同期した | 済（`check_one_top_level_type_per_block`、`check_cpp_semantic_spacing`、`check_run_locally_section`、`check_chapter0_file_layout_guidance`で検出。負のテスト`CODE-PRESENT-001`〜`004`で検査が生きていることを確認） |
| 2026-08-14 | 読者向け本文へ著者向けメモと先取りが111件混ざっていた。原稿内の相互参照ラベル（`実行対象コード：`／`対応する動作例：`／`確認したいこと：`）54件、編集側の言い方（`> **抜粋の前提（周辺は現状のまま）：**`）12件、執筆の段取りを読者へ言う文（「この段階では、解決クラス名・契約名・パターン名・生成場所を決めません」ほか）25件、ID体系の配線説明11件、掲載の都合3件、そしてフェーズ1〜3がフェーズ7でしか出ない型名を名指しする先取り6件。著者評「ネタバレや著者向けメモが多数散見されます。フェーズ前半に多いです」 | `CLAUDE.md` に「執筆者向けの進行管理を読者へ言わない」と書いてあったが、機械で見ていなかった。さらに `validate_book.py` が **`抜粋の前提（周辺は現状のまま）` と `実行対象コード` というラベルの存在そのものを必須化**しており、編集ラベルをルールとして固定していた。検査が著者向けメモを強制していたのが真因 | 全111件を読者向けの文へ直した。相互参照ラベルは「〜を、上のコードで通します。見るのは〜です。」という散文へ、「抜粋の前提」は「> **この抜粋の外は、現状のままです。**」へ、「この段階では〜決めません」は「ここで挙げるのは〜まで。それをどう置くかはフェーズ6で決めます」へ。先取りは型名を役割の言葉へ置き換えた（`ReservationExpiryScheduler` → 実行ハーネス、`NotificationResult` → 通知の受付成否を返す内部契約）。あわせて `validate_book.py` の2つのチェックを、ラベルの存在ではなく**実質**（抜粋の範囲を伝えているか／何を見るかを書いたか）を見る形へ直した | 済（`script/check_author_notes.py` を新設し、相互参照ラベル・抜粋の前提・執筆の段取り・掲載の都合・ID配線説明と、フェーズ1〜3での解決構造型名の先出しを検出する。先取りは「7-1にあり、1-4にも3-1にも無い型」を機械で算出するので例外リストが要らない。`run_completion_gate.py` の Author notes and spoilers 段として固定し、負のテストは EDIT-002 の2件。「その文が読者の役に立つか」は機械では見られないため、`rules/checklist.md` の観点で人が確認する） |
| 2026-08-14 | 全12章の1-1「代表入力」が、ほぼ1行の呼び出しだけだった。しかも呼ぶ相手（`processor`・`db`・`manager` など）をその抜粋の中で作っていないため、**そのままでは1行もコンパイルできない**。読者が最初に見る実行なのに、入力の準備も、続けて呼んだときに状態がどう動くかも見えなかった。著者指摘「1行実行が多いが、何回か実行してステータス変化を見る場合は、そのようなところまで見せて欲しい。あとは、入力の準備とかも」 | 1-1を「1つの入力と1つの出力の対応を見せる場所」と狭く捉えていた。実際には1-1は章の入口で、ここで動きが見えないと以降が読めない。検査も `check_representative_input_preparation` が「代入した変数を宣言しているか」しか見ておらず、**抜粋が単体で動くか**は誰も確かめていなかった | 全12章の代表入力を「準備 → 複数回の呼び出し → 状態の移り変わり」まで広げた。第9章は1件のチケットが `Open → InProgress → Escalated → Resolved → Open` と巡る5行、第2章は2回目の残高確認が145,000円から始まること、第5章は残高が積み上がること、第11章は診断ログが `0->1`・`1->2` と増えることを、それぞれ出力で見せる。第11章は1-4の `main()` 自体が1回きりだったので、週次Excelと未登録テンプレートの2件を足した。各章に「2回並べると何が見えるか」の1段落を添えた | 済（`script/check_representative_run.py` を新設し、1-1の代表入力を1-4のクラス定義へ結合して**実際にコンパイル・実行し**、掲載している実行結果と一致するかを確認する。呼び出し回数の下限も見る。`run_completion_gate.py` の Representative run in 1-1 段として固定し、負のテストは `test_recurrence_checks.py` のRUN-002。「その状態変化が読者にとって意味のある変化か」は機械では見られないため、`rules/checklist.md` の観点で人が確認する） |
| 2026-09-01 | 第4章1-1で2回分の呼び出しを先にまとめ、その後へ2回分の結果を一括掲載していたため、読者が対応を組み直す必要があった。さらに結果説明は、出力にない「検証」を直接観察できるように書き、入力件数を差し替える処理差分として扱っていた。全章監査では第1章の会員名、第8章の金額の入力元、第9章の操作回数にも食い違いがあった | 掲載出力と実コードの一致だけを検査し、散文が出力から読み取れる範囲に収まるか、複数呼び出しと結果が隣接しているかを見ていなかった。1-1の正本も「出力先行」と「入力先行」が競合していた | 全12章の1-1を呼び出し→その結果のペア型へ統一し、観察事実とコード・状態表から分かる内部事実を書き分けた。第4章は区切り差を実際の出力行で示し、件数を入力データ量と明記し、保存語彙を共通化した | 済（`check_phase1_system_overview`で`// N回目`と`N回目の実行結果`の集合・隣接順を検査し、負のテスト`RESULT-PAIR-001`で結果ラベル欠落を検出。散文の意味範囲はチェックリストで人が確認） |
| 2026-08-14 | 第7章の現状コード（1-4）で、3つの通知先がそろって `CurrentDeliveryReceipt` を返しており、共通化の余地が最初から用意されていた。関数名だけが `send` / `update` / `send` と揺れていた。一方フェーズ3の試行コードは `bool` / `int` / `string` というバラバラな戻り値を前提にしており、1-4と食い違っていた。著者指摘「引数と戻り値と関数名を変える。その複雑度で共通化してく過程が見たい」 | 現状コードを書くとき、後で共通契約へ寄せることを見越して**あらかじめ形をそろえて**しまった。そろっているものをそろえ直しても、読者には共通化の仕事が見えない。さらに1-4とフェーズ3で別々に書いたため、両者の想定シグネチャがずれても誰も気づかなかった | 1-4の3クラスを、別々に導入した外部基盤らしい形へ直した。`sendMail(subject, body)->bool` / `refreshStockWidget(productCode, stock)->void` / `postMessage(channel, text)->string`。とくにダッシュボードは**失敗を表せない**ため、「呼べたことをもって成功とみなす」という割り切りが必要になる。7-1では各通知先が自分の基盤の呼び方を private に持ち、`INotification::send()` の中で契約から変換する形にした。**基盤の呼び方は1つも変えず、翻訳する場所だけを移した**ことを対比表で示した。1-3の型表・クラス図・メンバー表、フェーズ3の試行コード、フェーズ4の原因、7-1の実行結果を同期した | 一部（1-4と7-1の掲載コードは `check_execution_output.py` が実際にコンパイル・実行して照合するため、シグネチャの食い違いはここで止まる。フェーズ3の試行コードは対象外なので、今回は手元でコンパイルし、掲載ブロックの連結が実行コードとバイト一致することを機械照合した。「現状コードが共通化の余地を先に用意していないか」は機械では見られないため、`rules/checklist.md` の観点で人が確認する） |
| 2026-08-14 | 第6章フェーズ3の痛みコードが、トッピングごとに `int milkSeq` などの選択順フィールドを並べ、`getDescription()` が1番から5番まで走査して該当種類を探す形だった。コンストラクタ引数は12個。著者評「やろうとも思わないし複雑過ぎる」 | フェーズ3で「対策を先取りしない」というルールを、「素直な実装も採ってはいけない」と読み違えた。`vector` へ積む形はフェーズ6の採用構造と方向が同じなので避けた結果、**誰も書かないコードで痛みを演出**していた。痛みが作り物だと読者は「自分ならこう書かない」で終わり、構造の問題が伝わらない | 選択順のトッピング名を `vector` へ積み、取り出すときも積んだ順に読む素直な形へ書き直した。その結果、変更ID2（入力順）は**素直な変更で解けた**ことを本文で明示し、残った痛み（価格の分岐が `CustomDrink` に集中する／種類名が `string` なのでつづり違いと分岐の足し忘れが実行時も止まらず0円で通る／販売可否が呼び出し側へ出る）だけを問題IDにした。3-2の影響グラフ、3-3、問題ID表、4-1の症状表、4-3、5-1、課題ID1の記述を同期した | 一部（掲載コードと実行結果の一致は `check_execution_output.py` が1-4と7-1で見るが、フェーズ3の試行コードは対象外。今回は手元でコンパイル・実行し、掲載ブロックの連結と実行コードがバイト一致することを機械照合した。「読者が書きそうな実装か」は機械では見られないため、`rules/checklist.md` の観点で人が確認する） |
| 2026-08-14 | 掲載コードの断片が、どのクラスのどの関数の一部か分からないまま並んでいた（115ブロック）。あわせて7-1の完成コードに80行超のブロックが15件、4型以上を1ブロックへ詰めたものが24件あり、最大は第9章の131行 `TicketService`。読者が「今どこを読んでいるか」を追えず、責任の切れ目も見えなかった | 著者指摘は「せめてどのクラスのどの関数かはわかる形にしてほしい」「1クラスが大きいならその時点で分割して」。原因は、コードの掲載単位を決める規約が正本になく、章ごとに「動くひとまとまり」を優先して貼っていたこと。検査も、7-1に限って120行かつ2型以上という緩い条件（`check_long_final_cpp_blocks`）しか持たず、フェーズ3・4・6の断片は対象外だった | 全12章の断片へ所属を明記した（`Class::method()` または `main()`。直前の説明文へ織り込み、新しいラベル行を増やさない）。80行超・4型以上の35ブロックを、値・列挙／境界クラス／契約／具体クラス／組み立ての切れ目で分割した（Markdownだけを挟み、コード行は1文字も変えていないため連結すれば同じ1本のまま）。第0章へ「掲載コードの読み方 ―― 断片・分割・全貌表」を新設し、断片の所属表記・分割ブロックの連結規約・フェーズ6の2表の読み分けを説明した。checklistへ3項目追加 | 済（`validate_book.py` の `check_code_block_attribution` が、所属不明・80行超・4型以上・省略記号をフェーズ3/4/6/7の全C++ブロックで検出。`test_recurrence_checks.py` のDOC-001 4件が負のテストで担保。`script/audit_code_blocks.py` は章別・種別の残件一覧ツールとして残す。所属の「書いてあるか」は機械で見られるが「正しいか」は見られないため、クラス名・関数名が実在するかは人が確認する） |
| 2026-08-14 | フェーズ6の①〜⑥のうち、②安定骨格と⑥利用開始を一つの見出しへまとめていた章が12中8。`②⑥`・`②⑤⑥`・`⑤⑥` のほか、`①②`（第4章）・`②③`（第2章・第6章）・`④⑤`（多数）も統合されていた。第4章は①と②、第6章と第11章は②、第8章は④が見出しとして存在しなかった。第7章は⑥の見出しに②の内容（`InventoryManager::reduceStock()` の定義）が入っていた | 第0章が⑥を「実行」とだけ定義しており、「利用者が公開入口を呼ぶこと」「骨格が契約を呼ぶこと」「契約の向こうで具体が動くこと」の3つへ意味が分岐した。その結果②へ吸収され、独立した利用開始コードが消えた。また①→⑥を「説明順」と「実行時の呼出順」の両方に使っていたため、④→⑤→⑥→②→①→③という実際の呼出順が本文から追えなかった | 全12章で①〜⑥を別々の見出しへ分け、各番号へ所属の分かる実コードを付けた。6-2への参照だけで済ませていた②の骨格コードを各課題の位置へ掲載した。各章の代表課題へ実行接続表（実行順・ポイント／掲載箇所／実際のコード接続／次の呼出先）を置いた。第0章へ「②は呼ばれる側、⑥は呼ぶ側」「説明順と実行順は別」を明記し、テンプレートとchecklistへ反映した | 済（`validate_book.py` の `check_phase6_point_separation` が、②と⑥の統合見出し・①〜⑥の欠番・実行接続表の欠落を検出。`test_recurrence_checks.py` のDOC-002が負のテストで担保。ただし呼出グラフそのものは静的検査で保証できないため、表の各行が実コードを指すかは人が確認する） |
| 2026-08-14 | 第9章の優先度が登録時に決まり、チケットの生涯を通じて一度も変わらなかった。`PriorityCalculator::calculate(UserType)` の入力がユーザー種別だけで、`UserDatabase` は起動時に固定されるため、`t.priority = calc.calculate(...)` は値が絶対に変わらない代入だった。それでも本文は1-1・仕様フロー図のひし形・動作例テーブルの優先度列で「再計算」と「維持」を区別していた | 分岐した先で結果が変わらないのに、区別があるかのように書いていた。動作例テーブルの8行は状態遷移では意味を持つが、優先度列は1〜2行目で完全に決まっており、行を増やしても新しい情報が出なかった。さらに問題ID1「状態遷移と優先度計算が密に絡み、片方をいじると他方を壊す」が、再計算が値を変えない以上は壊れようがなく、章の痛みが飾りになっていた | エスカレーションで契約区分によらずHighへ引き上げ、再受付でユーザー種別から計算し直して初期値へ戻す規則へ変更。「引き上げ」「計算し直し」「維持」の3つが観測可能になった。動作例テーブルと1-4・7-1のmainを、一般ユーザーが Normal→High→Normal と動く構成へ組み直した。変更ID1は「エスカレーション時にHigh」が常に真になるため「登録時と再受付時にHigh」へ書き換え、同期先6か所（変更ID一覧2か所・2-2・受入条件・変更後要求ID1・7-4・7-1受入エビデンス）をそろえた | 未（「分岐した先で結果が変わらない」ことの機械検出は、実行結果の差分比較が要るため未着手。`rules/checklist.md` へ観点を追加して人が確認する） |
| 2026-08-14 | 第8章の`PaymentRequest`が、注文台帳から引ける`amount`・`customerId`を利用側に渡させ、食い違えば`ORDER_MISMATCH`で弾いていた。`PayPayInput`の`merchantId`は加盟店＝システムの設定値なのに毎回渡させていた。台帳照合の4分岐は実行結果に一度も現れず、コード内にしか存在しなかった | 台帳を持っているのに正本にしていなかった。検証を通った後も外部APIへ渡すのは`req.amount`（利用側の値）で、照合を1行外せば利用側の値がそのまま流れる状態だった。`amount < 1`の検査も、台帳の登録額が全て1円以上なので独立した意味を持っていなかった。さらに顧客氏名を空チェックした後どこにも使わず、振込名義は別に受け取っていた | `PaymentRequest`を決済方法・注文ID・手段固有入力だけへ縮小。`pay()`の契約を`pay(const PaymentRequest&, int amount)`へ変え、台帳から引いた金額を渡す形にした。`ORDER_MISMATCH`と`INVALID_AMOUNT`を削除し、記録用に`chargedAmount()`を追加。未登録注文・未登録顧客の実行ケースを1-4と7-1へ追加し、要求ID5を台帳拒否まで含む文面へ拡張。顧客氏名チェックが注文の持ち主を確かめる検査であり振込名義とは別物であることを1-1へ明記した | 一部（`check_evidence_scenario_reference` が、実行していないケースを受入エビデンスへ書くことは検出する。実際この作業中に、ケースを追加する前にエビデンス行を書いて捕まった。「台帳から引ける値を利用側に渡させている」こと自体は機械で検出できないため、`rules/checklist.md` のR3観点で人が確認する） |
| 2026-08-14 | 「手元で動かすには」が勧める操作を実際にやると本文の約束と違った。第10章は「`PartnerDatabase`へ足せばその連携先でもバッチを実行できる」と書いてあるが`未対応の連携先`で失敗する。第4章は「列不足の行を足せば保存件数が1件減る」と書いてあるが減らない。第8章・第11章には節そのものが無かった | 書いたが試していない。特に第10章は、章が3-1で痛みとして示している当のことを1-4で「できます」と書いており、読者が手元で試すと本文の主張と食い違う結果に出会う。さらに第4章は`StoreDataImporter`が不正行を数えず`skipped`へ0を固定で返しており、7-1は数えているのに1-4だけ数えないという変更IDの裏付けが無い差分になっていた | 第10章は実際に試せる例（同期対象の切替、有効フラグをfalseにしてスキップ）へ差し替え、未知の連携先はDB登録だけでは送信できないと明記。第4章は`StoreDataImporter`へスキップ集計を入れて1-4と7-1をそろえ、掲載実行結果を実出力へ差し替え、文面を「保存件数は増えず、スキップ件数が増える」へ訂正。第8章・第11章へ節を追加し、第11章は実際にファイルが作られること・中身はデモ用テキストであることを明記 | 一部（`validate_book.py` の `check_run_locally_section` が節の有無を検出。`test_recurrence_checks.py` のRUN-001が負のテストで担保。中身が実挙動と合っているかは機械で見られないため、`rules/checklist.md` の観点で人が確認する） |
| 2026-08-14 | 全章の2-2が、箇条書き・分類表・絵文字・実装予測へ分岐し、第1章の変更ID2と第2章の変更ID3が落ちた | 2-2を1-5の確定変更ID一覧を受け取る工程としてテンプレート・validatorへ固定せず、章ごとに未来予測を書き直していた | テンプレートと全12章を「1-5と同じ変更ID・同じ要点」の箇条書きへ統一 | 済（`check_phase22_change_list`がID集合・要点・共通導入・表/色の混入を検査。負テストPHASE22-001） |
| 2026-08-14 | 代表入力で第8章の`r1`と第11章の`request`をコメントだけで準備し、第1章の`context`も未宣言だった | 「入力→結果」の順だけを検査し、入力値を実コードで生成したかを検査していなかった | テンプレートへ入力生成→設定→公開操作の規約を追加し、該当3章を修正 | 済（`check_representative_input_preparation`と負テストREPRESENTATIVE-INPUT-001） |
| 2026-08-14 | フェーズ別の番号置換で、9章29箇所のC++三項演算子`?`が`?6?`へ破損した | 記号置換を本文全体へ機械適用し、フェーズ境界とコード記号を区別せず、残留検査もなかった | 第0章・テンプレートで番号用途を分離し、全章を復元。置換後は全掲載C++を再コンパイルする | 済（`check_number_namespace`が丸数字のフェーズ外利用と`?6?`を検出。負テストNUMBER-001/REWRITE-001） |
| 2026-08-14 | 第7章の非同期SMSが受付結果だけで止まり、最終配信結果・UI変更見込み・失敗隔離の根拠が要求と完成コードで切れていた | 「SMS追加」から外部API制約と運用判断を確認せず、受付と完了を同じ同期経路のように扱っていた | 変更ID確定前に受付ID・コールバック・失敗時の確定範囲・UI要否をヒアリングし、状態ログとコールバック境界へ実装 | 済（要求ID/変更IDの根拠検査、全章C++実行照合、将来リスク評価で担保） |
| 2026-08-14 | 第8章の簡略化表に`TIMEOUT`がなく、`ERROR`を再試行可能と書く一方、コードと実行結果は`ERROR`を再試行不可、`TIMEOUT`初回を再試行可能としていた | 外部境界の契約を簡略化表・コード・実行結果へ重複記述したが、主要分岐の集合と再試行可否を同期する検査がなかった | 第8章の表を初回失敗・再試行後成功まで含む契約へ修正し、チェックリストへ外部境界分岐の照合観点を追加 | 済（`check_payment_timeout_contract`と負テストTIMEOUT-001） |
| 2026-08-14 | 入力追跡表が第7章だけコード後、第1〜6・8〜10章はコード前、第11・12章は欠落し、同じ目的の項目が横並びになっていなかった | 見出し名だけをそろえて配置・件数を検査せず、テンプレートも現状コードの掲載枠より先に追跡表を置いていた | 全12章を1-4のコード・実行結果後へ統一し、第11・12章へ不足表を追加。テンプレートもコード掲載枠→追跡表の順へ修正 | 済（`check_phase14_input_trace_position`と負テストINPUT-TRACE-001） |
| 2026-08-13 | フェーズ5-1の表頭を`原因として確定した事実`から`原因ID・確定した事実`へ変えたとき、全12章と第0章は直したが`templates/chapter-template.md`と`script/validate_book.py`が旧語のままだった。全12章が同じ検査で落ち、CIが赤くなった | このファイルは「テンプレートの項目名を変えたら validator も同じ語へ更新する」と定めていたが、破ったことを検出する手段が無く、人が気づくまで気づけなかった。本文だけを直しても本文検査は通るため、正本側の取り残しが表面化しにくい | テンプレートとvalidatorを新語へ同期。validator側は表頭を`REQUIRED_TABLE_HEADERS`定数へ切り出し、定義を1か所にした | 済（`validate_book.py`の`check_validator_template_sync`が、validatorの要求する表頭がテンプレートに実在するか照合。`test_recurrence_checks.py`のSYNC-001が負のテストで担保） |
| 2026-08-13 | 機能ID廃止（2026-08-02）のとき、各章の機能ID表を消した跡へ「機能IDは設けません」という断り書きを本文へ残した。読者向けの本文14か所に、第0章の番号表で定義していない語が否定形だけで並んでいた | 廃止作業の申し送り（後続AIが表を復活させないための注意）を、本文へ書いて済ませた。読者は機能IDを知らないので、存在しない番号の不在を繰り返し告げられることになる。禁止指示の置き場所は本文ではなく、テンプレート・ルール・agents である | 第0章「本書の番号の読み方」の表直後へ一度だけ、他書の機能IDにあたる番号を本書では設けない理由を説明。各章11か所の繰り返しを削除。禁止指示は`templates/chapter-template.md`・`rules/`・`agents/` 側に残す | 未（本文へ廃止済み方針の断り書きを書かない、という基準の機械化は難しい。レビュー観点として `rules/checklist.md` へ置く） |
| 2026-08-13 | 第0章の番号表で原因ID行が`| 原因ID1：痛みを生む構造上の原因 | フェーズ4 | … |`の3列になっており、表頭4列に対し「表記例」と「意味」が1セルへ潰れていた。Kindleでは列がずれて表示される | `check_kindle.py` は「4列を超えないか」だけを見るため、列が足りない行は素通りした。`check_standard_id_glossary` も要求ID・変更ID・リスクID・課題IDの4行しか照合しておらず、問題ID・原因ID行は検査対象外だった | 原因ID行を4列へ修復。`check_standard_id_glossary` の照合対象へ問題ID・原因ID行を追加 | 済（`validate_book.py` の `check_table_column_consistency` が全表で表頭とデータ行のセル数一致を照合。`test_recurrence_checks.py` のTABLE-001が負のテストで担保） |
| 2026-08-13 | 第6章1-4の表示ヘルパーが `void showOrder(MenuDatabase& db, int row, ...)` として動作例テーブルの行番号を引数で受け、`cout << "--- 行" << row << " ---"` と組み立てていた。呼び出し側は「モバイルアプリを想定」と書かれているのに、何行目かを渡さないと呼べなかった | 行番号はどの要求IDにも無い執筆上の目印である。掲載コードを動作例と1件ずつ並べる形へ直したとき、ラベル出力を業務コード側へ入れてしまい、要求に無い値が入力仕様へ混ざった。`check_unused_cpp_inputs.py` は「使われない引数」を見るが、この row は表示に使われていたため素通りした | `showOrder` の引数を注文データ（メニューID・トッピング選択）だけへ戻し、行ラベルは `main()` のリテラルで出す形へ変更。他の8章は元からこの形だった。あわせて本文の「`showOrder` ラムダ」という誤記を「関数」へ直した（掲載コードでlambdaは使わない） | 済（`validate_book.py` の `check_scenario_label_literal` が、行ラベルを変数から組み立てる書き方を検出。`test_recurrence_checks.py` のINPUT-001が負のテストで担保） |
| 2026-08-13 | 第6章のフェーズ3が変更ID1（Matcha・Choco追加）しか当てておらず、変更ID2（入力順）と変更ID3（販売可否）は試さないまま、問題ID3として「（変更ID1の試行から見込まれる痛み）」と表へ載せていた。3-3の締めは「変更ID1〜変更ID3を適用すると…確認できました」と実測のように書いていた | 問題IDの列見出しは「観測した痛み」なのに、見込みを観測として記録した。その上に原因ID・課題IDが積まれるため、実測でない前提から設計判断が導かれる。3-1のコードは 1-4 / 7-1 と違って audit_book.py の実行検査対象外なので、「コードが無いのに痛みだけ書いてある」状態を機械で検出できていなかった | 変更ID2・変更ID3を実際に現状構造へ当てはめ、コンパイル・実行して痛みを観測した。問題ID3を実測内容へ書き換え、問題ID4（販売可否がクラス外へ出た）を追加。あわせて5-1が参照していながらフェーズ4に定義が無かった原因ID2を4-4へ定義し、問題ID4→原因ID2→課題ID1の追跡をつないだ | 済（`validate_book.py` の `check_observed_problem_only` が、問題IDの行にある推量の語（見込ま・想定され・予想され等）を検出。`test_recurrence_checks.py` のTRIAL-001が負のテストで担保） |
| 2026-08-13 | 第6章と同じ「未試行の変更を痛みとして記録する」欠陥が、第2章（変更ID3）・第4章（変更ID2）・第5章（変更ID2・変更ID3）・第10章（変更ID3・変更ID4）にもあった。第9_2章は変更ID2が部分適用で、「Open/InProgressからの保留」のうちInProgress側がコードに無かった | 3-1のコードは実行検査対象外で、変更ID一覧と3-1コードの突き合わせを誰も機械で見ていない。散文で「〜を書き足すと、この肥大化はさらに進みます」と書けば、コードが無くても痛みを述べられてしまう。追加した `check_observed_problem_only` の初版は「予見され」を語彙に含めておらず、第2章・第10章の『（変更ID1・ID2の試行から予見される痛み）』を素通りさせた | 5章ぶんの未試行変更を実際に当て、コンパイル・実行して痛みを観測した。各章の問題ID表・痛みの散文・変更影響グラフを実測へ更新した。検出語へ「予見され」「見込み」を追加した | 一部（推量の語は検出できるが、「散文だけで痛みを述べ、問題ID表には推量の語を書かない」形は検出できない。3-1を実行検査対象へ入れるには全章の抜粋コードを結合可能な形へそろえる必要があり、未着手。読んで確かめる観点として `rules/checklist.md` へ置く） |
| 2026-08-12 | 7-1の受入エビデンス表が要求IDを「合格」と判定しているのに、その受入条件を確認する実行シナリオが実行結果に無い。9章で発生し、第4章のバージョン確認・第7章のログ項目・第12章の役職照合は実装そのものが存在しなかった | エビデンス表を先に書き、`main()`を後から縮めても検出できなかった。`phase-consistency-check.md`は「全有効要求IDに実行シナリオ・観測結果・判定があるか」を求めていたが、表の記述が実行結果の範囲内に収まっているかを機械検査していなかった | 全9章で`main()`へシナリオを追加し、実装が受入条件を満たしていない4件はコードを実装。第5章は`check_execution_output.py`のEXEMPTに登録され機械検査をすり抜けていたため、実出力へそろえてEXEMPTを空にした | 済（`validate_book.py`の`check_evidence_scenario_reference`が、観測結果欄の参照するシナリオ名が実行結果に実在するか照合） |
| 2026-08-12 | フェーズ3で試していない変更IDを「追加しました」と完了形で書き、その痛みを問題IDの根拠にしていた。8章で発生。第12章は「二重送信バグ」がコードにも実行結果にも存在しないまま問題確定・整理まで引き継がれていた | `logic-check-agent.md`に「フェーズ3で変更を試す前に痛みを断定している」という観点はあったが、変更ID集合と3-1掲載コードの対応を照合する手順がなかった | 第12章は主張の中心を試行コードへ実装して観測可能にし、残る7章は「観測済みの痛み」と「そこから予見される痛み」を書き分けた | 未（3-3の完了形表現と3-1のコードブロックの対応は意味判断が要り、Agentレビューで担保する） |
| 2026-08-12 | フェーズ6で「確定します」「採用後コード」と示した契約が、7-1完成コードと別シグネチャ・別メソッド名だった。9章で発生し、第8章はフィールド順不一致でコンパイルできない断片だった | フェーズ6のコードを7-1から逆生成せず手書きしていた。`checklist.md`に「フェーズ6の契約と7-1の契約が同一か」の照合行がなかった | 全9章でフェーズ6の契約を7-1からの抜粋へ揃え、差分がある場合は理由を本文へ書いた | 済（`validate_book.py`の`check_phase6_phase7_contract_match`が、純粋仮想契約のメソッド集合をフェーズ6と7-1で比較） |
| 2026-08-12 | 「フェーズ1のまとめ：変更ID一覧」の列名が「対象の**現行**要求ID」なのに、現行ベースラインに無い追加要求IDが並んでいた。列名自体は全12章にあった | テンプレート由来の固定列名で、追加要求を持つ章では必ず矛盾した | 全12章の列名を「関係する要求ID（追加は変更後ID）」へ統一 | 済（`validate_book.py`の`check_change_id_requirement_scope`が、列名が「現行」を名乗る場合に要求IDの所属を検査） |
| 2026-08-12 | 本文が「フェーズ6のステップ1〜3」を参照するが、実章のフェーズ6は課題ID別①〜⑥構成で該当する見出しが存在しなかった。9章で発生 | `output/chapter00_2.md`が旧ステップ方式の規定を保持したままだった。テンプレート・`checklist.md`・`agents/*.md`は既に①〜⑥方式で、第0章だけが取り残されていた | 第0章を課題ID別①〜⑥方式へ更新し、各章の宙に浮いた参照を張り替えた | 済（`validate_book.py`の`check_step_reference_target`が、ステップ参照に対応する見出しの存在を検査） |
| 2026-08-02 | 英字の要求・変更・リスク・課題IDが各表に繰り返し現れ、読者が略語の意味を引き直す必要があった | 追跡しやすさを優先して内部管理用の英字略称を本文へ露出し、日本語で役割を直接読めるかを基準にしていなかった | 全12章・第0章・テンプレート・Agent・Skill・ルールを`要求ID`・`変更ID`・`リスクID`・`課題ID`へ統一し、第0章の用語表とvalidatorを日本語表記へ更新 | 済（英字表記の追跡行をvalidatorが検出） |
| 2026-08-02 | 第12章の1-1を読み始めても、要求表や詳細仕様を読み進めるまで、入力から出力までの大筋とスタブの位置が分からなかった | 「詳細仕様をコードより先に説明する」基準はあったが、詳細へ入る前にシステム全体を短く俯瞰する共通の導入成果物がなかった | 全12章の1-1冒頭に入力・主要処理・出力・掲載コードでの代替を示す共通節を追加し、第0章・テンプレート・全Agent・Skill・ルール・validatorへ同じ順序を反映 | 済（見出しの位置と4項目をvalidatorが全12章で検査） |
| 2026-08-02 | 第11章で要求とコードのデグレを修正しても、他章には原因から課題へ直接飛ぶ流れ、変更依頼と要求のID混同、突然の完成クラス図、要求と課題を一表に強制する追跡が残っていた。Mermaidの文字列`\\n`と構文エラーも、テキスト検査だけでは漏れた | 第11章の局所修正を正本と全章へ同時に反映する契約がなく、テンプレート・Agent・validator・完了台帳がそれぞれ過去の流れを保持していた。検査も見出し・IDの存在中心で、要求集合の保存、候補評価、部分図の順序と全体図との一致、図の実レンダリンを検査していなかった | 全12章を`REQ-ID（現行／変更後要求）`、`CR-ID（変更依頼）`、`P-ID（設計課題）`へ分離。フェーズ5を原因→候補→システム全体評価→P-ID確定、フェーズ6を設計理由→着色部分図→最終構造→採用全体図→関連コードに統一。第0章、テンプレート、CLAUDE/AI引継、全Agent、Skill、ルール、チェックリスト、完了台帳を同時更新し、重複追跡表を削除 | 済（`validate_book.py`でREQ集合・文意・CR根拠・P連番・フェーズ5順序・部分図の順序/着色/型部分集合・採用図=完成図を検査。`check_mermaid.py`で全図をMermaid CLIへ渡しSVG生成数まで照合。releaseゲートで必須化） |
| 2026-08-02 | 第11章でREQ、CR、F、Pに加えて標準外のH-IDが説明なしに現れ、読者が略語の意味と管理対象を推測しなければならなかった | IDの工程上の役割は説明していたが、英語由来を一覧化せず、章固有の仮説番号を正本にないまま追加していた | 第0章へREQ（Requirement）、CR（Change Request）、F（Future Risk）、P（Problem）の英語・日本語・管理対象を4列表で追加。第11章のH-IDは本文・装飾・履歴という内容名へ置換し、チェックリストとvalidatorへ標準用語表を固定 | 済（全パターン章のH-IDと編集指示0件。`validate_book.py`が第0章の用語表4行と標準外H-IDを検査） |
| 2026-08-02 | 第11章の2-1で変化の見当を出した後、何を誰へ確認するかを決めずに2-3のヒアリングへ進み、会話が仮説から導かれたか分からなかった。同じ接続不足は他章にもあった | 正本は「仮説をヒアリングで確認する」とだけ定め、見当を具体的な質問と確認先へ変換する中間成果物を持っていなかった | 全12章の2-1末尾へ`見当 / 現時点の仮説 / 確認する質問 / 確認先`表を追加し、2-3をその回答として構成。第0章、テンプレート、CLAUDE、AI引継、全Agent、Skill、チェックリストへ同じ順序を反映 | 済（`validate_book.py`が全12章の質問表、配置順、具体行、2-3の質問数を検査） |
| 2026-08-02 | 第11章だけに`unique_ptr`と`make_unique`が突然現れ、第0章とチェックリストの「掲載コードではスマートポインタを使わない」規約に反した | コード実行は通っても、掲載記法の統一をvalidatorが検査せず、全面改修時にC++所有権の別記法が混入した | 第11章の装飾連結と操作履歴を生ポインタへ変更し、所有者のデストラクタで破棄。第0章で`unique_ptr`と`make_unique`はC++標準ライブラリの用語としてだけ説明し、テンプレート、ルール、Agent、validatorへ掲載禁止を反映 | 済（全パターン章のC++ブロックでスマートポインタ0件。C++実行出力照合とreleaseゲートで確認） |
| 2026-08-02 | 2-5が現在／将来の分類で終わり、整理したリスクを何の設計判断へ使うか分からなかった。フェーズ3で未確定の変更を痛みの根拠にし、フェーズ7でも未確定シナリオを完成構造へ当てていた | F-IDをフェーズ6へ接続する基準を追加した後も、旧テンプレートの「フェーズ3末尾で将来変更を目処づける」「7-4で将来変更先を予測する」を残し、工程ごとの入力をCR-ID／REQ-ID／F-IDで定義していなかった | 2-5をF-IDごとの変わる見込み・変えられる部分・守る部分へ統一し、F-IDはフェーズ2・6、CR-IDはフェーズ3と7-4、REQ-IDは7-1の受入確認で扱うと第0章・テンプレート・チェックリスト・執筆ルールへ明記。7-4は1-5と同じ変更依頼で現状影響と完成結果を比較する表へ変更した | 済（`validate_book.py`で2-4→2-5のF-ID・文言・「はい」、フェーズ3/7の未来語・F-ID不在、1-5→7-4のCR-ID・文言を検査） |
| 2026-08-02 | 将来リスクをフェーズ2で列挙しても、フェーズ6の設計判断へ紐づかず、採用構造がリスクを局所化するか、何が未解決かを追えなかった | REQ-IDとP-IDの追跡だけを必須化し、確定要求ではないリスクはコードへ実装しないことだけを確認していた。非実装と設計上の非考慮を区別する基準がなかった | 全12章の将来リスクへF-IDを付け、フェーズ6で同じID・同じ文言を使って、採用構造での考慮、将来の変更先、今回の判断を明記。第0章・テンプレート・チェックリスト・執筆ルールへ「考慮するが完成コードへ追加しない」を正本化した | 済（`validate_book.py`でF-ID連番、フェーズ2→6の順序・文言一致、構造判断、変更先、非実装判断を検査） |
| 2026-07-12 | 第0章の構造一覧で一般的なパターン名との対応が分からず、生ポインタも未定義のまま使用理由だけを説明していた | 独自語彙とC++記法を読者が既知とみなし、初出時の定義と一般名称への接続が不足していた | 第0章で生ポインタを初出定義し、第1〜8章の構造名へGoFパターン名を併記。以後、独自構造名は一般名称と最初に接続する | 目視 |
| 2026-07-12 | 最終コードの後に完成後の具体クラス図がなく、章末の抽象的なパターン骨格図と実装を比較しにくかった | 現状図と抽象パターン図は必須だったが、両者をつなぐ完成後図の配置基準が正本になかった | `chapter-template.md` と `checklist.md` に、7-1直後の実クラス図と役割対応を追加。第1章で基準形を実装し全章へ横展開する | 目視 |
| 2026-06-28 | フェーズ2見出しで `validate_book.py` が全12章を誤って不合格 | テンプレのフェーズ2見出しを新方式へ変えたとき、検証スクリプトの必須見出しを更新していなかった（結合が未明文化） | 本ファイルに「正本どうしの結合」を明記。`validate_book.py` を新見出しへ更新 | 済（validate） |
| 2026-06-28 | Mermaidノードid `S1`/`S2` を旧9ステップ表記と誤検知 | 禁止正規表現 `S0|...|S8` が広すぎ、図のノード名に当たった | `validate_book.py` の正規表現をステップ表記限定へ絞り込み | 済（validate） |
| 2026-06-28 | 中間コードが存在しないフィールド `order.customerType` を使用 | コンパイル対象（1-4・7-1）以外のコードはフィールド名のズレが機械検出されない | `rules/phase-consistency-check.md` に「全コードブロック（非コンパイル含む）でクラス・フィールド名を統一」を追記 | 一部（目視＋レビュー） |
| 2026-06-28 | 禁止表現「別途テストしてください」が混入 | 禁止パターンに無く、目視頼みだった | `validate_book.py` の `BANNED_PATTERNS` へ追加 | 済（validate） |
| 2026-06-28 | 1-1に入力→加工→出力図と簡略化表が無い | テンプレ必須だが旧章が未移行、機械チェックも無い | `rules/checklist.md` で担保（図の有無の機械チェックは今後の候補） | 未（目視） |
| 2026-06-28 | 実行結果が最終値のみで「何をいくらで買い、いくらになったか」が不明 | 実行結果の粒度基準が正本に無かった | `rules/writing-rules.md` に「実行結果ルール」、`templates/chapter-template.md`・`rules/checklist.md` に基準追記 | 未（目視） |
| 2026-06-28 | 登場クラス表が1-2末尾に置かれ章ごとに不統一 | テンプレ内が矛盾（1-3の達成基準は図の前に表、末尾注記は「1-2の後1-3の前」） | テンプレの矛盾を解消し「1-3先頭」へ統一、`rules/checklist.md` に行追加 | 未（目視） |
| 2026-06-28 | 省略した外部I/O（DB・画面等）のエラー処理・非同期が未フォロー | 簡略化の基準が「省略の明示」止まりで、失敗・非同期を扱う基準が無かった | 第1章の `process()` に外部取得の `try/catch` を追加し、1-1へ「実運用でのエラー処理と非同期」補足を追記。`templates/chapter-template.md`・`rules/checklist.md` に基準を追加 | 未（目視） |
| 2026-06-28 | `main()` の動作例（入力）と実行結果が離れ、条件と金額を照合しづらい | 実行結果が出力の羅列のみで、入力条件と結果を1行で並べる対応表が無かった | 第1章 1-4・7-1 に入力↔出力の対応表を追加。`rules/checklist.md` に近接照合のチェックを追加 | 未（目視） |
| 2026-06-28 | 2-1の「適切/混在」判定の根拠が不明 | 分類の判断基準（骨格と同じ理由で変わるか／別業務機能が変えるか）を本文に書いていなかった | 第1章 2-1に判断基準を明記。`rules/checklist.md` に「分類は判断基準を示す」チェックを追加 | 未（目視） |
| 2026-06-28 | 2-5に確定結論と「変わらないもの」が無い | 見出しは「確定」だが、変わる軸の列挙だけで当面安定の前提と📌確定が抜けていた | 第1章 2-5に当面安定の行と📌確定を追加。`rules/checklist.md` に2-5の確定チェックを追加 | 未（目視） |
| 2026-06-28 | フェーズ2の2-1で「責務混在・違反」を断定（先取り）。前回さらに判定基準を足して悪化 | 混在はフェーズ4の原因なのに、フェーズ2で✅/❌の判定表にしていた。責務は設計判断で現状配置を最初から誤りと決めつけられない、という本書の方法に反していた | 2-1を「変わりやすさの見立て＋クラス図の居場所」に再構成し混在判定を削除（混在はフェーズ4へ）。0章フェーズ2に注記、`AI_HANDOFF.md` 禁止・`templates/chapter-template.md` 2-1・`rules/checklist.md` に「フェーズ2で責務を断定しない」を追加 | 一部（checklist＋目視） |
| 2026-07-02 | 「パターン名→構造名」一括置換の破損：「装飾連結構造（デコレーター）構造」等の重複表記、コマンドオブジェクト・オブザーバー個体まで「〜構造」と呼ぶ文（第5・7・11・12章）、「修正必要があります」等の文法崩れ（第7・8章） | パターン名を機械置換した際、文脈（構造名か個体名か・助詞の接続）を確認せずに全置換した | 各章の該当文を修正。`validate_book.py` の `BANNED_PATTERNS` へ「構造（…）構造」と「修正必要が」の検出を追加 | 一部（validate。個体名の誤用は目視） |
| 2026-07-02 | フェーズ間で値・識別子・名称がすり替わる：基本価格300/400円（第6章）、決済ID credit/credit_card（第8章）、スキーマ種別（第4章）、パートナー名とID衝突（第10章）、テンプレート種別（第11章）、状態名の語彙（第12章） | 章の一部（1-5以降やステップ）だけを後から書き直し、フェーズ1の仕様・データと突き合わせなかった | 各章の値・識別子をフェーズ1の仕様へ統一。照合基準は `rules/phase-consistency-check.md` の既存ラインで担保 | 未（目視。コンパイル対象コードのみ audit で担保） |
| 2026-07-02 | 7-1のコードがログ（printAll）を出力するのに、掲載した実行結果からログ部分が欠落（第9・10・11・12章）。第10章では変更要求の中心（C社連携）が最終実行に含まれていなかった | コードへログクラスや検証を後付けした際、実行結果ブロックを更新しなかった。audit はコンパイル可否のみ確認し、実行出力と掲載結果の照合はしない | 各章の実行結果へログ出力を追記し、第10章はC社・D社の成功ケースを実行する構成へ修正 | 未（目視。実行出力照合の機械化は今後の候補） |
| 2026-07-02 | 第5章1-1の仕様図が現状に無い操作（削除・Undo・履歴）で描かれ、現状の操作（支出登録・収入登録）が図のどこに出るか読者が追えなかった | 変更要求後の全体像を先に図化してしまい、「フェーズ1は事実のみ・図の要素は動作例とコードへ紐づける」という正本の基準（テンプレート1-1確認観点）と突き合わせなかった | 図を現状仕様の語彙（支出/収入・カテゴリ判定・金額判定）へ描き直し、動作例・main・実行結果へエラーケースを追加して対応づけた。基準自体はテンプレートに既存のため正本変更なし | 未（目視。図とコードの対応は checklist で担保） |
| 2026-07-02 | 第5章フェーズ1冒頭で同じシステムを「家計簿アプリ」「収支管理アプリ」と呼び分けており、読者に別システムと誤読させる用語の揺れがあった | 執筆時にシステム呼称を統一する基準が明文化されておらず、章内で呼称を突き合わせなかった | 「家計簿アプリ」（章タイトル・目次・章内多数派の呼称）へ統一。呼称の統一は `rules/phase-consistency-check.md` の「同じ対象を追う」ラインで担保 | 未（目視。特定呼称の混入は今後 validate 候補） |
| 2026-07-02 | 変更後の入力→加工→出力図が第5章にしかなく、他11章の読者は変更要求を1-1の図と同じ形で比較できなかった。第5章1-1には根拠のない設計意図の創作（「まず正確に記録することを最優先に設計」）も混入していた | 正本（テンプレート・チェックリスト）に「1-5に変更後の入出力図を置く」という基準自体が無かった。設計意図の創作は「フェーズ1は事実のみ」の基準と突き合わせなかった | 全12章の1-5へ変更後の入出力図（1-1と同じ記法・語彙＋差分の明示）を追加。`templates/chapter-template.md` の図表一覧と1-5確認観点、`rules/checklist.md` へ基準を追加。第5章の創作された意図は事実の記述へ置き換え | 未（目視。1-5内のflowchart有無の機械チェックは今後の候補） |
| 2026-07-02 | 「一般的な設計です」「珍しくありません」「標準的な手順です」等、根拠を示せない業界一般化の断定が9章17箇所に残っており、読者に「誰がそう言った？」という疑問を持たせていた | テンプレートに「『一般的』『業界標準』と断定しない。根拠を示せない場合は範囲を限定する」の基準は既にあったが、機械チェックが無く目視頼みだった | 各章の該当文を、本文中に根拠がある業務理由だけ残して削除、または「この章のシステムでは」へ限定。`validate_book.py` の `BANNED_PATTERNS` へ検出正規表現を追加 | 済（validate。GoF解説の検証可能な記述は対象外の語形のため誤検知なし） |
| 2026-07-04 | 第11章で、現状仕様に変更後の履歴が混ざり、フェーズ2で混在判定を先取りし、フェーズ5で対策名を先出ししていた。同じフェーズ2の先取り判定が第2・4・5・7・8・9・12章にも残っていた | 正本に「現状/変更後/最終動作を分ける」「フェーズ2は仕様変化候補に留める」「フェーズ5は課題だけ、フェーズ6で代替案比較」という基準は一部あったが、章生成・レビューAgentの指示として弱かった | 第2・4・5・7・8・9・11・12章本文を修正し、`templates/chapter-template.md`、`rules/checklist.md`、`agents/chapter-agent.md`、`agents/review-agent.md`、`agents/logic-check-agent.md` に観点を追加。`review-tasks.md` に対応ログを記録 | 未（目視。フェーズ内の意味判定は checklist/Agent で担保） |
| 2026-07-04 | 各章の対策検討が、課題から候補案を絞る読者の思考よりも、最終的な構造へ進む説明に寄っていた | テンプレートとAgentに「候補案を棚卸しし、各案が解ける課題と残る課題を比べる」基準が弱く、採用理由がパターン名ではないことを章本文で十分に見せられていなかった | 全12章のフェーズ6「採用する形を決める」へ候補案比較表を追加。`templates/chapter-template.md`、`rules/checklist.md`、`agents/*` に候補案棚卸しと課題起点の採用判断を追加。パターン寄りの締め表現を一部修正 | 未（目視。意味判定は checklist/Agent で担保） |
| 2026-07-04 | 章題材では理解できても、読者が世の中の任意のシステムへ適用する入口が弱かった | 0章と章末テンプレートに「任意システムをどう観察して7フェーズへ載せるか」の基準が不足していた | 0章へ任意システム診断の入口を追加。全12章の章末へ題材置き換え手順を追加。`templates/chapter-template.md`、`rules/checklist.md`、`agents/*`、`epilogue.md` に応用観点を追加 | 未（目視。章末問いの質は checklist/Agent で担保） |
| 2026-07-04 | 1-1の入力・加工・出力図が早く出すぎ、仕様説明の代わりに読めてしまった | 正本に「図は説明後に置く」とはあったが、章本文では図前の仕様要点表が不足し、図の役割が整理図として明確でなかった | 全12章の1-1で図前に現状仕様の要点表を追加し、図見出しを「仕様整理図」へ変更。`chapter-template.md`、`rules/checklist.md`、`agents/*` に図は仕様説明後の整理であることを追記 | 未（目視。順序と図の役割は checklist/Agent で担保） |
| 2026-07-04 | 1-1の入力・加工・出力が分類名として読め、具体的に何の値・条件・出力を扱うのか分かりにくかった | 「図や表を置く」基準はあったが、代表値・条件例・加工後の値/状態・正常/異常出力例・後続での使い道まで書く基準が弱かった | 全12章の1-1仕様表を4列化し具体例を追加。`templates/chapter-template.md`、`templates/chapter0-template.md`、`rules/checklist.md`、`agents/*` に分類名だけで終わらせない基準を追加 | 未（目視。具体例の妥当性は checklist/Agent で担保） |
| 2026-07-04 | フェーズ6の候補案がフェーズ4の原因・フェーズ5の課題と対応していても、本文上で明示されず、読者が「課題に着目したからこの案が出る」という思考を追いにくかった | 正本には原因・課題・対策の対応チェックはあったが、本文で対応表として見せる基準が無く、候補案比較表だけで済む余地があった | 全12章のフェーズ6冒頭へ原因・課題・対策候補の対応表を追加。`templates/chapter-template.md`、`rules/checklist.md`、`agents/*` にフェーズ6冒頭の対応表を必須観点として追加 | 未（目視。因果の妥当性は checklist/Agent で担保） |
| 2026-07-04 | 1-5の変更後図はあっても、1-1の入力・判定・加工・出力を変更前として退避し、変更後と横比較する形が弱く、読者が差分を頭の中で再構成する必要があった | 「変更後図を1-1と同じ記法で置く」基準はあったが、変更前のスナップショットを同じ粒度で横に置く基準がなかった | 全12章の1-5へ「変更前後の入力・判定・加工・出力差分」表を追加。`templates/chapter-template.md`、`rules/checklist.md`、`agents/*` に退避して横比較する基準を追加 | 未（目視。粒度の一致は checklist/Agent で担保） |
| 2026-07-04 | グラフ追加などの外部処理が `print` 直書きで表され、実システムならAPI・DB・ファイル・通知・描画ライブラリを呼ぶというイメージが弱かった | 「printが何の代替か説明する」基準はあったが、掲載コード上も境界クラス/インターフェースを呼び、その先だけをスタブにする基準が不足していた | 第11章に `ReportRenderingApi` を追加し、グラフ/透かしは描画API境界を呼ぶ形へ修正。`templates/chapter-template.md`、`rules/checklist.md`、`agents/*` に外部処理は境界を呼び、スタブだけを `print` にする基準を追加 | 未（目視。既存章への横展開は checklist/Agent で担保） |
| 2026-07-04 | 第11章だけ境界スタブ化され、他章にはDB・画面・決済API・通知・ファイルI/Oを `std::cout` 直書きで代替する説明やコードが残っていた | 指摘対応を発生章だけで完了扱いにし、同じ正本基準を全章へ横展開する確認が不足していた | 第1・4・5・8章の主要コードへ `Renderer` / `Gateway` / `Repository` / `Client` 境界を追加し、第6・7・12章の簡略化説明を境界スタブ前提へ修正。既存の正本基準は維持し、全章検索を対応手順として記録 | 未（目視＋全章検索） |
| 2026-07-05 | 第12章で、仕様整理図が状態一覧・遷移表・承認ルール説明より前に置かれ、読者が図の意味を理解できなかった。最終コードでは利用側が `setPhase()` で状態を直接指定しており、実システムの状態保持のイメージと合わなかった | 正本には図を説明後に置く基準があったが、章本文の順序レビューで見落とした。状態管理コードもパターン説明を優先し、申請IDから状態を取得・保存する実システムの境界を省略していた | 第12章1-1の仕様整理図を状態一覧・状態遷移・承認ルール説明の後へ移動。`WorkflowCaseRepository` を追加し、`WorkflowManager` が申請IDから現在状態を読み書きする形へ修正。Phase利用箇所表と実行例を追加 | 未（目視＋audit） |
| 2026-07-05 | 第12章で、通知先をコード側の `addListener()` で直接選んでおり、実システムの「通知先をデータとして保持し、その宛先へメール等を送る」イメージと合わなかった | 通知分離構造を説明するためにリスナー登録だけを見せ、通知先データの保存・取得境界を省略していた | 第12章に `NotificationTargetRepository` と `NotificationTarget` を追加。申請IDごとに通知先・チャネルを保存し、`WorkflowManager` が通知時に読み出して `EmailNotifier` / `ChatNotifier` スタブへ渡す形へ修正 | 未（目視＋audit） |
| 2026-07-05 | 第12章の仕様整理図が入力・判定・加工・出力の分類図に留まり、具体入力がどの処理で使われ、どの出力に変わるかを読めなかった | 図を「仕様の部品を並べるもの」として作っており、代表ケースの値を通して加工結果を追う基準が徹底できていなかった | 第12章の現状図を `REQ001` の通常申請、変更後図を `REQ004` の緊急申請という具体値ベースに変更。申請IDから状態・通知先を取得し、承認者検証、状態保存、通知送信、正常/異常出力へ分岐する流れを明示 | 未（目視＋audit） |
| 2026-07-05 | 第12章の仕様整理図にエラー分岐が混ざり、正常系として何が保存・取得・通知されるかを先に把握できなかった。`print` で省略した通知やDB更新について、成功・失敗・処理時間の扱いも読者が疑問に感じやすかった | 仕様図を「全部入りの分類図」として作り、正常系、保存データ構造、エラー条件、外部境界の省略理由を分けていなかった | 第12章の図を「保存データとアクセス関係」「正常系の入力→加工→出力」「エラー条件表」に分離。テンプレートとチェックリストに、正常系優先・エラー別表・外部境界の成功/失敗/処理時間の扱い基準を追加 | 未（目視＋audit） |
| 2026-07-05 | 各章に「設計の良し悪しを判断せず、現状を事実としてそろえる」「まだ責務が混在しているとは判断しない」など、執筆者向けの進行管理が読者向け本文として残っていた | テンプレートの達成基準やレビュー観点を、そのまま本文の導入文へ転写していた | 全章の該当表現を「何を確認するか」へ言い換え。`chapter-template.md` と `rules/checklist.md` に、管理口調を本文へ出さない基準を追加 | 未（目視＋検索） |
| 2026-07-05 | 第8章で決済の本質である認証、入金待ち、手段別データ、外部API手順、失敗が薄く、単なる決済方法IDの分岐とProcessor生成に見えていた | 題材のコア処理を章作成入力として固定せず、分岐・ハンドリング・同一シグネチャへ都合よく単純化していた。代表図と全決済方法IDの網羅表も分かれていなかった | 第8章を `PaymentRequest`/`PaymentResult`、決済API境界、手段別データ、成功/保留/失敗で再構成。`chapter-template.md`、`rules/checklist.md`、`agents/*`、`review-tasks.md` に「題材のコア処理を薄めない」AX-13観点を追加 | 未（目視。意味判定は checklist/Agent で担保） |
| 2026-07-05 | 第8章だけが同期・非同期・入力差分・エラー処理を持ち、他章が単純な関数呼び出し例に寄ると、7フェーズが複雑な現場システムにも使えることを示せない | 複雑度を上げる目的と章ごとの割り当てが正本に無く、各章で単純化の方向へ戻る余地があった | 第0章、テンプレート、チェックリスト、Agentに「複雑度ストレス条件」を追加。順次実行、非同期/ポーリング、イベント、部分失敗、ジョブ/スレッド、再試行を、原因・課題・対策へ接続する基準にした | 未（目視。章ごとの本文反映は X-COMPLEX タスクで追跡） |
| 2026-07-06 | フェーズ2(2-1)が「どの仕様が変わるか」ではなく「このクラスは責任が多い/構造が良い悪い」というシステム構造評価に寄る章があった（第3・6章に知識/混在表が残存）。またフェーズ6(対策検討)の起点が、変更途中コードか現状コードか章ごとに曖昧だった | 正本に「2-1の主語は仕様変化で構造評価ではない」「フェーズ6の起点は変更途中コード」という明文が無く、旧テンプレの知識/混在表が一部章に残っていた | `templates/chapter-template.md` と `rules/checklist.md` に、2-1の着眼点（仕様変化・構造評価しない）とフェーズ6の起点（フェーズ3の変更途中コードから共通点抽出＝痛み→共通点→抽象化）を明記。第3・6章の知識/混在表を仕様変化表へ置換、第8章除く全11章のフェーズ6冒頭へ起点明示文を追加 | 一部（目視＋checklist/Agent。旧「知るべきこと」「変わる理由の分析」表の残存は今後grep候補） |
| 2026-07-14 | フェーズ6冒頭には「変更途中コードを起点にする」と書かれていたが、ステップ1直前に比較元コードがなく、第3・6・7・9・10・11・12章ではステップ1以降から仕様変更で追加した状態・通知先・履歴なども一部落ちていた | X-STAR-06を「起点という説明文と段階見出しがあるか」だけで機械確認し、比較元コードの実在、各ステップの変更要求保持、直前ステップとの差を意味確認していなかった | 全12章のステップ1直前へフェーズ3の痛みコードを再掲し、仕様が落ちていた7章の全改善ステップを修正。第0章・テンプレート・チェックリストへ「説明文だけでは不可」「全ステップで変更要求を保持」「差分抜粋は維持仕様を明記」を追加。`validate_book.py` で再掲構造・仕様継続・直前ステップ参照を必須化 | 済（全12章・全改善ステップを機械確認＋要求保持を目視照合） |
| 2026-07-12 | 全章の実システム化(R1〜R5)確認と改修で、複数章の7-1が第8章の深さに届いていなかった（chapter06はトッピングが真偽フラグ、chapter01は金額intのみ、chapter02は要求/結果オブジェクトなし、chapter03は在庫更新が状態遷移の外、chapter05はUndoが保存データへ未到達、chapter11は装飾失敗・再実行が未実装）。さらにchapter03のコードがコンパイル不可、chapter02はOTP検証未実装で掲載コードと実行結果が不一致という実バグがあった | R1〜R5が正本に明文化されておらず、各章の要求/結果オブジェクト・保存データ副作用・失敗系の深さが章ごとにばらついていた。audit_book.pyはコンパイルは見るが、掲載実行結果と実出力の一致までは自動照合しないため不一致が残存できた | `rules/checklist.md`へ「実システム化ルーブリック（R1〜R5）」節を追加し`chapter-template.md`から参照。chapter06/01/02/03/05/11を要求/結果オブジェクト・保存データ境界・状態遷移副作用・失敗/再実行で改修し、chapter03コンパイル修正・chapter02のOTP検証実装で実行結果と一致させた。chapter07/04/09_2/10/12は充足を確認。システム構成の多様化(ARCH-01〜04)基準も正本へ追加 | 済（各章 g++コンパイル＋実行出力を掲載値と照合、validate/audit 0件、mainへpush） |
| 2026-07-12 | X-STAR-13の機械監査（全12章の1-4/7-1コードを実際にコンパイル・実行し掲載実行結果と照合）で、chapter07の1-4が`main()`で`setStock("PRD003", 6)`としていたため、行4「PRD003を1減らす」が仕様表（在庫数0）・実行結果（在庫0エラー）と食い違い、実際にはエラーにならず在庫5＋通知を出していた | audit_book.pyはコンパイルのみで実行結果と実出力の一致までは自動照合しないため、値の取り違え（0のところ6）が検出されずに残った | `setStock("PRD003", 0)`へ修正し、行4が仕様表・実行結果どおり「在庫が0です。出庫できません。」を出すようにした。全12章の1-4/7-1で実出力と掲載実行結果の一致を機械照合し、他章は一致（chapter05の7-1は意図的な要約表記）を確認。chapter07のPRD001/002についても、商品マスタ値(50/3)と動作例の開始値(10/8)の関係を本文へ明記した | 済（g++実行出力と掲載値を全章照合、validate/audit 0件、開始値の説明を目視確認） |
| 2026-07-13 | 「対応済み」と聞いていたのに、掲載コードのクラス単位分割（X-STAR-03）が第3/5/6/7/10章で未実施、第8章はラベル無しのまま残っていた。システム説明に時系列の背景ストーリー・評価的伏線が第4/7/9/10/11/12章に残り、第12章の「思考の型」副題にシステム固有語が入っていた | X-STAR-03の完了判定を「1-3登場クラス表の有無」で行い、実物の1-4/7-1コードブロックが責任単位で分かれているかを見ていなかった（代理指標での誤完了）。フェーズ1の「背景・伏線・評価を書かない」基準は目視頼みで、時系列ナラティブ（「リリース当初は…次々と追加」）の機械検出が無かった | 1-4現状コードと7-1解決後コードの両方を、責任の固まり（クラス）ごとのブロック＋各ブロック直後の役割/入力/副作用説明へ全章で分割（第2/6/8章の同居ブロック分割、第5章157行モノリス分割、第2章重複ラベル解消、第3/7章の無ラベル7-1へラベル付与）。背景ストーリー・評価的伏線を除去し事実記述へ置換。第12章の思考の型を普遍的な構造表現へ。判定基準を `templates/chapter-template.md:317` と `rules/checklist.md` で「掲載コードブロックが責任単位で分かれ各ブロック直後に説明があるか（1-4と7-1の両方）」へ統一 | 済（validate/check_kindle/check_execution_output/audit=OK、ブランチへpush） |
| 2026-07-14 | フェーズ6では変更途中コードを保持していたのに、第9章のフェーズ7だけ法人向けSLAルールと保留中状態が消え、7-4も複数章で今回の変更要求ではなく将来例だけを示していた | フェーズ6内の継続だけを検証し、採用案から7-1完成コード、1-5変更要求から7-4変更シナリオへの出口側の照合を機械化していなかった | 第9章の7-1へ `CorporatePriority` と `PendingPhase`、実行例、図を復元。全12章の7-4先頭へ今回の変更要求と実際の変更先を明示し、`validate_book.py` に7-1・7-4の変更仕様継続チェックを追加 | 済（変更要求の代表要素を7-1コードと7-4表で全12章確認） |
| 2026-07-14 | フェーズ3・6の差分抜粋で、フェーズ1のDB・Registry・履歴・検証が消え、フェーズ7で突然復活したように見える章があった。タスク正本の案内も新旧で矛盾していた | 中心ロジックだけを短く見せる意図はあったが、抜粋の外で何を維持するかを書く共通契約と機械チェックがなかった。監査表も公開操作と委譲先操作を同じシグネチャとして比較していた | 対象10章のフェーズ3・6へ中間コードの継続条件を追加し、境界・検証・副作用と責任移動を明記。`validate_book.py` に境界継続チェックを追加し、`review-tasks.md` を正本へ統一 | 済（対象10章のフェーズ3・6で境界継続を機械確認） |
| 2026-07-14 | 再発防止ログの「未・一部」が本文修正の残件に見え、現行の完了判定と矛盾して読めた。1-1・1-3・1-5の定型構造も古いログでは目視確認のままだった | 表の最終列が修正状態ではなく記録時の機械チェック化状況だという定義がなく、後から追加された検証との関係も示していなかった | 列名と注記で履歴の意味を明確化し、`review-tasks.md` の過去見出しを発見時の項目と明示。`validate_book.py` に仕様要点表・仕様整理図・クラス表の先行配置・変更前後差分表・変更後図の検査を追加 | 済（全12章の必須構造をvalidateで機械確認） |
| 2026-07-18 | フェーズ6の対策コードが関数・契約・1行クラスのスケッチに縮み、元コードの構造と改行、分離クラスの生成・依存注入を読者が頭の中で補う必要があった | 「フェーズ6は設計だけ、完全実装はフェーズ7」という正本が、設計案の接続をコードで完成させないことを許していた。クラス分離の完了条件も分離先定義に寄り、Composition Root・所有・注入・実行入口を同時に示す基準がなかった | 2026-07-16の「フェーズ6に`main`を置かない」方針を改定。全12章のフェーズ6へ1-4の比較元完全コードと7-1の採用候補完全コードを同じ改行で再掲し、断片コードを撤去。第0章・テンプレート・チェックリスト・整合ルールへ完全コード、改行維持、生成・所有・依存注入・実行入口の条件を追加 | 済（validateで1-4↔比較元、7-1↔採用候補のコードを改行込みで全章照合） |
| 2026-07-15 | フェーズ6導入の定型文末尾「章の題材に合わない案を省略したり…理由を本文中で説明します」が全11章に入り、"本の書き方の方針"という編集メモが読者向け本文へ露出していた。また第4・5章のフェーズ6内に「私の経験でも、最初は…に至ったのです」という架空の著者体験、第12・7章に「不安が頭をよぎります」等の作り物の心情が残っていた | テンプレート `chapter-template.md` が編集方針の宣言文と「最初の小さな案（関数化など）で始める」をそのまま本文モデルとして持ち章へ転写されていた。「架空の著者体験を書かない」基準は目視頼みで機械検出が無かった | 全11章から末尾定型文を削除し、第4・5章の体験談を削除・第12・7章の心情を中立化。テンプレートの該当文を「編集方針の宣言を本文へ書かない」「共通シグネチャが見えていれば関数化を飛ばす」へ改め、省略理由の書き方は `rules/connection-point-review.md` 参照へ集約 | 済（validate/kindle/exec/audit=OK、mainへpush。架空体験・編集メモは grep で追跡） |
| 2026-07-15 | フェーズ1のエラー条件表が正常系図の直後に置かれ、その後にまだ状態遷移マトリクス・メニュー価格・通知先一覧・業務機能などの正常仕様表が続く章（第3〜11章の大半）があり、「システム説明の途中でエラーが差し込まれる」構成になっていた。テンプレート/チェックリスト自体が「正常系図の直後にエラー条件表」と規定しており、正本がこの配置を追認していた。あわせて第4章のエラー条件表は、掲載コードが `cout` 表示のみ（ファイル未オープン・DB無し）なのに「入力ファイルを開けない」を実エラーとして列挙し「DB保存なし」という実体のない副作用を書いていた | エラー条件表の位置基準を「正常系図の直後」と正本へ書いてしまい、正常系の各種仕様表をすべて説明し終えた後に置く（システム説明の最後）という読者フローを満たしていなかった。またエラー条件を掲載コードの実挙動と照合する仕組みがなく、print専用コードでは起こり得ないエラーが実エラーとして残っていた | 第3〜11章の9章でエラー条件表を1-1の最後（正常系の各種仕様表の後）へ移動。テンプレート・チェックリスト・writing-rules・第0章の「直後」記述を「正常系（図と各種仕様表）をすべて説明した後、1-1の最後に置く」へ改訂。第4章のエラー条件表を「掲載コードで実際に起こるエラー（未登録形式・不正行スキップ）」と「実システムの境界で起こるエラー（ファイルオープン失敗＝掲載コードでは発生しない）」に分け、副作用列を実挙動へ修正 | 済（validate/audit=OK。全12章でエラー条件表が1-2直前にあることを機械確認。他章のスタブ境界エラーは既に注記済みであることを確認） |
| 2026-07-16 | フェーズ6（対策検討）が実質「実装」になっていた。4〜9個の動くコード塊（main・実行結果つき）で段階的にコードを進化させ、構造（クラス図）と影響範囲は実装後のフェーズ7でしか現れなかった。フェーズ名は6=検討/7=実施なのに6で作り始めており、「設計を固める前にコードを書く」ため構造の選択理由と影響範囲が後追いになっていた。さらに試作の初版では構造の見立て（クラス図）を6-1の先頭に置き、答え（構造）を分解の思考より先に見せていた（本書が避ける「構造を先に選ぶ」形） | フェーズ6の正本（第0章の段階的進化アプローチ、テンプレート、チェックリスト）が「痛みコードを段階的にコードで整理する」ことを標準フローと規定し、設計（契約・データ配置・構造・影響範囲）を先に決める場だと定義していなかった。検証も段階的進化のステップ・コード継続を必須化しており、設計先行を機械判定していなかった | 全12章のフェーズ6を設計先行へ再構成：起点(痛みコード)→6-1 痛みコードを分解して接続点の形(共通シグネチャ)を探す(変換の思考、mainや実行結果を置かない)→6-2 契約とデータ配置→6-3 構造の見立て(分解の結論としてのクラス図)→6-4 影響範囲→採用。動く実装一式はフェーズ7へ集約。第0章・テンプレート・チェックリストを設計先行(6-1〜6-4)へ改訂。`validate_book.py` に設計先行フォーマットの機械判定と `check_phase6_design`(クラス図・データ配置・契約・影響範囲・採用があり main を置かない)を追加。生ポインタで参照を保持する契約には所有・非所有・生存期間の明記を必須化(第1章で顕在化) | 済（全12章 validate/check_execution_output/audit=0件、7-1は無変更でコンパイル一致、mainへpush） |
| 2026-07-19 | フェーズ6が「現行コード全体→完成候補コード全体」の比較になり、各課題のどの行へ、なぜその変更を加えるかという中間判断が見えなかった。クラス図の掲載クラス漏れ、システムが継続すべき処理の手動呼び出し、内部数値の変更前後が見えないログも章横断で残った | 2026-07-18の再掲方針が「コードを省略しない」を「フェーズ6に完成コードまで再掲する」と解釈し、振り返り用の完全コードと検討用の関連コードを区別していなかった。また図・自動連鎖・数値ログの完了条件が正本と検証に不足していた | 第0章・テンプレート・執筆ルールを「冒頭で現行コードを1回だけ全再掲→痛み差分→課題ごとに着目コード・最小変更・減った痛み・残課題を段階表示→統合完成はフェーズ7」へ改訂。全12章のフェーズ6を同構造へ変更し、実クラス図の全クラス対応、システム所有の後続処理の自動接続、数値ログの変更前→変更後、状態不変エラーを遷移矢印へ入れない基準を追加。State章はキャンセル直後のFIFO自動昇格と50/50→49/50→50/50を実装 | 済（`validate_book.py`でフェーズ6構造・1-4再掲一致・クラス図対応・State自動昇格を検査。全章C++コンパイルと横断監査を実施） |
| 2026-07-19 | フェーズ6の「原因・課題・候補」表の直後に課題カードが置かれていたが、同じ課題を再定義しているのか、候補とカードがどう接続するのか分からなかった。また課題と無関係な現行コード全体のおさらいが長く、検討対象がぼやけた | 候補表に課題IDがなく、候補の役割（何を試すか）と課題カードの役割（どこを変え何を守るか）を区別していなかった。全体構造の再確認を優先し、フェーズ5で絞った課題範囲をフェーズ6で再び広げていた | 全12章の候補表へP1/P2/P3を付け、同じIDの課題カード・関連コードへ進む説明を追加。State章で欠けていたP2候補も補完。フェーズ6の現行コード全体再掲を削除し、課題カードの着目コードに該当するフェーズ3関連コードだけをおさらいする構成へ変更。第0章・テンプレート・ルール・執筆手順も同じ論理へ更新 | 済（`validate_book.py`で候補表と課題カードのID一致、関連コードのおさらい、旧全コード見出しの不在を検査） |
| 2026-07-19 | 対策候補から課題カードへ落とし込む根拠が見えず、話が切れていた。さらに同じ課題IDが一つの表で複数行に重複し、IDが行番号なのか課題の識別子なのか分からなかった | 変更後に許す影響範囲を決める前に候補を並べ、候補から課題カードを逆算していた。IDも独立課題ではなく原因・候補の行へ付けていた | 全12章を「現在の変更影響→理想の変更影響グラフ→差から導く課題カード→カードから出す候補→課題関連コード」の順へ変更。課題IDを独立課題の識別子と定義し、P1からの連番・成果物内の重複禁止・成果物間の追跡だけでの再利用を正本へ追加 | 済（`validate_book.py`で順序、グラフ/カード/候補のID一致、重複・欠番を機械検査） |
| 2026-07-19 | 現在→理想の差、境界・契約、完了条件、候補を示しても、それぞれがどのコードへ適用され、結果として何が減り、完成コードで条件を満たしたかを一続きに追えなかった。理想グラフも前工程の痛みを置き換えており、どの過去結果を微修正したか見えにくかった | 課題カードと候補を別表にし、フェーズ6のコード適用結果とフェーズ7の完成結果を同じIDで回収する成果物がなかった。図も現在/理想の対比だけで、間の構造変更を表していなかった | 全12章を「痛みの変更影響→構造の微修正→同じ要求の再適用結果」の3領域図へ変更。差・境界/契約・完了条件・候補を同じIDの横一行へ統合し、6-2後に適用コード・微修正・結果・契約・完了判定、7-1後に完成コード結果を追加。第0章・テンプレート・ルール・Agent・validatorも同じ追跡規則へ更新 | 済（`validate_book.py`でフェーズ5引き渡し、一続き表、構造変更グラフ、コード適用結果、完成コード結果のID一致を機械検査） |
| 2026-07-19 | 「構造をどう変えるか」の図が変更1→変更2という作業フローになり、問題定義の変更影響をどの構造で切り、変更後の影響がどう変わったか読めなかった。「微修正」も変更量の小ささなのか段階的検討なのか曖昧だった | 構造の形と、その構造へ到達するコード変更手順を同じ中央領域へ混在させた | 全12章の中央をクラス・契約・委譲・登録の依存関係へ変更し、左を変更前影響、右を変更後影響として同じ課題IDで接続。「微修正」を「段階的なコード変更」へ改め、第0章で変更理由→対象→変更内容→結果→残る問題/契約→次の判断の6点を定義 | 済（`validate_book.py`で `Pain`・`TargetStructure`・`Result`、連番ID、コード変更結果の必須項目を検査） |
| 2026-07-20 | フェーズ5の後に引き渡し表・受け入れ条件・課題カードが重なり、フェーズ6でも課題別の手段比較が続いたため、前工程からシステム全体の完成構造へ進む論理が途切れて見えた。対策検討図と完成後図の差や、正常系図の成功側だけの判定も読者の見方を不明確にした | 課題を記録する成果物と設計を決める成果物の境界が曖昧で、P1〜Pnを部分案の採否に使っていた。完成可能なシステム全体ではなく途中状態を比較し、同じ採用構造をフェーズ間で再利用する機械検査も全章対象ではなかった | 第1章を完成見本として、フェーズ5を一つの接続点表とシステム全体の課題文へ集約。フェーズ6は分離方法・配置場所・組み立て方法から完成システムを決め、構造差のある完成案が複数残る場合だけ比較する型へ変更した。P1〜Pnは同一システム内の課題から完成コード結果までの追跡IDとし、フェーズ6・7のクラス図を同一定義に統一。成功側しか示さない正常系判定は直線フローへ変更し、第0章・テンプレート・ルール・全12章・validatorへ横展開した | 済（構造・Kindle・全章C++実行出力・監査0件・diffを検証） |
| 2026-07-25 | 第10章の1-1仕様図に実装クラス名が先に現れ、主要システム・対象システム・外部システム間のデータが読めなかった。さらに、仕様変更の対象外である`DeliveryResult`と`BatchLog`が変更後にだけ存在し、変更前後で保存方法まで変わっていた | 変更対象と変更対象外の共通基盤を最初に分けず、完成コードにある要素を「新要素」と誤認して変更前から削除した。既存validatorも`class`の図掲載だけを確認し、`struct`、登場型表、余分な型、孤立型、変更前後で維持すべき結果契約・保存境界を検査していなかった | 第10章をシステム全体図とシステム内部図へ分離し、現状動作例と変更後動作例を1-2／1-5へ分離。`DeliveryResult`／`BatchRecord`／`BatchLog`を変更前後の共通基盤として表・図・コードへ揃え、変更差分を連携先・通知・順次実行へ限定した。全12章にベースライン差分監査R3-Bを追加し、テンプレート・チェックリスト・validatorへ基準を導入した | 済（第10章へ新検査を適用。残章はR3-01〜R3-12とR3-Bで追跡） |
| 2026-07-25 | 第9章で変更前後の状態・型・クラス図・変更途中コードが揺れ、`TicketService`が具象Phaseと優先度Ruleの生成・配線・選択まで抱えたため、責任分離後も中心クラスの判断が多く見えた | 現状仕様、変更試行、対策図、完成コードを別々に直し、変更対象外の型と構成責任をフェーズ間で一つの契約として追っていなかった。クラス図の全クラス掲載検査はあっても、具象部品の組み立て所有者とServiceの全メンバー利用理由は検査していなかった | 変更前の`TicketStatus`／`statusName`をフェーズ1へ戻し、変更途中`main()`と変更後状態遷移仕様を追加。具象Phase・Ruleの生成・配線・選択を`TicketPolicySet`へ移し、`TicketService`を検証・委譲・保存・ログへ限定。対策検討図と完成図を同一定義にし、validatorの第9章構造トークンへ`TicketPolicySet`を追加した | 一部（構造・コード・実行結果はvalidate/exec、メンバー責任の妥当性は表とレビュー） |
| 2026-07-25 | 第10章の仕様図は連携先IDと実行種別を入力としていたが、`execute()`は連携先IDしか受け取らず、注文・在庫を取得せずに`"data"`／`"manualData"`を送っていた。内部図も全体図の言い換えで、API呼び出しの詳細が見えなかった | 月次・手動という起動方法と、注文・在庫という同期対象を同じ「実行種別」と呼び、仕様図とコードを別々に簡略化した。従来の照合は型・値・出力の存在を見ても、入力が処理選択や送信データへ実際に使われるかを検査していなかった | 入力語彙を「連携先ID・同期対象」へ確定し、`SyncRequest`と`SyncDataCatalog`を変更前から完成後まで共通化。注文`ORD001`／在庫`SKU001`を取得して`send(data)`へ渡した。第0章・テンプレート・チェックリスト・整合ルールへ「入力の実使用」を追加し、全章R4タスクと第10章の機械検査を追加した | 一部（第10章はvalidateで機械化。全章の意味監査はR4-01〜R4-12で追跡） |
| 2026-08-02 | R-IDだけが一致したまま第3・9章で要求の意味が入れ替わり、第4・8・10・11章では確定要求にない将来機能や便宜的な処理が完成コードへ混入した。さらに完成コード前の設計図がなく、対策検討から実装が突然現れて見えた | 検査がID・見出し・コンパイル・出力の存在を中心に確認し、確定要求文そのものと完成時の機能集合、設計情報の初出順を照合していなかった。完了台帳の根拠も、実在しない見出しで通過できた | 全章の要求追跡を「同じR-ID・同じ要求文・同じ順序」へ統一し、確定要求外の最終クラスと出力を除去。フェーズ7を完成クラス一覧→完成クラス図→実行シーケンス→完成コードへ統一し、第0章・テンプレート・チェックリストへ反映。`validate_book.py`へ要求文照合、全コード型の図掲載、設計先行順序を追加し、`check_completion_gate.py`へ根拠見出しの完全一致確認、`check_unused_cpp_inputs.py`へ未使用入力検査を追加 | 済（全12章の要求意味・完成構造・機能集合・未使用入力を出版完了ゲートで一括検査） |
| 2026-08-02 | 第11章の要求外機能を除く全面再構築で、正しい要求・コードと一緒に、1-4／7-1のクラス別説明、クラス図の主要操作、7フェーズの回収表、学習目標の回収、抽象パターン図まで削除された。ほかの11章には残る説明が第11章だけ欠け、A1〜A4のコードと結果も離れた一括出力になった | releaseゲートが要求、型名、見出し順、コンパイル、出力を検査しても、コードブロック直後の説明、全型を明記した一覧、章末の学習回収、パターン図の数、ケースコードと結果の隣接を確認していなかった。全面書き換え時に「誤った内容の削除」と「正しい説明の保全」を分ける差分監査も不足した | 現在のR1〜R3と完成コードを維持し、現状・完成コード全ブロックへ責任・入力・副作用説明を追加。完成27型を一覧とフェーズ6／7の同一クラス図へ明記し、原因のコード根拠、7フェーズ表、学習目標回収、抽象図・実装対応図・シーケンスを復元。A1〜A4を実行関数へ分け、各コード直後へ対応結果を配置。`validate_book.py`へ全章の章末回収・パターン図と、第11章のブロック説明・全型照合・ケース結果配置を追加 | 済（第1〜12章を横並び確認し、第11章も正本・チェックリストの説明要件へ再適合） |
| 2026-08-02 | 第11章で要求外の`ReportLog`を除く際、仕様変更のない内部デバッグログまで現状・完成コードから削除した。完成機能を要求へ絞る監査が、既存の診断基盤を「要求元のない新機能」と誤判定した | 業務要求、要求履歴、監査ログ、内部デバッグログの用途と保持データを分けず、「R-IDにないログはすべて削除」と判断した。変更前後で維持する内部基盤のコード同一性も検査していなかった | `DebugLog`をフェーズ1から掲載し、イベント名・成否・件数変化を記録する同一実装をフェーズ3・7まで維持。完全な要求を4件保持する`ReportActionHistory`と、操作結果を6件記録するDebugLogを図・コード・実行結果で分離した。`validate_book.py`へ現状／完成DebugLogの同一実装、必須説明、数値出力の検査を追加 | 済（R1〜R3は不変、DebugLogは変更対象外、全章構造・C++実行出力・未使用入力を検証） |

## 2026-07-31 実行結果の捏造を照合スクリプトが見逃す死角

**事象**：ch04の7-1実行結果に「保存金額合計: X円 -> Y円」が載っていたが、`SalesImportRepository::save()`はこの文字列を一切出力していなかった。にもかかわらず`check_execution_output.py`は緑だった。

**原因**：`check_execution_output.py`の`lines_present_in_order`は「実出力の各行が本文セクションに部分文字列として含まれるか」だけを一方向で検証する。実出力「DBへ10件を保存しました。」は本文行「DBへ10件を保存しました。 保存金額合計:…」の部分文字列として合格し、本文が余分に主張する「保存金額合計」部分（コードが出さない出力）は検証対象外だった。

**対応**：ch04の3行から「保存金額合計」を削除し実出力へ整合（A方針＝実装を正）。

**再発防止（推奨）**：`check_execution_output.py`に逆方向チェックを追加する。実行結果ブロック（cppでない```フェンス）の各非空行が実出力にも存在することを確認すれば、コードが出さない出力の捏造を検出できる。ただし「--- 行N ---」ラベルや省略記号を許容する例外処理が必要なため、導入時は誤検出に注意する。

**追記（実装済み）**：上記の逆方向チェックを`check_execution_output.py`へ実装した（`fabricated_output_lines`）。実出力の過半が一致する“本物の出力ブロック”に限り、コードが出さない行を捏造候補として不合格にする。省略記号・要約行・コードブロックは対象外。現行全章で0件（誤検出なし）、ch04スタイルの捏造行注入で検出することを確認済み。

## 2026-08-02 main()先頭提示に伴う前方宣言とvalidatorの検出衝突

**事象**：第11章★4対応で、A1〜A4を呼ぶ`main()`を各シナリオ定義より前へ移した際、(1) `main()`が未宣言の`scenarioA1`等を呼びコンパイル失敗、(2) 追加した前方宣言`void scenarioA1(...);`がvalidatorのA1〜A4結果隣接チェックの検出トークン`void scenarioA1(`と衝突し、定義ではなく前方宣言を掴んで誤検出した。

**原因**：呼び出し側を先に見せる読者向けの提示順変更が、C++の宣言順序制約と、定義を「宣言の先頭一致」で探すvalidatorの単純検出の両方に触れた。

**対応**：(1) `main()`ブロックへ4関数の前方宣言を追加してコンパイル整合を維持（読者へは「呼び出し側を先に見せるための前方宣言」と明記）。(2) validatorの検出トークンを定義シグネチャ`void scenarioX(ReportApplication& application) {`（`;`で終わる前方宣言に一致しない）へ変更。

**横展開の注意**：他章でも`main()`や利用例を個別ケースより前へ出す場合、前方宣言の追加とvalidator検出トークンの定義一致を必ずセットで確認する。CONS-065のフェーズ6再構成で提示順を変えるときの共通チェック項目とする。

## 2026-08-02 フェーズ6の物語再構成で「6-1固定」の段階コード検査が誤検出

**事象**：CONS-065で第11章フェーズ6を「課題ID1→課題ID2→課題ID3を①契約〜⑦完了条件のつながったコードで示す物語」へ全面再構成した際、採用後の連結コードを課題ID各節と「システム全体の最終構造（再結合）」へ置いた。従来の`check_phase6_complete_comparison_code`は段階コードが`### 6-1：`直下に固定で並ぶ前提で、`### 6-1：`〜`#### システム全体のコード適用結果`のcppブロック数だけを数えていたため、第11章では段階コードが0〜1件と判定され「6-1/6-2に段階C++コードが不足」と誤検出した（実際は課題ID節に十分な連結コードがある）。

**原因**：検査が採用後コードの物理的な配置（6-1直下）を前提にし、章によって「おさらいを前半に置く／後半に置く」「連結コードを課題ID節に置く／6-1節に置く」という提示順の違いを吸収できていなかった。おさらい小節と段階コードの境界を`### 6-1：`という単一見出しで区切っていた。

**対応**：`check_phase6_complete_comparison_code`のおさらい／段階コード境界を、見出し位置に依存しない形へ一般化した。おさらい小節は「`#### 課題箇所のおさらい`見出し行の直後から、次に現れる見出し（`###`／`####`）まで」で確定し、段階コードは「おさらい小節より後・`#### システム全体のコード適用結果`より前」の全cppブロックとして数える。これにより、おさらいを前半に置く本文先出し型（第11章）でも、後半に置く章（第3・10章など）でも、採用後の連結コードを正しく段階コードとして検出する。第1〜12章すべてで再検証しPASS。

**あわせて是正した第11章の逸脱**：(1) 5-1の表頭を他章共通の`| 原因として確定した事実 | …`へ戻した（第11章だけ`原因ID・確定した事実`に逸脱。原因IDは各セル本文に保持）。**※2026-08-13にこの判断は覆った。**著者指摘AF-20260813-093（課題候補表から原因IDを追跡できない）を受け、全12章と第0章の表頭を`| 原因ID・確定した事実 | …`へ統一した。当時は「第11章だけの逸脱」と読んだが、原因IDを表頭に出すほうが問題ID→原因ID→課題候補→課題IDの追跡を読者が追える。(2) 他章共通の`#### 接続点の分離・配置・組み立てを決める`見出しと3観点表（分離方法・配置場所・組み立て方法）を復元し、具体クラスへ入る前の抽象地図とした。(3) `【課題ID1〜3の原因】`ラベルを各課題節の先頭へ復元（問題ID＝原因IDを1行で明示。課題IDはフェーズ5で定義済みのため原因→課題の順序は崩さない）。

**横展開の注意（2026-08-30再更新）**：変更前コードのおさらいと`#### システム全体のコード適用結果`は置かない。一方、主要なクラス名・処理名を含む構想上の経路はフェーズ6の最初に置き、相互依存する契約・具体・生成・選択や登録・受け渡し・実行を一緒に見渡せるようにする。続く要点コードは同じ実体を上から追い、完成クラス図と実行結果はフェーズ7だけに置く。
| 2026-08-03 | フェーズ6のコード展開が、第11章だけ「課題ID別H3＋①契約→②骨格→③具体→④生成→⑤注入→⑥実行」の詳細形で、他11章は「実装ステップ1/2/3」の交互配置どまりだった。読者が課題ごとに分離→生成→注入→実行を追えない | 第11章を後から詳細形へ改善したが、テンプレ・validator・エージェントへ逆輸入されなかった（テンプレ・ドリフト）。むしろvalidatorが`#### 実装ステップ1/2/3`を必須化し、他章を旧形式に固定していた | テンプレのフェーズ6を課題ID別①〜⑥形式へ改訂／validatorを「課題ID別H3（①〜⑥）または移行期の実装ステップ」のどちらか必須へ緩和し全章移行後に新形式必須化予定／chapter・review・logic-checkエージェントとchecklistへ同形式を追記／全11章のフェーズ6を新形式へ書き直し | 進行中（正本改訂済、章の書き直しを順次） |

## 2026-08-04 完了台帳の根拠アンカーがフェーズ6書き直しで陳腐化し、mmdc未導入のため未検出だった

**事象**：`run_completion_gate.py`（統合ゲート）を実行すると`FAIL: Completion ledger`となり、`quality/completion-gate.json`の6件のCONS根拠見出し（CONS-033/034/044/052/058/061）が「存在しない見出し」を指していた。章本文・全229図の実レンダリング・C++実コンパイル＋実行照合・validate/audit/kindleはすべてPASSで、**原稿自体は正しく、ドリフトしていたのは台帳の根拠アンカーだけ**だった。

**原因（真因）**：フェーズ6の課題ID別①〜⑥形式への全面書き直し（CONS-065系）と第11章の節移動（今回の「今回実装しないもの」2-5移設等）で見出し名が変わったが、`completion-gate.json`の`evidence`アンカーが旧見出し名（`実装ステップ1（課題ID1）…`、`6-2：生成・所有・注入・実行を確定する`、`6-5：将来リスク…`、template`設計判断ごとの部分クラス図`）のまま残った。**このアンカー整合を検査するのは`check_completion_gate.py`（＝`run_completion_gate.py`の最終段）だけで、当該統合ゲートは`mmdc`（Mermaid CLI）を必要とする。作業環境に`mmdc`が未導入で、統合ゲートがMermaid描画段で失敗・中断していたため、台帳検査まで到達せず、ドリフトが長期間検出されなかった。** これまでの「全ゲート通過」は、実際には高速な個別ゲート（validate_book/audit/kindle）だけを指しており、それらは台帳アンカーを検査しない。

**対応**：(1) 6件の根拠アンカーを現行見出しへ再同期（章本文は無変更、`status: done`も維持）。(2) `mmdc`を`npx @mermaid-js/mermaid-cli`＋Chromium(`--no-sandbox`)で導入し、統合ゲートを最後まで実行して全229図の実描画＋台帳同期を確認。

**再発防止**：(1) 見出しを変える章改稿の完了条件へ「`completion-gate.json`のevidenceアンカーを新見出しへ更新」を明記する。行番号参照（`file:N`）より見出し参照（`file#見出し`）は改稿に弱いので、改稿頻度の高い章では行参照も検討する。(2) `mmdc`未導入でも台帳ドリフトを検出できるよう、`check_completion_gate.py`は`run_completion_gate.py`と**独立に**必ず実行する運用とする（mmdc非依存）。(3) 「ツールが無いため実行されず緑と見なす」を禁止し、完了条件#15（全図実レンダリング）は実際に`mmdc`で描画した証跡がある場合のみ緑とする。

## 2026-08-04 classDiagramの`class X focus`がphantom浮きクラスを量産し、検証器が誤構文を受理していた

**事象**：読者から「対策検討のクラス図に浮いているクラスが多数ある」と指摘。ソースのグラフ接続解析では浮きクラス0件だったが、**実際にMermaid CLIでレンダリングすると、`IWorkflowPhasefocus`・`DraftPhasefocus`のような関係線を持たないphantomノードが現れていた**。全13章のclassDiagramの着色指定`class X focus`（スペース形）が原因で、合計103箇所。

**原因（真因）**：Mermaidのclassifierで、classDiagramの`class X focus`は「Xに着色クラスfocusを適用」ではなく**「Xfocusという別クラスの新規宣言」**と解釈され、関係線のない孤立ノードを生む。正しい着色は`class X:::focus`または`cssClass "X" focus`。flowchartでは逆に`class X focus`（スペース形）が正しいため、図種で構文が異なる。**さらに`validate_book.py`のフェーズ6着色チェックが`^\s*class\s+\w+\s+focus\s*$`（＝phantom生成形）を"正しい着色"として明示的に受理していた**ため、誤構文が全章へ伝播・固定化していた。加えて、これまでの浮きクラス検査はソースのグラフ接続だけを見ており、**レンダリング結果のノード数を実測していなかった**ため、phantomを検出できなかった（「ツールで実描画せず緑と見なす」問題の再現）。

**対応**：(1) 全103箇所を`class X:::focus`へ一括修正（flowchartの1件のみスペース形へ戻す）。(2) `validate_book.py`の着色チェックを`:::focus`／`cssClass`要求へ改め、phantom生成形を受理しないようにした。(3) 新チェック`check_class_diagram_focus_syntax`を追加し、classDiagram内の`class X <style>`スペース形を機械的に不合格にする（flowchartは対象外）。(4) 全112枚のclassDiagramを個別レンダリングし、SVGノード数と宣言クラス数の一致（phantom0件）を実測確認。(5) checklistへ「classDiagramの着色は`:::`」を追記。

**再発防止**：(1) 浮きクラス検査は「ソースの接続解析」だけでなく「実レンダリングSVGのノード数＝宣言クラス数」を必ず実測する。(2) 検証器が特定構文を"正しい"と受理する条件は、実レンダリング結果で裏取りしてから固定する（誤構文を受理する検査は誤りを量産する）。(3) 図種（classDiagram／flowchart）で着色構文が異なる点をchecklist・validatorの両方で担保する。

## 2026-08-16 品質ゲートが「図と本文の論理矛盾」を1件も検出しなかった

**事象**：第9章のフェーズ6を6段1周形式へ書き直し、`StaffDirectory`／`TicketEventLog` を削除した後、`validate_book.py` 0件・`audit_book.py` 0件・`run_completion_gate.py --package` PASS・`check_execution_output.py` PASS（実コンパイル一致）・全245図のMermaid実レンダリングOK、という「全緑」の状態で、次の11件の論理矛盾が本文に残っていた。

| 種類 | 残っていたもの |
|---|---|
| 図がコードに無い関係を描く | 採用図・完成図の `Ticket --> ITicketPhase : 現在状態`。段1で「次状態は状態名で返す」と決めており、`Ticket` は `TicketStatus` の値しか持たない。同じ節の散文は「チケットのデータが部品を指すことはありません」と正反対のことを書いていた |
| 注記が導出を否定する | `TicketPolicySet` の注記「生成・所有・**配線**」。段4で「コンストラクタがありません。配線する処理そのものが不要になりました」と導いた直後の図 |
| 注記が改名を新設と呼ぶ | `TicketService` の注記 `【新設】`。段2で「`TicketManager` から改名する」と導出している |
| 削除した責任が残る | 監査ログを削除した後も注記・6-2・エビデンス表に「ログを実行」「ログを維持する」「各Directory」 |
| まとめ表の数値が本文と違う | 6-1の【契約】行「状態軸は2つ、優先度軸は1つ」。5-3の実数は4つと2つで、削除した用語（「渡すもの／受け取るもの」）の残骸 |
| まとめ表の数値が本文と違う | 不変条件表「`TicketService` が5つを値メンバで保持」。実際は3つ |
| 引用が原文と違う | 段3の完了条件「新しい状態クラスと遷移元の配線に閉じ」。5-3の原文は「新しい状態動作と遷移登録に閉じる」 |
| 図に使わない要素 | シーケンス図の `participant Rule as PremiumPriority`。エスカレーション経路では一度も呼ばれない |
| 先出し | 段2の散文に段3で決まる `PendingPhase` |

**原因（真因）**：検証スクリプトは**構造（見出し・表頭・ラベル・レンダリング可否）を見ており、意味を見ていない**。Mermaidチェックは「描けるか」だけを見るので、コードに無い関係を描いた図も緑になる。表の存在チェックは表頭しか見ないので、中身の数値が本文と食い違っても緑になる。そして矛盾の大半は**書き換えの取り残し**である。責任を削除する（監査ログ）／導出を変える（配線が不要になる）／数え方を変える（接続するものの個数）といった変更は、本文の1か所を直すだけでは終わらず、図の注記・エッジ・まとめ表・エビデンス表・シーケンス図まで波及する。波及先は機械では列挙できるが、正しさは機械では判定できない。

**対応**：(1) 11件すべてを本文で修正した。(2) `rules/checklist.md` のフェーズ6へ、今回の失敗をそのまま項目化した10項目を追加した（図の関係をコードで裏取りしたか／注記がそこまでに導いた事実だけか／改名は`【改名】`か／削除した責任を全文検索したか／完了条件の引用が5-3の文言どおりか／まとめ表の数値が本文と一致するか／シーケンス図の未使用participant／クラス増加数に消えたクラスと足した値型が算入されているか、ほか）。(3) `agents/logic-check-agent.md` の検出対象へ、循環導出・識別子の先出し・図が描くコードに無い関係・削除した責任の残存・まとめ表の数値ずれ・未使用participant を追加した。(4) `agents/review-agent.md` にも同じ観点のレビュー項目を追加した。

**再発防止**：(1) **構造を変える書き換えの後は、必ず人（またはlogic-check-agent）が該当フェーズを頭から通しで読む。** ゲート緑を読了の代わりにしない。(2) 責任・クラス・用語を削除したときは、その名前で全文検索し、注記・まとめ表・エビデンス表・シーケンス図まで潰す。(3) 図は「レンダリングできるか」ではなく「エッジ1本ずつがコードに存在するか」で確認する。(4) 前工程（5-3など）の文言を引用するときは、コピーして貼る。言い換えると条件が強弱する。(5) フェーズ6の識別子を並べ、初出の段が「その名前を決めた段」と一致するか機械的に確認する（この確認だけは手順化できる）。

## 2026-08-16 6段の規定を第9章1つから書き、他章の形へ過適合していた

**事象**：フェーズ6の6段（【契約】〜【利用開始】）を第9章の書き直しから導出し、テンプレート・第0章・チェックリスト・Agent へ「各章へ適用できる形」として固めた。その直後に全12章へ当てたところ、**7か所が第9章の形を一般則として書いていた**。とくに次の3つは、他章では規定どおりに書くと誤りになる。

- 「【注入】は引き当て関数が契約への参照を返す1行。コンストラクタ引数のことではない」→ 第6章（Decorator）はコンストラクタそのもの、第7章（Observer）は `attach()` 登録、第4章（Template Method）には入る行が存在しない。**規定が3章で反例に当たる。**
- 「2【安定骨格】呼ぶのは、残った側」→ 第4章の骨格は「分けた結果残ったもの」ではなく「変更前から存在していた共通手順を取り出したもの」。契約も別クラスではなく基底クラスの中にある。**見出しと中身が合わない。**
- 「5-3の接続するものの数を数え直さない」→ 第9章自身が、5-3の候補4つのうち境界を実際に流れるのは2つになっている。**規定が、それを書いた当の章と食い違っていた。**

**原因（真因）**：**サンプル1つから規則を書いた。** 第9章は State×Strategy で、(a) 契約が独立した interface、(b) 骨格が契約とは別のクラス、(c) 実装が呼び出しごとに引き当てられる、という3条件がすべて揃った章である。この本が扱う12章のうち、3条件が揃うのは4章だけで、残り8章はどれかが違う。第9章で導出が通ったことを「導出が正しい」ではなく「規則が一般に正しい」と読み替えたのが誤り。さらに、規則を書いた直後に他章へ当てる工程が無かったため、**正本へ入るまで誰も反例を探していない。**

**対応**：(1) 7件を一般化した（詳細は `review-tasks.md`「思考ロジックの見直し（2026-08-16）」）。【注入】は3形の判別表、【安定骨格】は2型の判別表、5-3は候補一覧という位置づけへ。(2) あわせて、指摘のうち第9章にも当てはまる4件（守る範囲がフェーズ7まで未検証／6段に引き返す口が無い／段4と段5の境界／課題の独立性判定）も直した。(3) 第9章を新規定へ合わせ直した。基準実装が規定と食い違うと、他のAIは章のほうを真似る。

**再発防止**：(1) **フェーズ6の規定を変えたら、正本へ入れる前に最低3章へ当てる。** 選び方は「契約が基底クラスの中にある章（第4章）」「注入がコンストラクタ・登録の章（第6章・第7章）」「課題が3つある章（第11章）」——**基準章と条件が違う章を選ぶ。** 同型の章に当てても反例は出ない。(2) 規則を書くとき、その規則が**どの条件のもとで成り立つか**を書く。「第9章ではこうだった」で止めず、「相手が実行中に入れ替わるならこう、実体ごと決まるならこう」と条件で書く。条件を書けないなら、まだ一般化できていない。(3) 断定形（「〜ではない」）は反例を1つ探してから書く。`【注入】はコンストラクタ引数のことではない` は、この手順を踏んでいれば3章で止まっていた。

## 2026-08-19 章の見出しを変える改稿で、台帳アンカーと負のテストが再びドリフトした

**事象**：全12章のフェーズ6を6段1周へ移行した結果、(1) `quality/completion-gate.json` の根拠アンカー4件（CONS-033/034/056/061）が存在しない見出しを指し、(2) `test_recurrence_checks.py` の負のテスト5件が「見逃し」になった（アンカーにしていた本文の一文が消えたため、検査対象の条件そのものが成立しなくなった）。原稿・validate・audit・kindle・実行照合はすべてPASSしていた。

**原因（真因）**：2026-08-04に同じ事象を記録し、再発防止として「見出しを変える章改稿の完了条件へ、台帳アンカーの更新を明記する」と書いたが、**明記しただけで機械的な検出手段を足していなかった。** 加えて今回は、負のテストが**特定の一文**（`**【契約】：共通契約 `IDrink` を定義する。**` など章固有の文言）をアンカーにしていたため、改稿で同じ問題が起きた。`check_phase6_overview_diagram` も同じ理由で黙って無効化されていた（第4章で「まだクラスの中身は見ません」を削除したため区切りが取れず、チェックがスキップされていた）。**特定の一文を区切りやアンカーに使う検査は、改稿のたびに静かに死ぬ。**

**対応**：(1) 台帳アンカー4件を新見出しへ再同期した。(2) 負のテスト5件を、章固有の一文ではなく**全章共通の段の見出し**（現在は`**1. 境界の表し方と、何が渡るかを決める 【契約】**`）へ再アンカーした。(3) `check_phase6_overview_diagram` の区切りを「次の見出しまで」へ一般化した。(4) 移行用分岐を撤去し、旧形式が残っていたら不合格へ反転した。

**再発防止**：(1) **検査のアンカーには、全章共通の構造（見出し・表頭・ラベル）だけを使う。** 章固有の一文を区切りやアンカーにしない。既存の検査でそうなっているものは、見つけ次第一般化する。(2) 負のテストが「見逃し」を報告したら、**まずアンカーの陳腐化を疑う**。検査ロジックより先にフィクスチャを確認する。(3) 見出しを変える改稿の後は、`check_completion_gate.py` と `test_recurrence_checks.py` を**必ず両方**回す。`validate_book.py` だけでは両方とも検出できない。(4) 2026-08-04の記録に「明記する」とだけ書いて手段を足さなかったことが、今回の再発を許した。**再発防止に「気をつける」と書いたら、それは対策ではない**と考える。

## 2026-08-22 読者の発見より先にパターン名と作図記法を教えていた

**事象**：各章のフェーズ6で「Strategyを適用する」のように解法名を先に示し、問題から境界を導く思考よりも型への当てはめに見える箇所が残っていた。第0章のクラス図説明は、実際の図を読む前にMermaidのソース記号を列挙しており、読者が必要とする線・先端・所有関係の読み方になっていなかった。また、フェーズ2の仕様見通しとフェーズ4の責任比較をともに「変わりやすい／変わりにくい」で扱い、将来不変という断定に読めた。第4章には実在しない`Main`クラスと、実コードにはない`SampleFileStore`からImporterへの依存も図示されていた。

**原因（真因）**：テンプレートと検査が、(1) パターン名をフェーズ6の説明材料にする、(2) 図の正しさを記法の有無で確認する、(3) 変更可能性を単一の二分法で表す、という作成者都合の構造を持っていた。章だけを直しても、テンプレートから同じ書き方が再生成される状態だった。さらに代表実行照合はWindowsのCRLFを差異として扱い、内容が同じ出力を不一致にしていた。

**対応**：(1) 全12章で、問題・変更理由・境界・構造・効果を先に導き、`パターン解説`直前で初めて一般名を明かす流れへ変更した。(2) 第0章へ実例クラス図、6種類の線の見た目・意味・コード上の事実・使い分け、対応するC++骨格を追加し、Mermaidソース記法の一覧を削除した。(3) フェーズ2を「変更候補／今回維持する」、フェーズ4を観測した責任の比較として定義し直した。(4) `Main`と実コードにない逆向き依存を図から除外し、関数が行う生成・注入は実行シーケンスとコードで示した。(5) テンプレート・執筆ルール・チェックリスト・Agentを同時更新し、代表実行照合ではCRLFを正規化した。

**再発防止**：(1) `validate_book.py`で、パターン解説より前のパターン名、実例関係線のないクラス図説明、架空の`Main`クラス、既知の偽依存を不合格にする。(2) `test_recurrence_checks.py`へ4つの負のテストを追加し、検査が実際に違反を捕捉することを確認する。(3) 章内参照は初出で`フェーズ3「変更を試みる（3-1）」`のように意味を併記し、番号だけを道しるべにしない。(4) フェーズ6では先に「境界を関数・表・クラス契約のどれで表すか」を判断し、クラス契約が不要ならクラス化しない。

### 2026-09-04：画像前後の参照先とA5配置が曖昧だった

**事象**：図と図の間にある文章が「この図」とだけ書かれ、上の図の説明か下の図の導入か判別できない箇所があった。縦一列のMermaid図は小さな内容のまま1ページを占有し、図の導入文だけが前ページに残る箇所もあった。コード画像には、固定行分割によって閉じ波括弧だけが次画像へ残るものが3枚あった。

**原因**：原稿の論理順だけを確認し、PDFでの改ページ後にも参照先が自明かを確認していなかった。図画像に固有の題名がなく、画像の最大表示高と縦横比もA5用に制御していなかった。コード画像も目標行数を優先し、分割後の末尾が意味のあるコード単位かを見ていなかった。

**修正**：図の前後を「次の図」「直前の図」へ明示し、出版処理で図番号付きキャプションを生成する。導入段落へ改ページ抑制を付け、縦長図の最大高を制限した。第0章のフェーズ図を2段にし、縦一列だったクラス図・状態図を横配置へ変更した。C++画像は空行・メソッド末尾・クラス末尾を優先して分け、数行の超過なら直前の画像へ結合するよう変更した。

**再発防止**：テンプレート、執筆ルール、チェックリスト、Agentへ同じ基準を追加する。PDF生成後は画像ページだけでなく、その前後ページも確認し、キャプション、導入文の孤立、縦長図の占有を検査する。さらに、HTMLから実際に参照されるコード画像を走査し、ほぼ単色の画像がないことを確認する。分割関数には「閉じ波括弧だけの画像を作らない」回帰テストを置く。

## 2026-08-23 対策検討を六つに分け、生成した実体の使われ方が見えなくなった

**事象**：契約、安定骨格、具体、生成、注入、利用開始を対等な六段階として分けた結果、契約と具体を往復して境界を確かめる関係と、生成した実体が注入され実行される一続きの経路が離れた。読者は六つの断片を自分で再結合しなければならず、とくに生成処理の目的と実行箇所を追いにくかった。

**原因**：設計上の問いと説明用の小見出しを同じ粒度で扱った。安定骨格と配置を独立した決定として数えたため、対策検討の中心である「何を分けるか」と「どう動かすか」が見えなくなった。さらに各小項目の末尾で同じ守る範囲を照合し、文章量と中断が増えた。

**対応**：フェーズ6を、(1) 契約と具体による分離、(2) 生成・注入・実行による組み立て、の二つへ再構成した。安定骨格は分離の検算とし、配置は二つの判断の中で導く。守る範囲との照合は二つの判断の末尾に一回ずつへ集約し、6-1の要約も二行へ変更した。

**再発防止**：(1) テンプレート、執筆ルール、チェックリスト、Agent、validator、負のテストを同時に更新する。(2) 一般的な注入方式の一覧は第0章だけに置き、各章は採用した経路だけを書く。(3) フェーズ6の章内参照は「契約の確認」「生成の検討」のように意味名で書き、番号だけを使わない。(4) 第4章・第6章・第9章・第12章のように構造条件が異なる章で、二つの判断が同じ実体を最後まで追えるかを確認する。

## 旧対応：2026-08-23 二つの判断を先に説明しても、全体のデータと実体の流れが見えなかった

> この節の対応は、その後いったん「完成経路の先出し」として撤回しましたが、相互依存する判断を順番に確定する説明にも無理があると再確認しました。現在の正本は、主要な名前を含む構想上の経路を仮置きし、契約・具体・生成・選択や登録・受け渡し・実行の要点コードで成立を連続確認する形です。

**事象**：六段を「契約と具体」「生成・注入・実行」の二つへまとめても、第1章では詳細が先に続き、`main()`と`OrderProcessor::process()`をまたぐ全体像を読者が自分で再構成する必要があった。さらに、`main()`で生成済みのルールを`RuleSelector`へ登録する操作、`select()`で既存ルールを選ぶ操作、選択した参照を`PaymentCalculator`へ渡す操作を、まとめて「生成・注入」と呼んでいた。

**原因**：対策検討の中心を説明上の分類に置き、実際に決めるべき「入力から結果までの全体経路」を詳細の後ろへ置いていた。また、生成・登録・選択・注入を抽象語へ寄せたため、コード上の異なる操作と責任が隠れた。

**対応**：(1) 全12章のフェーズ6を、`全体のデータと実体の流れを先に決める`→`全体の流れを実現するコードを決める`の順へ変更した。(2) 第1章は起動時の`main()`と注文時の`OrderProcessor::process()`を一本の木構造と8行の表で示し、その後に該当コードを置いた。(3) `add()`は登録、`select()`は選択、`PaymentCalculator calculator(rule)`はCalculatorの生成と選択済みルールの注入、と明記した。(4) 第0章、テンプレート、ルール、Agent、validatorを同時更新した。

**再発防止**：(1) 全体経路のH3と実行順表が詳細コードのH3より前にあることをvalidatorで固定する。(2) 全体経路表は固定の六語へ当てはめず、章で実際に起きる操作名を使う。(3) 第1章では生成・登録・選択・注入の区別を必須文として検査する。(4) 負のテストで、全体経路を後ろへ移す変更と、`select()`を注入と書き換える変更の双方を検出する。

## 2026-08-30 章ファイルの一括書き換えで、混在改行をLFへ潰した

**事象**：1-4のクラス責任表を10章から削除する際、`open(path,'w',encoding='utf-8').write(...)` で書き戻した。第6・8・9_2・10・11・12章は**CRLFとLFが混在**したファイルで、Pythonの既定の改行変換によって全行がLFへ落ちた。`git diff` がファイル全体の差分になって発覚した。

**原因（真因）**：CRLFを持つのは第0章だけだと思い込んでいた。実際には章ファイルの半数が混在改行で、第6章は2613行中2431行がCRLFだった。読み込み時の `open(...).read()` は universal newlines で `\r\n` を `\n` へ変換し、書き込み時の既定 `newline=None` は `\n` を `os.linesep`（Linuxでは `\n`）へ変換するため、**読んで書き戻すだけで改行が失われる。** 検査スクリプトは改行コードを見ていないので、`validate_book.py` も `check_kindle.py` も素通りする。

**対応**：`git checkout` で6ファイルを復元し、`open(path,'rb').read().decode('utf-8')` → 編集 → `.encode('utf-8')` → `open(path,'wb').write()` で再実行した。CR行数がHEAD比で削除行数ぶんだけ減り、それ以外は不変であることを確認した。

**再発防止**：(1) **章ファイルを書き換えるときは、常にバイト単位で読み書きする。** テキストモードの `open(path,'w')` を使わない。第0章だけの特例ではなく、全章の既定の扱いとする。(2) 一括書き換えの後は `git diff --stat` を見て、**編集量に対して差分が大きすぎないか**を確認する。全行差分は改行の破損を疑う。(3) 改行の破損は既存のどの検査にも掛からない。差分の目視が唯一の検出手段なので、コミット前に必ず見る。

## 2026-08-30 本文の定型文を文字列一致で検査していたため、一括置換で12章が落ちた

**事象**：節番号の初出へ節名を併記する一括変換（EDIT-006）をかけたところ、`validate_book.py` が12章で「2-2は1-5の確定済み変更IDを受け取る共通導入へ統一してください」を出した。検査が本文の一文 `1-5で確定した変更IDを、そのまま今回確実に変わることとして確認します。` を**リテラル一致**で要求しており、`1-5（変更要求）で確定した…` へ変わって一致しなくなったため。

**原因（真因）**：全章で同じ導入文を使わせる検査は、その一文を機械的に固定する。一方で本文の一括置換は、その一文を含む全箇所を書き換える。**「全章で同じ文言にせよ」という検査と「全章の同じ表現を一括で変えたい」という編集は、必ず衝突する。** リテラル検査があること自体は妥当だが、それがどこにあるかを編集前に把握していなかった。

**対応**：全12章が同じ形（`1-5（変更要求）で確定した…`）へそろっていることを確認したうえで、検査側の必須リテラルを併記つきへ更新した。本文を戻すのではなく検査を追随させたのは、併記が著者の求めた変更だからである。

**再発防止**：(1) 本文の一括置換をかける前に、`validate_book.py` から**リテラル一致で本文を要求している箇所**を洗い出す（`required_intro`、`required_tokens`、`REQUIRED_TABLE_HEADERS` など）。置換対象と重なるなら、検査側も同じコミットで直す。(2) 一括置換ツールの docstring とルールへ「走らせた後は `validate_book.py` を必ず回す」と書く。(3) 一括置換の結果が全章で同一形になっているかを、検査を直す前に確認する。一部だけ変わっていたら、検査ではなく置換側の不具合である。

## 冊を分けたら、冊ごとの検査を足す（2026-09-01）

**起きたこと。** 第1冊を `books/volume01-core-patterns/` へ切り出した後、
`run_completion_gate.py` は緑のままだった。緑だったのは `output/` の15ファイルで、
実際に出版する6ファイルは1件も検査されていなかった。その状態で、
存在しないクラス名の例示、参照先が別ファイルへ移った「第0章の〜」、
比較対象が消えた「1つ目です」、収録章と合わない見出し階層が残っていた。

**原因。** `validate_book.py`・`check_kindle.py`・`audit_book.py` は
`OUTPUT_DIR = BOOK_ROOT / "output"` を固定で持ち、検査対象が原稿の置き場所と
結びついていた。出版する単位（`book.json` の `chapters`）とは別物である。

**対策。** `script/check_volume.py` を足し、`books/*/publishing/book.json` を
すべて `--package` ゲートへ自動で載せた。冊が増えても、glob が拾うので
ゲート側の変更は要らない。検査するのは、冊をまたいだときに壊れるものに絞る。

1. 収録していない章番号への参照
2. 「はじめに」「おわりに」への参照先が、その冊に実在するか
3. 存在しない節への前方参照（「〜の項で示します」）
4. 例示クラス名が、その冊の掲載コードに実在するか
5. 見出しの最上位レベルが全ファイルでそろっているか（EPUB目次の階層）
6. 「N つ目です」に受け皿があるか
7. 「変更した定義はN つ」「N つの辛い状況」が直後の表・段落の数と一致するか

**一般化。** 出版する単位を増やしたら、その単位で回る検査を同時に足す。
検査対象をディレクトリ名で固定すると、単位が増えた瞬間に嘘の緑が出る。
検査対象は、出版設定（`book.json`）から解決する。

## 表をカード型の箇条書きへ機械変換し、比較できなくなった（2026-09-03）

**起きたこと。** 6インチ端末で横幅の広い表を避けるため、`detable.py`で
長い表を「項目見出し＋列名を繰り返す箇条書き」へ一括変換した。文章は折り返せる
ようになったが、複数の課題・構造・完了条件を縦横に見比べられなくなった。
同じラベルが何度も現れ、情報量も減らないため、原稿が長く見える結果になった。

**原因。** 読みやすさを「横幅に収まること」だけで判定し、表が担っていた
比較という目的を確認しなかった。また、列を減らす前に表示形式だけを変えたため、
本文と重複する説明、全行で同じ判定、読者が使わない管理情報まで残った。

**対策。** 第1冊のはじめに・Strategy・State・Observerで、同じ観点を比較する
カードを表へ戻した。その際、重複列を削り、説明用の表は原則3列へ絞った。
状態×操作や通知API差分など、縦横の対応自体に意味がある表は4列のまま残した。
`detable.py`は自動変換を廃止し、読みにくい可能性がある表の検出専用にした。

**再発防止。** 表を直す順序は、(1)読者がこの表で比べるものを一文で言う、
(2)全行で同じ列・本文と重複する列・判断に使わない列を削る、(3)原則3列にする、
(4)それでも比較でない情報だけ本文や箇条書きへ移す、とする。表示幅だけを理由に
表をカードへ一括変換しない。
