# skills/chapter-writer.md
# 章を執筆する手順スキル

---

## このスキルの役割

`patterns/*.yaml` の定義と `templates/chapter-template.md` を組み合わせて
1章分のMarkdownを生成する手順を定義する。
対象は第1〜8章。

> **注意：章の構造（何をどの節に書くか）の唯一の正は `templates/chapter-template.md`。**
> このファイルはその補完説明（品質基準・ニュアンス・生成手順）を提供する。
> 両者に差異がある場合は `chapter-template.md` を優先すること。
> 生成ルールの詳細は `agents/chapter-agent.md` を参照すること。

---

## 前提：必ず読んでいること

このスキルを使う前に以下を読んでいること：
- `../../shared/skills/author-voice.md`
- `../../shared/personas/reader-profiles.md`
- `CLAUDE.md`
- `templates/chapter-template.md`
- `agents/chapter-agent.md`（生成ルールの正）
- `rules/design-decision-guide.md`（ステップ5〜ステップ8の詳細モデル）

---

## 分量と詳しさの基準（重要）

他章との品質・ボリュームの統一を図るため、各章は **全体で約1300〜1700行程度** のしっかりとした分量になるように執筆すること。
単に説明を長くするのではなく、以下の点に注力して「詳しさ」を担保すること：

1. **実装コードの充実**：変更前・変更後のコードは省略せず、実用的な文脈（DIの組み立て、`main`関数、テストコード等）を必ず含める。
   - **【コードブロックの分割ルール】**：1つのコードブロックに複数の話題を詰め込んではならない。必ず状態や話題ごとにコードブロックを分割し、直前に説明の散文を置くこと。
2. **関係者ヒアリングの具体性**：ステップ2での会話はリアルに、なぜその決断に至ったのかの背景を厚く描く。
3. **原因分析の深掘り**：表面的な事実だけでなく「なぜその構造が辛いのか」をケーブル比喩も交えて読者が共感できるレベルで詳述する。
4. **対策案の対比**：案0〜案Nはそれぞれのトレードオフを正直に示し、どれが正解とは言わない。

---

## 7フェーズの構造

この章テンプレートは以下の7フェーズで構成される。
前段→フェーズ1〜7→後段（整理・振り返り・パターン解説）の順に並ぶ。

| フェーズ | H2見出し | 含まれる主な節 |
| :--- | :--- | :--- |
| 前段 | — | 章タイトル・核心・得られること |
| 🔵 フェーズ1：現状把握 | フェーズ1：現状把握 | 1-1 システムの背景・1-2 仕様表・1-3 クラス構成図・1-4 責任配置テーブル・1-5 依存グラフ・1-6 実装コード・1-7 実行結果・1-8 責任チェック表 |
| 🟠 フェーズ2：仮説立案 | フェーズ2：仮説立案 | 2-1 届いた変更要求・2-2 仮説テーブル・2-3 関係者ヒアリング・2-4 確定した変動/不変テーブル |
| 🟡 フェーズ3：問題特定 | フェーズ3：問題特定 | 3-1 変更シミュレーション・3-2 変更影響グラフ・3-3 痛みの言語化 |
| 🔴 フェーズ4：原因分析 | フェーズ4：原因分析 | 4-1 観察→原因テーブル・4-2 変わるもの/変わらないものテーブル・4-3 ケーブルで考える |
| 🔴 フェーズ5：課題定義 | フェーズ5：課題定義 | 5-1 接続点の特定・5-2 非機能制約・5-3 クライアントへの影響範囲・5-4 課題まとめ表 |
| 🟢 フェーズ6：対策案検討 | フェーズ6：対策案検討 | 6-1 2×2マトリクス・6-2〜6-6 案0〜案4・6-7 評価軸・6-8 コスト天秤・6-9 採用案の決定・6-10 耐久テスト |
| 🔵 フェーズ7：対策実施 | フェーズ7：対策実施 | 7-1 最終コード（全体）・7-2 変更影響グラフ（改善後）・7-3 変更シナリオ表 |
| 後段 | 整理 | 7フェーズとこの章でやったこと対応表・各クラスの最終的な責任 |
| 後段 | 振り返り | 「得られること」の確認・3つの設計原則の適用 |
| 後段 | パターン解説 | パターンの骨格クラス図・章固有マッピング図・使いどころと限界（過剰コード含む） |

---

## 生成手順

### ステップ1：pattern_file から素材を取り出す

以下の情報を確認する：
- `scenario.domain` : システムのドメイン
- `scenario.system_overview` : システムの概要
- `scenario.current_spec` : 現在の仕様
- `scenario.situation` : 変更要求（誰から・何の要求・いつまでに）
- `observations` : 観察リスト（ステップ3 変更シミュレーションの素材）
- `advanced_scenario` : より難しい変化（ステップ7 耐久テストの素材）
- `threshold` : 使う・使わない状況（ステップ7 使い分けの素材）

---

### ステップ2：各節を順番に生成する

#### 冒頭：この章を読むと得られること

**ステップ0より前に必ず書く（`> **この章の核心**` ブロックの直後）。**

`## この章を読むと得られること` というH2セクションとして配置する。

書き方の原則（詳細は `agents/chapter-agent.md` を参照）：
- 「〜できるようになる」形式で3〜4項目
- パターン名を前面に出さない（「〇〇パターンが書けるようになる」はNG）
- 以下の3視点を基本とする：
  1. どんな観点でコードの変動箇所を識別するか（変わる理由の見極め方）
  2. 接続点がどの形（2×2マトリクスのどのセル）になっているかをどう読み取るか
  3. 接続点の形を変えることで変更がどのように局所化されるか

また、書き始める前に CLAUDE.md のドメイン割り当て表を確認し、この章のドメインが割り当て済みのものと一致していることを確認する。

---

#### フェーズ1（1-1〜1-8）：現状把握 ―― システムの背景からコードを把握する

**入力：** システムのシナリオ説明 ＋ クラス構成の概要 ＋ 実装コード。
**産物：** 1-2 仕様表・1-3 クラス構成図・1-4 責任配置テーブル・1-5 依存グラフ・1-6 実装コード・1-7 実行結果・1-8 責任チェック表

3層で書く：
- **第1層**（1-1）：システムの概要（2〜3文。業務用語を定義）＋ 末尾に「一見整理されている」印象を1段落
- **第2層**（1-2〜1-4）：仕様表・クラス構成図（mermaid classDiagram）・責任配置テーブル
- **第3層**（1-5〜1-8）：依存グラフ・実装コード・実行結果・責任チェック表

当時の担当者への敬意を忘れない。「このコードが今日まで現場を支えてきた事実」に触れる。
仮説はここでは立てない。観察した事実をフェーズ2に持ち込む。

---

#### フェーズ2（2-1〜2-4）：仮説立案 ―― 変更要求を受けて変動/不変を整理する

**入力：** フェーズ1の観察結果 ＋ 変更要求（2-1）
**産物：** 確定した変動/不変テーブル（2-4。「誰の判断で変わるか」明記）

- **2-1（届いた変更要求）**：`scenario.situation` の内容を人間的な文体で書く。「誰から・何の要求が・いつまでに」の形式。著者の口癖を1箇所入れる
- **2-2（仮説テーブル）**：フェーズ1の観察から「変わりそう / 変わらなそう」を立てる（根拠列付き。断定しない）
- **2-3（関係者ヒアリング）**：「コードを読んだだけで断定するのは危険」として問答形式で会話を描く。ヒアリングで挙がった将来のリスクはフェーズ6（6-10 耐久テスト）で実演する（伏線回収）
- **2-4（確定テーブル）**：「🟢不変を契約（インターフェース）として固定し、🔴変動をその裏側に押し込む」という設計の方向を宣言して締める

---

#### フェーズ3（3-1〜3-3）：問題特定 ―― 変更を試みて痛みを発見する

- 変更要求を「素朴に実装しようとすると何が起きるか」を描く（3-1）
- 変更が飛び火する様子をmermaidで図示する（3-2）
- 「痛み」を2点、散文で言語化する（3-3。grep地獄・影響範囲の広さ等、現場の実体験に基づく辛さ）

---

#### フェーズ4（4-1〜4-3）：原因分析 ―― 「なぜ辛いのか」を構造的に言語化する

- 観察 → 原因の方向 の表を示す（4-1）
- 「変わり続けるもの ｜ 変わってほしくないもの」の2列表で仕分ける（4-2）
- 「ケーブルで考える」：現在の接続形態をケーブル比喩（Lightning/USB-C/ハブ）で示す。2×2マトリクスのどのセルに当たるかを説明してからImagePromptを出力する（4-3。詳細は `agents/chapter-agent.md` 参照）

---

#### フェーズ5（5-1〜5-4）：課題定義 ―― 解くべき問題を具体的に定める

フェーズ4で「分ける」と判断した場所に接続点を特定し、以下の4視点で課題を定義する（詳細は `rules/design-decision-guide.md` 参照）：

1. **接続点の特定**（5-1）：どこに何個の接続点があるかを明示する
2. **非機能制約の確認**（5-2）：変更頻度・パフォーマンス・メモリを表で確認する
3. **クライアントへの影響範囲**（5-3）：どのクラスが影響を受けるかを列挙する
4. **課題まとめ表**（5-4）（接続点 ｜ 分けた理由 ｜ 非機能制約 ｜ クライアント影響）を必ず出力する

---

#### フェーズ6（6-1〜6-10）：対策案検討 ―― 解決策を並べ、コストで選ぶ

この節の核心：フェーズ5の課題定義に対する正当な応答として案を提示し、採用案を決定する（詳細は `rules/design-decision-guide.md` 参照）。

- **冒頭に2×2マトリクス接続図（mermaid）を必ず出力する**（6-1）
- 案0〜案4を並列の選択肢として展開する。各案に優劣はない（6-2〜6-6）
  - 案0：構造を変えない（`if`文追加・定数変更などで対応）
  - 案4：接続点を全て変更（フェーズ5で特定した接続の形を全て変える）
  - 中間案（意味のある段階がある場合のみ追加）
- 各案のコード・クラス図・残るトレードオフをセットで示す
- **評価軸を比較より前に宣言する**（6-7。変更容易性・拡張性・テスト容易性・実装複雑度・パフォーマンス）
- コスト天秤表（案0 vs 中間案 vs 案4）で現在コスト・未来コストを比較する（6-8）
- 採用案を決定し、理由を「現在/未来コストの観点から」1〜2文で説明する（6-9）
- **耐久テスト**（6-10）：フェーズ2のヒアリングで挙がった将来の変化を実際にシミュレートする（伏線回収）

---

#### フェーズ7（7-1〜7-3）：対策実施 ―― 決断し、変化に強い設計を手に入れる

- BatchApplication / main() を含む最終コード全体を示す（7-1）
- 変更影響グラフ改善後（mermaid graph LR）を示す（7-2）
- 変更シナリオ表（シナリオ ｜ 変わるクラス ｜ 変わらないクラス）（7-3）

---

#### 整理

- **7フェーズとこの章でやったことの対応表**（フェーズ1〜フェーズ7 ｜ この章でやったこと）
- 各クラスの最終的な責任（クラス ｜ 責任 ｜ 変わる理由）
- 「このプロセスを回した結果にたどり着いた構造こそが【パターン名】パターンです」で締める

---

#### 振り返り：第0章の3つの原則はどう適用されたか

- **原則1「変わるものをカプセル化せよ」の現れ**：どのクラス・どの構造に現れているかを具体化した場所を示してから散文で説明
- **原則2「実装ではなくインターフェースに対してプログラムせよ」の現れ**：同様
- **原則3「継承よりコンポジションを優先せよ」の現れ**：同様

各原則の説明は箇条書きにせず、散文で書く。

---

## パターン解説：〇〇パターン

### パターンの骨格

このパターンが解決する構造上の問題を、ここに1〜2文で記述します。

```mermaid
classDiagram
    class Context {
    }
    class ITarget {
        <<interface>>
        +request()*
    }
    class ConcreteImplA {
        +request()
    }
    class ConcreteImplB {
        +request()
    }
    Context --> ITarget
    ITarget <|.. ConcreteImplA
    ITarget <|.. ConcreteImplB
```

- **Context**: パターンの利用者であり、抽象的なインターフェースを通じて処理を呼び出します。
- **ITarget**: 具象クラスが実装すべき共通の振る舞いを定義するインターフェースです。
- **ConcreteImplA/B**: インターフェースを具体的に実装し、それぞれの固有の振る舞いを提供します。

---

### この章の実装との対応

本章で作成したクラスと、GoFの抽象ロールを対応させた図です。

```mermaid
classDiagram
    class [章のContextクラス] {
    }
    class [章のInterface] {
        <<interface>>
    }
    class [章の具象クラスA] {
    }
    [章のContextクラス] ..> [章のInterface]
    [章のInterface] <|.. [章の具象クラスA]
```

抽象ロールと実際に動く具象クラスの対応を1〜2文で説明します。

---

### 生成後の確認（セルフチェックリスト）

#### 1. 構造チェック（節の存在・順序）

- [ ] **author-voice.md** の口癖が3〜5箇所、自然に含まれているか
- [ ] フェーズ1（1-1〜1-8）に仮説テーブルが混入していないか（仮説はフェーズ2のみ）
- [ ] 変更要求が2-1（フェーズ2の冒頭）に置かれているか（フェーズ1末尾に入っていないか）
- [ ] パターン名がフェーズ6（6-2〜6-6）の対策案で初登場しているか（それより前に出ていないか）
- [ ] 6-1に2×2マトリクス接続図（mermaid）があるか
- [ ] 6-7で評価軸が比較表（6-8）より前に宣言されているか
- [ ] 整理セクションに「7フェーズ対応表」と「最終責任テーブル」の両方があるか
- [ ] パターン解説に「GoF抽象クラス図」と「章固有マッピング図」の両方があるか
- [ ] パターン解説に「使いどころと限界」（過剰コード例含む）があるか

#### 2. 内容整合チェック

- [ ] フェーズ3（3-1）の変更シミュレーションがフェーズ1・フェーズ2の変更要求と結びついているか
- [ ] フェーズ4（4-3）のケーブル比喩末尾にImagePromptが出力されているか
- [ ] フェーズ5の接続点特定がフェーズ4の原因分析から導出されているか
- [ ] フェーズ6（6-10）の耐久テストがフェーズ2（2-3）のヒアリングで出た将来のリスクと対応しているか（伏線回収）
- [ ] フェーズ7（7-3）の変更シナリオ表で「変わるクラス・変わらないクラス」が明示されているか

#### 3. 設計判断の明示

- [ ] 各案を「正解 vs 不正解」ではなく「並列のトレードオフ」として扱っているか
- [ ] 「正解はない」という前置きの後に「今回の状況ではこう判断した」という結論が続いているか
- [ ] 「使わない方が良い状況」「過剰コード例」がパターン解説に含まれているか
