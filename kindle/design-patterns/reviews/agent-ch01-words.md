# 第1章 全文点検（03-chapter01.md 全3059行）

対象：`books/volume01-core-patterns/output/03-chapter01.md`
方法：1行目から3059行目まで通読。数を挙げている箇所は図・表・コード・実行結果・
`sources/chapter01/` の実ファイルへ突き合わせて検算した。

---

### [1] 本文が「分離が崩れた合図」と名指しした形に、同梱ファイル一式がそのままなっている

- **場所**: 03-chapter01.md:2807, 2810, 2815
- **原文**:
> | `PaymentCalculator.h` / `.cpp` | 金額計算 | 契約だけを見るので、`Discounts.h` を含めない |

> **このとおりに分けたファイル一式を用意しています。** 本書のリポジトリの次の場所に、上の表どおりのヘッダーと `main.cpp`、`Makefile` が入っています。`make run` でそのまま動きます。

> 分け方の目安は「**そのファイルが何をインクルードすることになるか**」です。`PaymentCalculator.cpp` が `Discounts.h` を含めることになったら、それは分離が崩れた合図です。

- **何が問題か**: 実際の `sources/chapter01/PaymentCalculator.h` の6〜8行目は
  `#include "Discounts.h"` / `#include "RuleSelector.h"` / `#include "DiscountRuleSet.h"` で、
  本文が「分離が崩れた合図」と定義した条件そのものを満たしている。`RuleSelector.h` も
  `Discounts.h` を含む。さらに表に無い `Order.h` が実在し、そこに `Item` `CampaignContext`
  `Order` `CustomerInfo` `CustomerDatabase` `PaymentResult` と `MemberType` / `CampaignCode`
  の8つが同居している。`PaymentCalculator.h` も `PaymentCalculator` だけでなく
  `CartPreviewService` `CheckoutResultRenderer` `OrderProcessor` の4クラスを持つ。
  つまり「上の表どおり」ではない。`make run` して確かめた読者は、章の結論
  （＝計算側は具体ルールを知らない）が手元のファイルで反証されたと読む。
  この本で唯一、読者が自分の手で検算できる成果物がここなので、外れると痛みが大きい。
- **型**: 8（「表どおり」と書いてあるが、実物がそうなっていない）
- **直し方**: `sources/chapter01/` 側を表に合わせる（`PaymentCalculator.h` から
  `Discounts.h`・`DiscountRuleSet.h` の include を外し、`RuleSelector.h` からも
  `Discounts.h` を外し、値クラス群の行を表へ足す）のが本筋。原稿だけで閉じるなら、
  2810の一文を次へ置き換える。

  > **このとおりに分けたファイル一式を用意しています。** 本書のリポジトリの次の場所に、
  > 上の表のヘッダーに加えて、値クラスをまとめた `Order.h`、`main.cpp`、`Makefile` が
  > 入っています。`make run` でそのまま動きます。

---

### [2] 「開くのが1行だけになった」という章の結論が、変更ID2の分を数えていない

- **場所**: 03-chapter01.md:2833, 2851
- **原文**:
> 変更影響グラフで当てたのと**同じ変更要求**を、完成した構造へもう一度当てます。

> **枠が2つからぬりつぶし1つ＋枠1つへ変わりました。** 変更影響グラフでは2つとも枠、つまり既に動いているコードを開くしかありませんでした。ここでぬりつぶしなのは新しく作る `SummerSaleDiscount`、枠は登録行を1行足す `DiscountRuleSet` です。**「開く」が2か所から1行だけになりました。**

- **何が問題か**: フェーズ3で当てたのは変更ID1（サマーセール追加）と変更ID2（逐次割引）の
  両方である（3-1の表の`PaymentCalculator`行の根拠が「変更ID1・変更ID2」、実際に4分岐へ
  書き換えている）。同じものを完成構造へ当てるなら、新設は `SummerSaleDiscount` と
  `SummerSaleAndCampaignDiscount` の2クラス、`DiscountRuleSet` の登録も2行増える。
  7-4の表も「変更ID2 … 複合ルールの `apply()` で8,550円を計算し」と、複合ルールの新設を
  前提にしている。7-3の図とこの一文だけが変更ID1しか当てておらず、比較の分母がずれている。
  この「2か所から1行へ」は章全体の結論なので、読者が自分で数え直すと合わない。
- **型**: 5（数を挙げている箇所の検算漏れ）
- **直し方**: 図に `SummerSaleAndCampaignDiscount` を `added` でもう1ノード足したうえで、
  2851を次へ置き換える。

  > **枠が2つから、ぬりつぶし2つ＋枠1つへ変わりました。** 変更影響グラフでは2つとも枠、
  > つまり既に動いているコードを開くしかありませんでした。ここでぬりつぶしなのは新しく作る
  > `SummerSaleDiscount` と `SummerSaleAndCampaignDiscount`、枠は登録行を2行足す
  > `DiscountRuleSet` です。**開いて中を読み直すのは、2か所から登録一覧の1か所だけになりました。**

---

### [3] 「3つを触った」と書いた直後の変更影響グラフに、2つしか出てこない

- **場所**: 03-chapter01.md:1246, 1252-1262（図）, 1264, 1268, 1283
- **原文**:
> 痛いのは結果ではなく、そこへ至る過程です。施策を1つ足しただけで、入力データ（`CampaignContext`）、計算本体（`PaymentCalculator`）、表示（`CheckoutResultRenderer`）の3つを触り、既存2分岐の条件と順序まで書き換えました。

> **枠が2つ、ぬりつぶしが0です。**

- **何が問題か**: 3-1の「手が入るのは次の3つです」の表と1246の本文は
  `CheckoutResultRenderer` を3つ目に数えているのに、3-2の変更影響グラフには
  `PaymentCalculator` と `CampaignContext` の2ノードしか無い。問題ID1
  （1283「`PaymentCalculator` と `CampaignContext` を同時修正し」）と3-3の1つ目の痛み
  （1268）からも表示クラスが消える。読者は「3つ触ったのに図は2つ。どちらが本当か」と
  止まるうえ、[2]で見たとおりこの「2」が7-3の比較の基準になっている。
  数が減った理由がどこにも書かれていない。
- **型**: 8（本文が表・図から読めないことを主張している）
- **直し方**: 図へ `CheckoutResultRenderer` を `touched` で足し、1264を次へ置き換える。

  > **枠が3つ、ぬりつぶしが0です。** 新しいルールを1つ足すだけなのに、新規に作れるものは
  > 何もなく、既に動いている `PaymentCalculator`・`CampaignContext`・`CheckoutResultRenderer`
  > を開いて書き換えることになります。さらに、同じ計算結果を表示するカートプレビューの
  > 回帰確認も要ります。

  あわせて問題ID1の痛みの文にも `CheckoutResultRenderer` を入れる。

---

### [4] 設計判断の根拠が、ヒアリング結果と正反対の数字になっている

- **場所**: 03-chapter01.md:1621
- **原文**:
> ではいま移るべきか。私は移しませんでした。**フェーズ2で「施策は年に2〜3回増える」と確認したから**です。組み合わせが問題になる規模まで、まだ距離があります。

- **何が問題か**: フェーズ2のヒアリングは「秋にはハロウィンキャンペーン、冬には年末大感謝祭など、
  **毎月のように**新しい企画を予定しています」（1058）で、リスクID1も「新しい割引ルールの追加が
  **毎月**続く」（1068）、原因（確定）も「割引ルールが「**毎月追加される**」と確認できている」
  （1411）、パターン解説も「本章で共通契約とSelectorを採用した根拠は「**毎月**新しい割引が
  追加される」というヒアリング結果でした」（3041）である。1621だけが「年に2〜3回」。
  しかもこれは「いま組み合わせ構造へ移らない」という判断の唯一の根拠として置かれているので、
  読者は「毎月増えるなら、この判断は逆になるのでは」と読む。設計判断がひっくり返る。
- **型**: その他（数字の不一致 ／ 根拠のない断定）
- **直し方**:

  > ではいま移るべきか。私は移しませんでした。**いま重なりを持つ施策が1つしかないから**です。
  > 毎月増えるのは施策の数で、重なりのクラスが増えるのは「同時に成立しうる施策」が増えたときです。
  > 組み合わせが問題になる規模まで、まだ距離があります。

---

### [5] 「フェーズ4で原因として扱います」と予告した論点が、フェーズ4に一度も出てこない

- **場所**: 03-chapter01.md:1244
- **原文**:
> `PaymentCalculator` は分岐の中で金額を引くだけで、どの割引を当てたかを名前として持っていないため、表示側へ渡す値がありません。割引名を出すには、4分岐それぞれに名前を持たせて戻り値へ足すことになります。これはフェーズ4で原因として扱います。

- **何が問題か**: フェーズ4（4-1の痛み／原因の表、4-2、4-3、原因ID表）は、選択条件と計算式の
  同居だけを扱う。「割引名を返せない」「戻り値が金額1つしかない」はどこにも出てこない。
  原因ID1・原因ID2のどちらにも入っていない。この論点が次に出るのはフェーズ6の
  「表示用の名前を返す操作も添えます」（1567）で、原因を経由せずに契約へ足されている。
  予告どおりに読み進めた読者は、フェーズ4で該当箇所を探して見つけられない。
  課題IDにもなっていないので、`name()` と `PaymentResult` は「原因→課題→対策」の線の外に浮く。
- **型**: 6（読者の判断に使わない予告）／フェーズ間の連鎖の断絶
- **直し方**: 予告を落とし、その場で言い切る。

  > `PaymentCalculator` は分岐の中で金額を引くだけで、どの割引を当てたかを名前として
  > 持っていないため、表示側へ渡す値がありません。割引名を出すには、4分岐それぞれに名前を
  > 持たせ、金額と一緒に返す形へ戻り値を変えることになります。

  そのうえで4-3の原因（確定）へ「割引の名前を知っているのは各割引の側なのに、戻り値が
  金額1つなので外へ出せない」を1文足すか、要求ID5の受入として7-1で回収する旨を
  フェーズ4ではなくフェーズ5の課題候補側で扱う。

---

### [6] 現行要求ID5の受入条件を、現状コードも現状の実行結果も満たしていない

- **場所**: 03-chapter01.md:109（現行要求ベースライン）
- **原文**:
> | 要求ID5（購入結果の表示） | 計算結果を注文確定時の購入結果として表示する | 顧客・小計・適用割引・支払金額を表示する |

- **何が問題か**: 「適用割引」を表示すると書いてあるが、1-4の `showOrderResult()` が出すのは
  顧客名・商品・条件（会員／キャンペーン）・小計・支払金額だけで、割引名は出ない。
  1-4の実行結果（739-741 ほか）にも「適用 〜」の行は無い。しかも1244で
  「どの割引を当てたかを名前として持っていない」と自ら書いている。読者は
  フェーズ1の受入条件を実行結果で確かめようとして確かめられず、以降のベースラインの
  信頼が落ちる。変更後の受入条件（886「全割引の名前と最終金額を表示する」）との差も
  出なくなり、「要求ID5が変更になった」理由が読めない。
- **型**: その他（本文の数値・記述とコード／実行結果の不一致）
- **直し方**: 109の受入条件を、現状の出力で真偽が言える形にする。

  > | 要求ID5（購入結果の表示） | 計算結果を注文確定時の購入結果として表示する | 顧客・会員種別・キャンペーン状態・小計・支払金額を表示する |

---

### [7] フェーズ3だけ、4回の実行が区切りもラベルも無く1ブロックに詰まっている

- **場所**: 03-chapter01.md:1215-1240
- **原文**:
> **`CheckoutResultRenderer` の `showOrderResult()` ―― 条件表示（変更後）**

（この太字行と `cpp` ブロックの直後に、`main()` の4実行分の出力が1ブロックで続く）

> ```
> 田中 一郎 さんの注文: ワイヤレスイヤホン 10000円
>   条件: 会員=Premium, キャンペーン=あり, サマーセール=あり
>   小計 10000円 → 支払金額 8000円
> 佐藤 花子 さんの注文: ワイヤレスイヤホン 10000円
> ```

- **何が問題か**: 3つ問題が重なっている。(a) 1-4と7-1は `--- 行N: 説明 ---` を
  `std::cout` で出して1実行1ブロックに割っているのに、3-1の `main()` にはラベル出力が
  無く、4件が空行も無しに続く。どこで切れているか読者に分からない。
  (b) 直前の太字行が `CheckoutResultRenderer` の題名なので、`build_epub.py` は
  この実行結果画像の題名を `CheckoutResultRenderer` から取る。`main()` の出力なのに
  表示クラスの結果に見える。(c) `CheckoutResultRenderer` の変更断片が、それを使う
  `main()` の後ろに置かれている。読む順が逆になっている。
- **型**: その他（実行結果は1ブロックに1つ／掲載コードの見出しは1ブロックに1つ）
- **直し方**: 3-1の `main()` へ 1-4 と同じラベル出力を入れ、実行ごとにブロックを割る。
  `CheckoutResultRenderer` の断片は `main()` より前、`PaymentCalculator`（変更後）の
  直後へ移す。ラベルは次の4つ。

  > `--- 行1: Premium・キャンペーンあり・サマーセールあり ---`
  > `--- 行2: Regular・キャンペーンあり・サマーセールあり（逐次割引） ---`
  > `--- 行3: Regular・サマーセール単独 ---`
  > `--- 行4: Regular・割引なし ---`

---

### [8] 同じ問題IDに、場所ごとに違う名前が併記されている（最大3通り）

- **場所**: 03-chapter01.md:1283, 1284, 1285, 1418, 1419, 1478, 1479
- **原文**:
> | 問題ID1（修正範囲） | `PaymentCalculator` と `CampaignContext` を同時修正し、既存割引まで再確認した | 変更ID1（サマーセール追加） |

> | 原因ID2（計算骨格が計算式を抱える） | 金額計算の骨格が、施策ごとの割引計算式まで抱えている | 問題ID1（計算本体と入力の両方を修正） |

> - **課題ID2** ← 原因ID2（金額計算の骨格が、施策ごとの計算式を抱えている）← 問題ID1（施策を1つ足すと、選択と計算を同時に直すことになる）

- **何が問題か**: 問題ID1は「修正範囲」「計算本体と入力の両方を修正」「施策を1つ足すと、
  選択と計算を同時に直すことになる」の3通り、問題ID2は「解読範囲」「差し込み位置の解読」の
  2通り、問題ID3は「壊しやすさ」「適用順序を壊しやすい」「選択条件と適用順序が読み解けず、
  触ると壊れる」の3通り。括弧の中は「思い出すための手がかり」のはずなのに、名前が毎回
  変わると、読者は同じIDなのか別物なのかを番号で確かめ直すことになる。
  変更ID→問題ID→原因ID→課題IDの線を追わせる章で、この線がいちばん切れやすい。
- **型**: その他（IDの併記名の不統一）
- **直し方**: 3-3の表で確定した名前へ全箇所をそろえる。
  問題ID1（修正範囲）／問題ID2（解読範囲）／問題ID3（壊しやすさ）。
  たとえば1419は次にする。

  > | 原因ID2（計算骨格が計算式を抱える） | 金額計算の骨格が、施策ごとの割引計算式まで抱えている | 問題ID1（修正範囲） |

---

### [9] 「main()を2ケースだけに絞ったもの」が、実際のmain()に無いケースを含んでいる

- **場所**: 03-chapter01.md:36-52, 56-60
- **原文**:
> **代表入力（現状コードの`main()`を、この2ケースだけに絞って示したもの）：**

> ```
> 佐藤 花子 さんの注文: ワイヤレスイヤホン 10000円
>   条件: 会員=Regular, キャンペーン=なし
>   小計 10000円 → 支払金額 10000円
> ```

- **何が問題か**: 1-4の `main()` に「C002（Regular）＋キャンペーンなし」の実行は無い
  （行1・行2はC001、行3はC002でキャンペーンあり、行4はC003でスマホケース、行5は未登録ID）。
  1回目として示したこの出力は、現状コードのどこからも出てこない。準備の4行も、実際の
  `main()` にある `CartPreviewService preview(db);` を落としてある。
  「絞って示したもの」と断ったうえで別物を見せているので、1-4へ着いた読者は
  最初に読んだ2ケースを探して見つけられない。
- **型**: 4（抜粋の改変）
- **直し方**: 1-4の `main()` に実在する2ケースへ差し替える（C001キャンペーンなし →
  同じ注文にキャンペーンあり、が現状の行1・行2でそのまま使える）。それが章の導入として
  重い場合は、断り書きのほうを事実に合わせる。

  > **代表入力（この2ケースだけを取り出して組んだ `main()`。現状コード全体は1-4で読みます）：**

---

### [10] 「4つのどれにも触っていません」の「4つ」が、どこにも定義されていない

- **場所**: 03-chapter01.md:1678
- **原文**:
> **守る範囲との照合：** 4つのどれにも触っていません。施策の中身を5クラスへ移しただけで、小計計算も選ぶ流れも結果返却も顧客照合も変えていません。

- **何が問題か**: この章で「守る」と確定したのは4-2の表の2行（「商品単価を順に足して小計を
  出す」「計算を頼んで最終金額を受け取る呼び出し側の流れ」）だけで、4つではない。
  2-5の「今回守る側」は5項目、1837の同じ「守る範囲との照合」は
  「顧客照合、小計計算、結果返却、公開入口`process()`の形」で、1678の4つ
  （小計計算・選ぶ流れ・結果返却・顧客照合）と中身が違う。読者は毎回どの4つかを
  探し直すことになる。さらに「選ぶ流れ…変えていません」は、課題ID1（選択条件の境界）が
  まさに選ぶ流れを `RuleSelector` へ移す話なので、章の主張と逆に読める。
- **型**: 5（数を挙げている箇所が検算できない）
- **直し方**: 4-2で確定した2つに限って言い切る。

  > **守る範囲との照合：** 4-2で守ると決めた2つ——小計の合算と、最終金額を受け取る
  > 呼び出し側の流れ——には触っていません。施策の中身を5クラスへ移しただけです。

---

### [11] 「次の3点です」の下に4項目、「3つのことが読み取れます」の後に4つ目

- **場所**: 03-chapter01.md:193-198 と 382-390
- **原文**:
> 直前のシステム内部図から読み取ることは、次の3点です。
>
> - 支払金額は、商品リストだけでは決まらず、顧客IDから取得する会員種別とキャンペーンフラグによって変わる。
> - 加工の中心は、小計の合算ではなく、どの割引ルールを選んで適用するかにある。
> - 同じ顧客ID・商品・キャンペーン条件から、注文確定結果と事前プレビューの二つへ同じ支払金額を返す。
> - 未登録顧客や空注文のように計算へ進めない場合は、次のエラー条件表で分けて扱う。

> 直前の処理シーケンス図からは3つのことが読み取れます。
> …
> そして、どのクラスがどの値（`memberType`・`int` 金額）を受け渡すかも、この時系列から確認できます。

- **何が問題か**: 前者は「3点」と宣言して4項目。しかも4つ目の「未登録顧客や空注文…」は
  図から読み取れることではない（この図には異常系の経路が1本も無い）。次に見る表の予告であって、
  図の読み取りではない。後者は「3つ」と宣言したあと、1つ目〜3つ目に続けて4つ目を足している。
  数を宣言してから外すと、読者は自分の数え間違いを疑って読み直す。
- **型**: 5（数を挙げている箇所の検算漏れ）
- **直し方**: 193の4つ目を落として3点にそろえる（エラー条件は直後のエラー条件の節が
  そのまま受け持っている）。390は「そして」以下を3つ目へ吸収する。

  > 3つ目は、顧客の取得に失敗した場合、図のメモのとおり `calculate` へ進まず、エラー表示で
  > 終わることです。あわせて、どのクラスがどの値（`memberType`・`int` 金額）を受け渡すかも、
  > この時系列から確認できます。

---

### [12] フェーズ6で見せた `PremiumDiscount` と、完成コード・実行結果で割引名が違う

- **場所**: 03-chapter01.md:1600 と 2220 / 2567
- **原文**:
> ```cpp
>     std::string name() const override { return "Premium会員割引"; }
> ```

> ```cpp
>     std::string name() const override { return "プレミアム割引"; }
> ```

- **何が問題か**: 1587で「**ここで確認するコード：`PremiumDiscount`（クラス全体）**」と
  断って見せた実装が、7-1の完成コードと違う文字列を返す。実行結果は
  「小計 10000円 → 適用 プレミアム割引 → 支払金額 8000円」（2567・2595）なので、
  フェーズ6を読んだ読者が期待する「Premium会員割引」はどこにも出てこない。
  同じ食い違いが `SummerSaleAndCampaignDiscount` にもあり、1653は全角の
  `"サマーセール＋キャンペーン"`、2242と実行結果2623は半角の
  `"サマーセール+キャンペーン"`。「クラス全体」と断った抜粋で中身が変わっている。
- **型**: 4（抜粋の改変）
- **直し方**: 1600を完成コードへそろえる。

  > ```cpp
  >     std::string name() const override { return "プレミアム割引"; }
  > ```

  1653も半角の `+` にそろえる。

---

### [13] 「変わる側／今回守る側」の2列に判定の根拠が無く、行ごとに守る側の顔ぶれが違う

- **場所**: 03-chapter01.md:1079-1082
- **原文**:
> | リスク | 変わる側 | 今回守る側 |
> |---|---|---|
> | ID1 割引ルールが毎月増える | ルールの追加と選択 | 小計の合算、注文処理、金額の返却 |
> | ID2 定額引きへ変わる | 割引条件と計算式 | 割引前金額の受け渡し、結果表示 |

- **何が問題か**: なぜID1の守る側に「結果表示」が無く、ID2の守る側に「注文処理」が無いのか、
  表からも本文からも決まらない。どちらのリスクでも結果表示も注文処理も守る側である。
  根拠の列が無いので、読者は左右が1対1で対応していると読み、「ID2では注文処理は守らないのか」
  と受け取る。4-2の表は同じ分類を「このコードが持っているもの／変更を当てたとき／どちら側か」
  と根拠付きの3列で作っており、こちらだけ形が違う。
- **型**: 2（対応していない多列表）
- **直し方**: 4-2と同じ形に寄せ、1項目1行にして根拠の列を置く。

  > | このコードが持っているもの | 変わる見込みの根拠 | どちら側か |
  > |---|---|---|
  > | どの割引を当てるかの条件 | ID1：新しい割引が毎月増える | **変わる** |
  > | 割引額の計算式 | ID2：定額クーポンの企画がある | **変わる** |
  > | 商品単価を順に足して小計を出す | どちらのリスクでも触らない | **守る** |
  > | 割引前金額の受け渡しと結果表示 | どちらのリスクでも触らない | **守る** |

---

### [14] 凡例が「青い枠」を宣言しているのに、その図に青い枠が1つも無い

- **場所**: 03-chapter01.md:952 と 954-975（図）
- **原文**:
> 変更後の仕様を、現状の仕様と同じ粒度で、正常系の入力・判定・加工・出力として確認します。**新しく足したノードは青のぬりつぶし、もとからあって中身が変わったノードは青い枠です。** 色の付いていないノードは現状のままです。

- **何が問題か**: この図で色が付くのは `class S,T,U added;` の3ノードだけで、`touched`
  （青い枠）は1つも無い。読者は「もとからあって中身が変わったノード」を図の中で探すが、
  見つからない。無いなら無いと言うか、実際に中身が変わる「割引ルールを選ぶ」「割引額を計算」
  へ枠を付けるかのどちらかで、宣言だけ置くと図を1周余計に読ませる。
- **型**: 1（情報を足していない凡例）
- **直し方**: 図の `H` と `I` を `touched` にして凡例どおりにするのが本筋。色を足さないなら
  952を次へ置き換える。

  > 変更後の仕様を、現状の仕様と同じ粒度で、正常系の入力・判定・加工・出力として確認します。
  > **青のぬりつぶしが、今回新しく足したノードです。** 色の付いていないノードは現状のままです。

---

### [15] 「※表の1行目」が、5行後ろにある別の表を指している

- **場所**: 03-chapter01.md:924
- **原文**:
> ※表の1行目（Premium・キャンペーン・サマーセールがすべて重なった場合）は、「プレミアム割引（20%引き）」が最優先され、他のキャンペーンやサマーセールは無効になるという仕様を表しています。そのため、変更後も8,000円のままとなります。

- **何が問題か**: この※の直前にある表は「仕様変更の内容」（1行目はプレミアム割引）と
  「変更対象外の共通基盤」（1行目は `CustomerDatabase`）で、どちらの1行目も
  「Premium・キャンペーン・サマーセールがすべて重なった場合」ではない。
  該当する行は5行あとの「変更後の動作例」の1行目（Premium・両方あり）である。
  読者は直前の表を見上げて対応する行を探し、見つからないまま先へ進む。
- **型**: その他（宙に浮いた指示語＋前方参照）
- **直し方**: この※を「変更後の動作例」の表の直後（936行目付近）へ移し、指し先を書き換える。

  > ※1行目（Premium・両方あり）は、「プレミアム割引（20%引き）」が最優先され、
  > キャンペーンもサマーセールも無効になる仕様です。そのため、変更後も8,000円のままです。

---

### [16] 「クラスそのものではなく受け取る相手が変わった」が、`PaymentCalculator` に当てはまらない

- **場所**: 03-chapter01.md:1957
- **原文**:
> 枠は `OrderProcessor`・`CartPreviewService`・`PaymentCalculator` の3つで、クラスそのものではなく**受け取る相手**が変わりました。割引条件を自分で書く代わりに `RuleSelector` を受け取ります。

- **何が問題か**: `PaymentCalculator` が受け取るのは `RuleSelector` ではなく
  `IDiscountRule` である（2306のコンストラクタ）。しかも公開操作が
  `int calculate(const Order&, const std::string&, const CampaignContext&)` から
  `PaymentResult calculate(const Order&)` へ変わり、引数2つが消えて戻り値の型も変わっている。
  「クラスそのものではなく受け取る相手が変わっただけ」は事実と違う。読者は完成コードで
  シグネチャが変わっているのを見て、図の説明と突き合わせられなくなる。
- **型**: その他（根拠のない断定）
- **直し方**:

  > 枠は `OrderProcessor`・`CartPreviewService`・`PaymentCalculator` の3つです。
  > 前の2つは割引条件を自分で書く代わりに `RuleSelector` を受け取るようになり、
  > `PaymentCalculator` は選ばれたルール1つを受け取って、小計と割引名を含む
  > `PaymentResult` を返す形になりました。

---

### [17] 差分表で「出力」行だけ太字が無く、変わっていないと読める

- **場所**: 03-chapter01.md:943-948
- **原文**:
> | 要素 | 変更前 | 変更後 |
> |---|---|---|
> | 入力 | 商品、顧客ID、キャンペーン状態 | **サマーセール状態が加わる** |
> | 判定 | Premiumか、キャンペーン中か | **Regularかつサマーセール中かが加わる** |
> | 加工 | 小計へ既存割引を適用 | **既存割引後へ5%引きを逐次適用** |
> | 出力 | 最終支払金額 | Regular会員ではサマーセール反映後の金額 |

- **何が問題か**: 上3行は太字＝変わったところ、という約束で読ませておいて、4行目だけ
  変わった内容が書いてあるのに素のまま。読者は「出力は変更なし」と読むが、
  セルの文言（「Regular会員ではサマーセール反映後の金額」）は変更を述べている。
  同じ表の中で太字の意味が2通りになる。
- **型**: 1（同じ印が場所によって逆の意味になる）
- **直し方**: 4行目も変更後を太字にする。

  > | 出力 | 最終支払金額 | **Regular会員はサマーセール反映後の金額になる** |

---

### [18] 「現状コードのコード」「現状コードの現状コード」という重複語が3箇所ある

- **場所**: 03-chapter01.md:276, 359, 1173
- **原文**:
> このシステムに登場するクラスと、それぞれがどの仕様を担うかを一覧で押さえます。現状コードのコードは、この責任分担に沿って読み進めます。

> 現状コードの現状コードを読む前の地図として使います。

> 上の2定義を現状コードのコードへ当てはめ、1万円の注文で4ケースを通します。

- **何が問題か**: 語が二重になっていて意味が取れない。359は文の主語も述語も揃っておらず、
  「何の地図なのか」が読み取れない。編集の途中で「現状コード」へ一括置換した跡が
  そのまま残っている。
- **型**: 7（読者が「これは何？」となる語）
- **直し方**:

  > 276 → 一覧で押さえます。**次に読む現状コードは、この責任分担に沿って読み進めます。**
  > 359 → **現状コードを読む前の地図として使います。**
  > 1173 → 上の2定義を**現状コードへ**当てはめ、1万円の注文で4ケースを通します。

---

### [19] 執筆手順・進行説明がそのまま本文に出ている（7-2は節まるごと）

- **場所**: 03-chapter01.md:941, 1372, 2827-2829（ほか430, 1314, 2048）
- **原文**:
> 現状の仕様を退避し、変更要求を当てた後の仕様と同じ粒度で並べます。以降の分析では、この差分を追います。

> 同じ構造の分岐が並んでいること自体が接続点の重要な判断材料なので、現状コードを略さずに再掲します。

> ### 7-2：動作シーケンス図の検証
>
> 完成クラス図と実行シーケンスは、完成コードへ入る前に示しました。ここまでのコード、要求追跡表、不変条件照合を証拠として、次節で変更影響を再確認します。

- **何が問題か**: 「退避し」は原稿を書く側の作業で、読者には何が起きたのか分からない。
  「再掲します」は、規約が名指しで禁じている書き方。7-2は「前に示した」「次節でやる」しか
  書いておらず、節の中に読者が使える情報が1つも無い。見出しを開いて何も無いと、
  読者は自分が何かを読み飛ばしたのかと戻る。
- **型**: 6（著者向けメモ／進行説明）
- **直し方**:

  > 941 → **変更前と変更後を同じ粒度で並べます。以降の分析では、この差分を追います。**
  > 1372 → **同じ構造の分岐が施策の数だけ並ぶことが判断材料なので、`calculate()` を全文で見ます。**
  > 7-2 → 節ごと削除し、7-1の完成シーケンスから7-3へ直接つなぐ。

---

### [20] 「後の「登録済みの顧客」で全件を並べます」という後述予告

- **場所**: 03-chapter01.md:68
- **原文**:
> **この本のコードでは、`〜Database` という名前のクラスが「あらかじめ登録されているデータ」を持ちます。** どんな行が入っているかは、後の「登録済みの顧客」で全件を並べます。

- **何が問題か**: Kindleでは先の節へ飛ぶのも戻るのも手間が大きい。ここで小見出し名を
  指されると、読者はいったん先へ飛ぶか、覚えておく負担を負う。しかも同じ3行の表は
  この後129-135と418-422の2箇所に出るので、どちらを指しているかも決まらない。
- **型**: その他（前後の行き来を強いる書き方）
- **直し方**: 予告を落とす。全件は直後の「登録済みの顧客」で自然に出てくる。

  > **この本のコードでは、`〜Database` という名前のクラスが「あらかじめ登録されている
  > データ」を持ちます。** いま引いたのはその中の1行です。

---

### [21] 「接続点」が章の3行目から未定義で使われ、意味が出るのは1407行目

- **場所**: 03-chapter01.md:18, 19
- **原文**:
> - **得られること2：** 接続点で呼び出し元がどの知識を抱えているかを調べ、「変わる理由が異なる知識が同じ場所に混在している」と現状の問題を認識できるようになる
> - **得られること3：** 接続点の形を変えると変更がどのように局所化されるかを構造から説明でき、改善後にどんな効果が生まれるかを見通せるようになる

- **何が問題か**: 「接続点」がこの章で具体物として説明されるのは1407の
  「`calculate()` が「合計金額」を出したあと、「割引後の金額」を作って `return` するまでの
  区間」で、1400行あと。読者は第0章へ戻れない前提なので、章の冒頭では「接続点」も
  「知識を抱えている」も何を指すか決まらない。得られること2は「接続点」「知識」「抱えている」
  「混在」の4語が全部この状態で、1文の中で意味が立たない。
- **型**: 3（定義していない専門語）
- **直し方**: 冒頭では、この章のコードで指せる言葉に置き換える。

  > - **得られること2：** 計算の骨格が、個々の割引の条件や割引率をどこまで自分で持っているかを
  >   調べ、「変わる理由が違うものが同じメソッドに同居している」と現状の問題を言えるようになる
  > - **得られること3：** 骨格と割引のあいだで受け渡す値を「金額を渡して、割り引いた金額を
  >   受け取る」だけに絞ると、変更がどこで止まるかを構造から説明できるようになる

---

### [22] 本文が言う「2種類の複雑さ」が、その表からは読み取れない

- **場所**: 03-chapter01.md:234-240
- **原文**:
> この表で見たいのは、「割引が複数あるから複雑」という事実だけではありません。複雑さには、割引ルールの追加・変更に関係するものと、顧客情報取得失敗のように外部境界の信頼性に属するものがあり、両者は性質の異なる複雑さです。

- **何が問題か**: 表は「複雑さ／この章での扱い／設計判断への影響」の3列で、どの行が
  どちらの性質なのかを示す列が無い。しかも3行のうち「複数の利用場所」は、本文が挙げた
  2分類（割引ルール系／外部境界系）のどちらにも入らない。読者は表を見返して分類を
  組み立て直すが、表からは決まらない。
- **型**: 8（本文が表から読めないことを主張している）
- **直し方**: 表へ性質の列を足すのが本筋。足さないなら本文で行を名指しする。

  > この表の3行は、性質が同じではありません。排他条件は割引ルールが増えるたびに動く複雑さ、
  > 顧客情報取得失敗は外部境界が失敗しうることから来る複雑さ、複数の利用場所は同じ計算を
  > 二か所から呼ぶことから来る複雑さです。この章で構造を変えて減らすのは1つ目だけです。

---

### [23] 動作例テーブルの4行目だけ、どこから来たのか分からない3,000円が出る

- **場所**: 03-chapter01.md:268
- **原文**:
> | Regular | ✗      | 割引なし              | 3,000円 → 3,000円  |

- **何が問題か**: 1-1の仕様表は商品を「ワイヤレスイヤホン 10,000円（**一例**）」とだけ
  置き、この表にも商品の列が無い。他の3行はすべて10,000円なのに、4行目だけ3,000円で、
  理由が書かれていない（3,000円は1-4の `main()` の行4で初めて出るスマホケースの値）。
  読者は「割引なしだと小計が減るのか」と読みかねない。しかも「変更後の動作例」（935）では
  同じ条件が10,000円で書かれており、突き合わせると食い違う。
- **型**: その他（唐突な数値）
- **直し方**: 4行目を他行とそろえる。商品差を見せたいなら商品の列を足す。

  > | Regular | ✗      | 割引なし              | 10,000円 → 10,000円 |

---

## 判断保留

### 保留1：完成クラス図の `RuleSelector o-- IDiscountRule`

- **場所**: 03-chapter01.md:1929
- **原文**: `    RuleSelector o-- IDiscountRule : 登録する`
- **なぜ迷ったか**: 規約が定めている線は「値メンバーは `*--`、参照・ポインタのメンバーは
  `-->`、ローカル変数は `..>`」の3つで、`o--` の意味が決まっていない。実体は
  `std::vector<std::reference_wrapper<const IDiscountRule>>` なので規約どおりなら `-->`
  だが、集約を `o--` で描くのはUMLとしては自然で、`check_diagram.py` の判定を
  読んでいないため、この記法が検査を通る想定なのか外れているのか確定できなかった。

### 保留2：1-1と1-4に同じ「登録済みの顧客」表が2回出る

- **場所**: 03-chapter01.md:131-135 と 418-422（さらに64-66に1行版）
- **なぜ迷ったか**: 規約は「1-4は現状コードと、そのコードを読むのに要る実データ表だけを置く」
  としており、1-4側の掲載は明示的に認められている。1-1側は「顧客IDから会員種別を引く」
  仕様の説明として要る、とも読める。同じ3行を1章で3回見せることの重さは、読み手の記憶に
  依存するので、削るべきかどうかを断定できなかった。

### 保留3：入力の呼び名が3通りある

- **場所**: 03-chapter01.md:30「キャンペーン期間中かどうか」／94「キャンペーンフラグ」／
  112「キャンペーン期間中フラグ（以後キャンペーンフラグ）」／153「キャンペーン状態」
- **なぜ迷ったか**: 112で正式に言い換えを宣言しているので、規約の「3か所で呼び名が違っては
  いけない」に触れるかどうかが微妙。ただし宣言（112）より前の30と94で既に2通り使っており、
  図（153）ではさらに「キャンペーン状態」になる。読者が同じものだと取り違えなく読めるかは、
  実機で通し読みしないと判断できなかった。

---

## 指摘なしと判断した観点

以下は探して、この章では問題が無い（または規約どおり）と判断した。

- **クラス図の線とコードの対応**：1-3の図（292-341）の全線をコードへ突き合わせた。
  `OrderProcessor *-- PaymentCalculator`（値メンバー）、`OrderProcessor --> CustomerDatabase`
  （参照メンバー）、`PaymentCalculator ..> Order`（引数）はすべて規約どおり。
  完成図2（1934-1955）の `OrderProcessor ..> PaymentCalculator : その場で作って使う`
  もローカル変数として正しい。
- **フェーズ3の抜粋とフェーズ1の掲載コードの一致**：3-1の変更前抜粋（1132-1138）を
  1-4の `PaymentCalculator`（553-558）と1文字ずつ照合した。改行位置とコメント文言以外は
  条件式・計算式・分岐順が完全に一致。シグネチャも戻り値の扱いも書き直されていない。
  4-2（1330-1347）と6-1（1526-1538）の抜粋も、3-1の変更後（1147-1161）と論理が一致。
- **掲載の都合で痛みを水増ししていないか**：3-1で「変更で触った」と数えた3箇所
  （`CampaignContext` のフラグ、`PaymentCalculator` の4分岐、`CheckoutResultRenderer` の
  条件表示）は、いずれも変更ID・要求IDから来ている。`main()` が値を取り出すための
  getter追加のような、掲載都合の変更を痛みへ数えている箇所は無い。
- **実行結果の金額の検算**：1-4（8000/8000/9000/3000）、3-1（8000/8550/9500/10000）、
  7-1（8000/8000/8550/9500/9000/3000）をすべて掲載コードの式で計算し直した。
  逐次割引 `(10000*90/100)*95/100 = 8550` を含め、全件一致。
  1-2・変更後の動作例・7-1のまとめ表（2751-2760）とも一致する。
- **完成図のノード数と本文の数**：「この8つは、すべて新しく作ったクラスです」（1900）は
  図の8クラスと一致。「色が付くのが3つだけで、残り4つは現状のまま」（1959）も
  図の7クラスと一致。「ぬりつぶしは `RuleSelector` と `IDiscountRule` の2つ、枠は3つ」（1957）
  も図と一致（説明文の中身だけが[16]の問題）。
- **全ノードが同じ印になる図への着色**：完成図1（1902-1930）は全8クラスが新規のため
  色を付けず、その旨を本文（1900）で1行書いている。規約どおり。
- **要求IDの受入・回帰エビデンス**：変更後ベースラインの要求ID1〜7が7-1の照合表
  （2768-2776）に欠番なく並び、それぞれ実行結果のどのシナリオで確認できるかを追える。
  要求ID3の回帰が行4bとして独立に置かれている点も、消失検出として成立している。
- **要求IDと課題IDの混線**：要求IDの受入（2764-2780）と課題IDの構造結果（2782-2789）が
  別の表に分かれ、「＝」で結んでいる箇所は無い。機能IDも設けていない。
- **フェーズ3が「まず変更してみる」に読めるか**：3-1冒頭（1093-1099）に、設計ではないこと・
  実務ではコードを書かずにやること・ここで書いたコードは捨てることの3点が明示されている。
  規約が求める3点をすべて満たしている。
- **比喩の言い直し**：「抱え込んでいる」「知っている」は使われているが、1403で
  「**本来は各割引が持っているはずなのに、計算の骨格が持っている**」と、規約が示す
  「誰が何を持っているか」の形へ同じ段落で言い直されている。
- **一文120字超**：全文を見たが、120字を明確に超える一文は見つからなかった。
  最長は2520（`DiscountRuleSet` のコピー禁止の説明）で、句点で3文に割れている。
- **6インチ端末で崩れる書き方**：言語指定なしのブロック（実行結果・構成図）は全て80桁以内。
  40桁を超える連続欧文は、コードブロック内を除いて見つからなかった
  （`PaymentCalculator::calculate(const Order&, const std::string&, const CampaignContext&)`
  は1351にあるが、独立した太字行に出ており文中には埋まっていない）。
- **Markdownの体裁**：1470（見出しが直前の本文行に接している）と2790（見出しが表の最終行に
  接している）で空行が無いが、CommonMark／GFMではどちらも見出しとして解釈されるため、
  レンダリング上の問題にはならないと判断した。
