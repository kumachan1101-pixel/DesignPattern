# CONS-065 AI作業指示書 ―― 対策検討のコード読解順を全章で再構成する

## この指示書の位置づけ

このファイルは、`review-tasks.md`に登録した`CONS-065`を別AIが実行するための具体的な指示書である。

最初に次を全文読むこと。

1. `NEXT_AI_HANDOFF.md`
2. `AI_HANDOFF.md`
3. `review-tasks.md`の`CONS-064`と`CONS-065`
4. `quality/completion-gate.json`
5. `templates/chapter-template.md`
6. このファイル

`TASK.md`と`RESTRUCTURE_PLAN.md`は過去の履歴であり、現在のタスク正本ではない。

## 現在の状態

- CONS-001〜063は完了済みである。
- CONS-064は対応途中である。追加済みの機能ID表や、機能・要求と課題を別系統で追う方針を壊さないこと。
- CONS-065は未着手である。
- `output/chapter11.md`には追加★が5件残っている。
- `book_status`は`in_progress`である。CONS-064とCONS-065の両方が完了するまで`ready`へ変更しないこと。

## 確認済みの問題

第1〜12章のフェーズ6は、ほぼすべて次の順序になっている。

```text
フェーズ6開始
→ 設計判断ごとの部分クラス図
→ システム全体の最終構造
→ 巨大な採用全体クラス図
→ 変更前／変更途中コードのおさらい
→ 所属クラスや生成元を省いた採用後の断片コード
```

この順序では、読者が変更前コード上の問題を思い出す前に、新しいクラス名と完成構造を読むことになる。そのため、次のつながりを追えない。

```text
変更前コードの判断
→ なぜ分けるのか
→ どの責任へ移すのか
→ どの構造関係を作るのか
→ 誰が具体クラスを生成するのか
→ どこへ注入・受け渡しするのか
→ 誰が所有・破棄するのか
→ 安定側が具体を意識せずどう実行するのか
→ 課題の変更影響がどう減ったのか
```

用語や見出しが存在するだけでは不十分である。初見読者が、掲載されたクラス図とコードから上記を再現できる状態にする。

## 作業の基本順序

1. 第11章のフェーズ4→5→6の接続を確認し、フェーズ6へ渡す課題がコード事実と完了条件を持つことを確認する。
2. 第11章を最初に修正し、全章へ展開する説明基準を確定する。
3. 第11章の基準を、第0章、テンプレート、Agent、ルール、チェックリスト、validatorへ反映する。
4. 第1〜12章を同じ基準で横並び監査し、必要な章だけを修正する。第1章も自動的に除外しない。
5. 全章の人手監査を行う。
6. releaseゲートを実行する。

第11章だけを修正してCONS-065を完了にしないこと。

## 部分修正で章の論理を壊さない

CONS-065は、該当箇所へ定型文を追加する一括置換タスクではない。説明順を変更すると、前後の参照、初出するクラス名、課題の完了条件、フェーズ7のコード説明まで影響する。章ごとに一つの作業単位として扱う。

### 修正前に、その章の基準線を固定する

各章を修正する前に、次を一枚の監査表へまとめる。

| 固定する対象 | 修正前に確認する内容 |
|---|---|
| 現行仕様 | 入力、判定、加工、出力、現行機能ID、現行要求ID |
| 確定変更 | 変更ID、変更後機能ID、変更後要求ID、対象外、将来リスク |
| 構造上の問題 | フェーズ3の痛み、フェーズ4の原因、フェーズ5の課題IDと完了条件 |
| 採用構造 | 分離する責任、契約、具体クラス、生成・注入・所有・実行経路 |
| 完成実装 | 完成コードの全型、主要メソッド、実行ケース、掲載出力 |
| 不変条件 | 仕様変更と課題解消に関係しない、変更前後で守るAPI・データ・結果 |

説明順の修正だけを目的とする場合、この基準線にない仕様・機能・クラス・動作を増減させない。

### 二つの追跡を混ぜない

章全体には、目的の異なる二つの追跡がある。

```text
機能・要求の追跡
機能ID → 要求ID → 完成コード → 受入・回帰結果

設計課題の追跡
変更試行 → 痛み → 原因 → 課題候補の評価 → 課題ID
→ 採用構造 → コード上の責任移動 → 変更影響の改善結果
```

両者は最終システムのコードで同時に成立するが、機能ID・要求IDと課題IDを直接対応させない。説明順を直すために、要求を原因や課題へ変換しない。

### 一章ずつ閉じてから次章へ進む

各章を次の順で完了させる。

1. フェーズ1〜7と整理を、修正前に通読する。
2. 上記の基準線を記録する。
3. フェーズ4→5→6の入力と出力を確認する。
4. フェーズ6の説明順、図、抜粋コードを修正する。
5. 移動した説明への参照、重複、先取り、初出順を章内検索で確認する。
6. フェーズ1から章末まで再度通読する。
7. 仕様、機能ID、要求ID、変更ID、リスクID、課題ID、クラス集合、コード、実行結果を修正前基準線と比較する。
8. 意図しない差分が0件であることを記録してから、次章へ進む。

第11章はこの一巡を完了し、人手監査で基準として成立してから他章へ展開する。第11章を直している途中の構成をテンプレートや全章へコピーしない。

### 機械的な一括修正を制限する

- スクリプトは、見出しの検出、対象箇所の列挙、定型表の配置、ID・クラス集合の比較など機械的な補助に使ってよい。
- 原因、課題、分離理由、継承理由、生成・注入・所有の説明本文を、全章へ同じ文で一括生成しない。
- 一括移動後は、各章で文脈、接続文、主語、クラス名、参照先を人手確認する。
- validatorがPASSしても、章全体を再通読していなければ完了にしない。

## 章全体の論理回帰チェック

各章の修正後に、次を確認する。

- フェーズ1の仕様・機能・要求と、フェーズ7の完成コード・結果が同じ機能集合である。
- フェーズ3の痛みが、実際の変更途中コードから観測できる。
- フェーズ4の原因が、フェーズ3にない問題を追加していない。
- フェーズ5の課題が、採用予定のパターンを正当化するために原因を書き換えていない。
- フェーズ6の構造が、全課題IDの完了条件を同時に満たす。
- フェーズ6で説明したクラス・関係・生成規則が、フェーズ7の完成コードと一致する。
- フェーズ7で、フェーズ6にない新しい設計判断が初出していない。
- 仕様変更や課題解消と関係のないAPI、保存データ、検証、ログ、実行結果が変化していない。
- 将来リスクを現在の完成コードへ先行実装していない。
- 移動前の説明が重複して残らず、移動後の参照先が存在する。
- 用語とクラス名が説明より先に現れず、同じ名前を章内で同じ意味に使っている。
- コード例、`main()`、実行結果が同じ入力と順序で対応している。

一つでも不一致があれば、局所的に文章を合わせて隠さず、どのフェーズの入力が誤っているかまで戻って修正する。

## 対策検討へ進む前に、原因から課題までを再確認する

CONS-065の主な修正対象はフェーズ6だが、フェーズ5の課題を正しい前提として無条件に扱わない。各章で次の接続を確認する。

```text
フェーズ3でコード上に観測した変更影響・痛み
→ フェーズ4で確定した構造上の原因
→ フェーズ5-1の課題候補と、その候補を導いた理由
→ フェーズ5-2の必要性・重複・統合／分割の全体評価
→ フェーズ5-3で確定した課題ID、接続点、変わる側、守る側、完了条件
→ フェーズ6へ渡す設計上の問い
```

次をすべて満たす場合、原因→課題は維持し、フェーズ6から説明順を修正する。

- 各原因がフェーズ3の具体的なコード行・条件・分岐・状態・依存を根拠にしている。
- 原因をそのまま言い換えて課題IDにせず、課題候補を一度置いている。
- 候補をシステム全体で評価し、重複、統合、分割、必要性を判断している。
- 課題IDごとに、変える接続点、変わる側、守る側、完了条件がある。
- 課題の完了条件をすべて満たしたとき、フェーズ3で観測した変更影響が減る理由を説明できる。
- フェーズ5に、継承、インターフェース、具体クラス、Factory、Decorator等の解決方法が先行していない。

一つでも満たさない章は、その不足箇所に限ってフェーズ4または5を修正してからフェーズ6へ進む。原因→課題を全面改稿する必要はないが、根拠が切れた課題を前提に対策を組み立てない。

## フェーズ6で固定する説明順

各課題IDについて、次の順で説明する。

1. フェーズ3のうち、その課題に直接関係する変更前／変更途中コードだけを再掲する。
2. コードのどの条件、分岐、状態、生成、呼び出しが一つのクラスへ集まっているか指し示す。
3. 何と何を分けるのか説明する。
4. なぜそれらが別の理由で変わるのか、何を安定側として守るのか説明する。
5. 責任移動を、着色した部分クラス図で示す。
6. 図に新しい型や関連を出す前に、本文で役割と必要性を説明する。
7. 部分図の直後に、採用後であることを明記した関連コードを示す。
8. 変更前コードの判断が、採用後コードのどのクラス・メソッドへ移ったかを説明する。
9. その変更によって、課題IDの完了条件をどのように満たすか確認する。
10. 全課題の分離後、生成・選択・注入・所有・破棄・実行を一つのシステムとして再結合する。
11. 具体入力を一つ使い、生成されるオブジェクト構造と実行順を示す。
12. その和として採用全体クラス図と実行シーケンスを示す。

巨大な採用全体図を最初に見せ、その後で変更前コードへ戻らないこと。

## 継承関係を採用する場合の必須説明

継承矢印をクラス図へ追加するだけでは不十分である。次を順番に説明する。

1. 何が全具体クラスで共通なのか。
2. 何が具体クラスごとに変わるのか。
3. 基底クラスまたはインターフェースが守る契約は何か。
4. 基底クラスへ置く処理・データは何か。
5. 具体クラスが実装または上書きする処理は何か。
6. 共通側が変更されない理由は何か。
7. 誰が具体クラスを選択・生成するのか。
8. 生成したオブジェクトを、どの基底型・インターフェース型として受け渡すのか。
9. 実行側が具体クラスの型判定をせず、共通契約だけを呼ぶこと。
10. 具体クラスを追加した場合に、変更する場所と変更しない場所はどこか。

共通性がない、具体クラスが一つしかない、実行側が引き続き具体型を`if`や`dynamic_cast`で判定する場合は、継承を採用した理由を再検討する。

## 「具体を生成して、契約として渡し、具体を意識せず実行する」を示す

各章で、次の知識配置が成立するかを説明する。

```text
生成する場所だけが具体クラスを知る
                 ↓
生成した具体オブジェクトを共通契約として渡す
                 ↓
安定した実行側は具体クラスを判定せず共通操作を呼ぶ
```

最低限、コードで次の二つを対応づける。

```cpp
// 具体クラスを知る生成・選択側
IContract* component = new ConcreteComponent(...);

// 具体クラスを意識しない実行側
component->execute();
```

上記は説明用の抽象例である。本文へそのまま貼らず、各章の実際のクラス名、入力、契約、実行メソッドに置き換える。

## 分離・生成・注入・所有・再結合を混同しない

各用語を次の意味で使う。

| 用語 | 本書で示す内容 |
|---|---|
| 分離 | 別の理由で変わる判断を別の責任へ移す |
| 選択 | 入力や設定から、利用する具体クラスを決める |
| 生成 | 具体クラスのインスタンスを作る |
| 注入 | 生成済みの依存インスタンスを、必要とするクラスへ外から渡す |
| 受け渡し | Factory等が生成結果を契約型で返し、利用側が受け取る。すべてを依存注入と呼ばない |
| 所有 | インスタンスの生存期間と、最後に破棄する責任を持つ |
| 組み立て | 複数のインスタンスを実行可能なオブジェクト構造へ接続する |
| 再結合 | 分離した責任を、要求を最後まで実行できる一つのシステムへ戻す |
| 実行 | 安定側が具体型を判定せず、契約の操作を呼ぶ |

「接続する」「注入する」「再結合する」だけで済ませず、実際のクラス名とコードで説明する。

## 生成時点と生存期間を分ける

各章で、少なくとも次を区別する。

- アプリケーション起動時に一度だけ生成する長寿命の部品。
- 一つの要求や操作ごとに生成する部品。
- 一回のメソッド実行中だけ存在する一時オブジェクト。
- 登録先や履歴が継続して所有するオブジェクト。
- 外側の部品が内側の部品を所有する合成構造。

生成・注入表には、生成場所だけでなく、生成時点、渡し先、契約型、所有者、破棄者を記載する。

## 掲載コードの必須条件

一メソッドだけの浮いた断片を出さない。設計判断を理解するために、最低限次が分かる範囲を掲載する。

- 変更前か、変更途中か、採用後か。
- 所属クラス。
- 必要なメンバー変数。
- コンストラクタと受け取る依存。
- 対象メソッド。
- 呼び出し元または生成元。
- 入力と戻り値。
- 所有・破棄の責任。

フェーズ6で全完成コードを再掲する必要はない。課題に関係するコードを、読者が途中を推測せず読めるまとまりで示す。全完成コードはフェーズ7へ置く。

各コードブロックの前に「変更前コード」「採用後コード」「採用後コードの関連部分」など、段階を明記する。

## 抜粋コードには、全体構造と抜粋位置を添える

フェーズ6で毎回すべてのコードを載せる必要はない。問題はコードを短くすることではなく、全体のどこを読んでいるか分からないことである。

抜粋コードの前に、最低限次を示す。

| 示す情報 | 内容 |
|---|---|
| 全体構造 | その処理が通る主要クラスの経路 |
| 今回の位置 | どのクラス・メソッドの、どの判断を抜粋するか |
| 変更段階 | 変更前、変更途中、採用後のどれか |
| 前後との接続 | 誰から呼ばれ、何を呼ぶか、何を返すか |
| 省略範囲 | 今回省略する安定処理と、全コードを確認できる節 |
| 確認目的 | この抜粋から、どの設計判断を確認するか |

例えば次のように、コードより先に位置を固定する。

```text
全体経路:
ReportApplication
→ ReportActionHistory
→ GenerateReportAction
→ ReportGenerationService
→ ReportAssembler
→ IReport

今回の抜粋:
採用後のReportAssembler::assemble()。
本文の具体クラスを選び、装飾列をIReportの入れ子へ変換する部分を示す。
検証と出力処理は安定側なので省略し、全コードはフェーズ7に掲載する。
```

コードを抜粋するときは、説明対象に応じて次を一緒に示す。

- メンバー変数を使うなら、そのメンバーの宣言とコンストラクタでどこから受け取るか。
- 仮想関数を呼ぶなら、基底契約と具体実装の対応。
- 具体クラスを`new`するなら、生成するコード、受け取る契約型、所有者。
- 別クラスへ委譲するなら、呼び出し側と呼ばれる側の両方。
- 登録や選択を説明するなら、登録コード、選択条件、返却型。
- 状態変化を説明するなら、変更前の値、変更処理、変更後の値、次に自動実行される処理。

一つの設計判断が複数クラスにまたがる場合は、一つの巨大コードブロックへ戻さず、番号付きで次のように接続する。

```text
1. 生成側のコード
2. 注入・受け渡し側のコード
3. 契約だけを使う実行側のコード
4. 所有・破棄するコード
```

各ブロックの間に「1で生成した`IReport*`を2でServiceが受け取る」のような接続文を書く。同じ変数名・型名を使い、読者に対応を推測させない。

抜粋の直後には、少なくとも次を説明する。

```text
変更前のどの判断が消えたか
→ 採用後のどのクラスへ移ったか
→ 安定側が具体を知らずに何を呼べるようになったか
→ 課題IDの完了条件へどう効いたか
```

短いコードブロック、`...`による省略、一つのメソッドだけの掲載を一律に禁止しない。ただし、上記の位置情報と接続がなければ掲載不十分と判定する。

## クラス図とコードの型を完全に対応させる

「浮いているクラス」を次の二種類に分け、どちらも残さない。

1. コードには定義されているが、登場型表または採用全体クラス図に載っていない型。
2. クラス図には載っているが、関係線がなく孤立している型、または完成コードに存在しない型。

部分クラス図と採用全体クラス図では基準が異なる。

### 部分クラス図

- その設計判断に関係する型だけへ絞ってよい。
- 抽象型と具体型だけを浮かせず、可能な限り、それを生成・登録・選択・注入・実行する側も含める。
- 図に載せたすべての型へ、継承、実装、保持、生成、登録、選択、引数、戻り値、呼び出しのいずれかの関係線を付ける。
- 部分図の型と関係は、後続の採用全体図の部分集合にする。

### 採用全体クラス図とフェーズ7の完成クラス図

- 完成コードで定義するすべての`class`と`struct`を、登場型表とクラス図へ載せる。
- データ型も、要求・設定・結果を受け渡す責任として省略しない。
- 完成コードに存在しない型を図へ載せない。
- 図中のすべての型に、コード上の根拠を持つ関係線を一つ以上付ける。
- 継承、実装、所有、参照、生成、注入、呼び出しを同じ線種・同じラベルで混同しない。
- 具体クラスの継承関係だけで終わらせず、具体クラスを作る側と、契約型で実行する側まで接続する。
- フェーズ6の採用全体図とフェーズ7の完成クラス図は、同じ構造定義にする。見た目を変えるために型や関係を増減させない。

変更影響グラフやシーケンス図についても、意味のない孤立ノードを残さない。変更対象外の安定クラスを示す場合は、実行・利用・所有など実際の関係へ接続し、「変更線が伸びない」ことと「システムから孤立している」ことを区別する。図の直前または直後に、線と色の読み方を説明する。

## 第11章で必ず直す内容

### 課題ID1：本文生成

- `ReportSkeleton`を継承する理由を、共通生成順と本文差分から説明する。
- `create()`が固定する処理と、具体クラスが上書きする`renderBody()`を対応づける。
- `ReportAssembler`が具体レポートを選択・生成することを示す。
- `ReportGenerationService`は`IReport`として受け取り、具体レポートを判定せず`create()`を呼ぶことを示す。

### 課題ID2：装飾

- 継承だけでなく、`ReportFeature`が内側の`IReport`を所有する合成関係を説明する。
- `LogoFeature`等が一つの装飾だけを追加し、前後順を判断しないことを示す。
- `ReportAssembler`が入力順に具体Featureを生成して包むことを示す。
- 例えば`Logo→Graph`入力から、次の実際の入れ子構造を示す。

```text
GraphFeature
  └─ LogoFeature
       └─ ExecutiveMonthlyReport
```

- 本文→ロゴ→グラフの実行順と、外側を削除したときの内側への破棄順を説明する。

### 課題ID3：操作履歴

- `IReportAction`の契約が、Historyから具体Actionの種類を隠すことを説明する。
- `ReportApplication`が`GenerateReportAction`を生成することを示す。
- `ReportGenerationService`と完全な`ReportRequest`をActionへ渡すことを示す。
- `ReportActionHistory`がActionを所有してから、共通契約の`execute()`を呼ぶことを示す。
- 再実行・取消でHistoryが本文種類や装飾種類を判断しないことをコードで示す。

### 現在確認できる不整合

- 「Actionが完成文書を出力する」という説明を、実際にServiceがRenderingApiへ出力を依頼するコードと一致させる。
- `ReportAssembler`を所有者とする誤った説明を直す。Assemblerは生成・組み立てを行い、最終的な所有と破棄は別責任である。
- クラス図の`ReportAssembler --> DataReader : 注入`のように、利用側を注入元のように見せる表現を直す。実際にインスタンスを生成してコンストラクタへ渡す側を示す。
- Application起動時に一度生成する`DataReader`、`ReportRenderingApi`、`TemplateRegistry`、`ReportAssembler`、`ReportGenerationService`と、要求ごとに生成するAction・Reportを分ける。
- 第11章に残る★5件は文字だけ削除せず、説明順・コード範囲・図の関係を直して意図を解消する。

## 継承を使わない章の扱い

全章を無理に継承の説明へ統一しない。章で採用した構造に合わせて、次を説明する。

- 委譲：どの処理を、どの契約へ任せるか。
- 合成：外側と内側がどのように接続され、誰が所有するか。
- 登録：誰が具体実装を登録し、誰が契約型で検索するか。
- Factory：誰が具体生成を引き受け、利用側から何を隠すか。
- State：Contextが状態契約だけを呼び、状態遷移後の自動処理もシステム責任として接続されるか。
- Command：Invokerが共通操作だけを呼び、具体操作が要求と実行先を保持するか。

共通する説明軸は、分離理由、具体生成の局所化、契約としての受け渡し、具体を意識しない実行、所有・破棄である。

## 人手で必ず確認する問い

各章について、初見読者がフェーズ6だけを読んで次を説明できるか確認する。

- 変更前コードのどこに、別の理由で変わる判断が集まっていたか。
- 何と何を分離したか。
- なぜその継承・実装・委譲・合成・登録関係を選んだか。
- 新しいクラスごとに、なぜ必要か。
- 誰が具体クラスを選択・生成するか。
- どのインスタンスを、誰が、どこへ注入または受け渡しするか。
- 誰が所有し、誰が破棄するか。
- 安定側が具体クラスを意識せず、どの契約を呼ぶか。
- 一つの具体入力が、どのオブジェクト構造へ変換されるか。
- 実行時にどの順で呼ばれるか。
- 課題IDの完了条件がコード上でどのように成立したか。
- フェーズ1から章末まで通読して、前工程の結論が後工程で別の意味へ変わっていないか。
- 修正前の仕様・要求・クラス・実行結果と比較し、説明順の変更に不要な差分が発生していないか。

一つでも説明できなければ、その章をPASSにしない。

## 制御資産への反映

第11章の基準を確定した後、少なくとも次を更新する。

- `output/chapter00_2.md`
- `templates/chapter-template.md`
- `CLAUDE.md`
- `AI_HANDOFF.md`
- `ai-context.md`
- `agents/chapter-agent.md`
- `agents/consistency-agent.md`
- `agents/logic-check-agent.md`
- `agents/review-agent.md`
- `agents/orchestrator.md`
- `rules/checklist.md`
- `rules/phase-consistency-check.md`
- `rules/step5-6-model.md`
- `script/validate_book.py`
- `review-tasks.md`
- `quality/completion-gate.json`

自動検査では、説明品質のすべてを判定できない。見出し順、変更段階ラベル、生成・注入・所有表、部分図と採用図の順序など、機械的に検出できる再発だけをvalidatorへ追加し、最終判定には人手監査結果を残す。

## 禁止事項

- ★の文字だけを削除して完了にしない。
- 新しいクラス名を、役割説明より先に出さない。
- 全完成コードをフェーズ6へ複製しない。
- 一メソッドだけの断片を、所属クラスを示さず掲載しない。
- 抜粋コードだけを示し、全体経路、抜粋位置、呼出元・呼出先、省略範囲を読者に推測させない。
- 「生成」「注入」「所有」「利用」を同じ意味で使わない。
- クラス図の矢印と、実際の生成者・注入者・所有者を食い違わせない。
- 具体クラスを生成する場所と、具体クラスを判定せず実行する場所を同じ責任へ戻さない。
- 要求ID・機能IDを課題IDへ直接対応させない。
- 将来リスクを完成コードへ先行実装しない。
- CONS-064の対応途中の機能表を削除しない。
- 自動ゲートだけで初見読者監査を代替しない。
- 第11章の修正途中の構成や未確認の定型文を、全章へ一括展開しない。
- 一節だけを読んで修正を完了し、フェーズ1から章末までの再通読を省略しない。

## 完了条件

- 第1〜12章のフェーズ6が、変更前コードから採用後コードまで論理順に読める。
- 各課題について、分離理由、構造関係、具体生成、注入・受け渡し、所有・破棄、実行順を説明できる。
- 抜粋コードごとに全体構造上の位置、変更段階、所属クラス、前後の接続、省略範囲、確認目的が分かる。
- 継承を使う場合は、共通部分・可変部分・基底契約・上書き箇所・具体生成・多態実行が説明されている。
- 説明文、部分図、採用全体図、シーケンス、完成コードで、生成者・注入者・所有者・破棄者が一致する。
- 完成コードの`class`・`struct`が登場型表と採用全体図から欠落せず、図だけの型と関係線のない孤立型が0件である。
- 原因→課題の接続を再確認し、不足がある章だけフェーズ4・5を補正したうえでフェーズ6へ進んでいる。
- フェーズ7で新しい生成規則や注入関係が初出しない。
- 第11章の★が0件である。
- 第0章、テンプレート、Agent、ルール、validatorが同じ順序を指示する。
- 各章の人手監査結果と根拠箇所が`review-tasks.md`へ記録されている。
- 各章で修正前基準線との比較結果が記録され、説明順の変更に不要な仕様・要求・型・動作差分が0件である。
- CONS-064とCONS-065の両方の完了条件を満たす。
- releaseゲートがPASSする。

## 最終確認コマンド

```powershell
python script/run_completion_gate.py --release
git diff --check
rg -n "★" output --glob "*.md"
git status --short --branch
```

すべての条件を満たした後にのみ、`quality/completion-gate.json`のCONS-065を`done`、`book_status`を`ready`へ更新する。完了後は変更内容、人手監査結果、検証結果を報告し、`main`へコミット・プッシュする。
