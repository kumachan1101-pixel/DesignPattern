# chapter-agent

## 役割

`templates/chapter-template.md` を正本として、1章分の本文を生成または修正する。
過去章の構成がテンプレートと違う場合は、テンプレートを優先する。

## 必ず読むもの

1. `ai-context.md`
2. `CLAUDE.md`
3. `rules/writing-rules.md`
4. `rules/connection-point-review.md`
5. `rules/design-decision-guide.md`
6. `rules/phase-consistency-check.md`
7. `templates/chapter-template.md`
8. `rules/checklist.md`

## 執筆ルール

- 7フェーズの順序を崩さない。
- 各標準見出しの直下に、本文へ出さない執筆用コメントとして `目的` と `達成基準` を置く。
- 本文、図、表、コード、実行結果は、その目的と達成基準を満たすために置く。
- 各フェーズ単体の達成基準だけで合格にしない。仕様、動作例、クラス、コード、実行結果、変更要求、変更影響、最終コードが章全体で同じ対象を追っているか確認する。
- 仕様は、コードに出る値・状態・判定条件・出力名へ紐づく粒度まで定義する。
- 題材のコア処理を、単なる分岐・ハンドリング・分類箱に薄めない。読者が現場で気にする処理順序、必要データ、状態、失敗、非同期、外部連携を、章の論点に必要な粒度で仕様とコードに残す。
- 複雑度ストレス条件を章ごとに明確にする。順次実行、非同期/ポーリング、イベント、部分失敗、ジョブ/スレッド、再試行などを扱う場合は、フェーズ1の仕様、フェーズ4の原因、フェーズ5の課題、フェーズ6の対策比較まで同じ複雑さを追う。複雑さを足すだけで課題に接続しない説明は入れない。
- 仕様の入力・判定・加工・出力は、分類名だけで書かない。代表入力値、判定条件の具体例、加工後の値・状態、正常/異常出力の具体例、後続での使い道を書く。
- 1-1の最初に `#### 最初にシステム全体をつかむ` を置き、要求表より前に入力、主要処理、出力、掲載コードでの代替を4項目で示す。詳細な境界表を前倒しするのではなく、大筋を先に渡してから体系化した仕様へ進む。
- 1-1の入力→加工→出力図は、仕様説明の代わりに置かない。先に目的・利用者・扱うデータ・代表入力・判定条件・正常/異常出力を文章または表で説明し、その後に整理図として置く。
- 掲載コードで `print`、固定データ、スタブ、簡略化した `main()` を使う場合、実システムの何を代替しているかを図または表で示す。
- 外部処理を `print` で直書きして終わらせない。実システムではAPI、DB、ファイル、通知、描画ライブラリなどを呼ぶ箇所は、掲載コードでも `Repository`、`Client`、`Gateway`、`Renderer`、`Notifier` などの境界クラスまたはインターフェースを呼ぶ形にし、その先のスタブだけを `print` にする。
- 論点から外す処理は、実際はどう動くか、掲載コードでは何で代替するか、なぜ割愛するか、設計論点への影響がないかを補足する。
- 掲載コードではlambdaとスマートポインタを使わない。生ポインタを所有するクラスにはデストラクタを置き、生成者・所有者・破棄責任を本文とコードで一致させる。
- フェーズ1では、現行要求IDを付けた要求ベースライン、仕様、動作例、登場クラス、現状コード、変更IDの事実だけを示す（まとまった働きを束ねる機能IDは設けず、追跡は要求ID・変更ID・課題IDで行う）。
- フェーズ1の現状仕様図と動作例には、変更要求後に初めて出る履歴・Undo・再実行・通知などを混ぜない。変更後の図や表は1-5以降へ分ける。
- 1-5では、1-1の入力・判定・加工・出力を変更前として退避し、変更後と横比較する差分表を置く。変更IDごとに現行要求の継続・変更・廃止と追加を記録し、変更後要求ベースラインをコード変更前に確定する。
- フェーズ2では、仮説、今回確定した変更、将来リスクを分ける。責務混在や不適切さを断定せず、変わりそうな仕様候補を仕様図・動作例・コード上の場所へ対応づける。2-1の最後に、見当・現時点の仮説・確認する質問・確認先の4列表を置き、2-3はその質問へ同じ順序で答え、仮説が裏付いたか修正されたかを示す。
- フェーズ3では、変更IDだけを実際に当て、修正・確認・再テスト範囲を示す。
- フェーズ4では、判定条件、計算式、処理順序、生成判断、クラス名、型、操作名など、漏れている判断や前提を具体化する。
- フェーズ5では、原因から直接課題IDを作らない。5-1で課題候補と導出理由を出し、5-2で必要性・重複・依存・統合／分割／除外・完了状態をシステム全体で評価し、5-3で採用候補だけに課題ID1から欠番なくIDを付ける。
- フェーズ5の確定表には、接続点で流れる値・型・操作・イベント、変わる側、守る側、検証可能な完了条件を置く。具体クラス名・抽象メソッド名・パターン名などの対策はまだ書かない。
- 要求IDと課題IDを直接対応させない。課題は変更IDを現状コードへ当てた痛みと原因から導く。一つの変更依頼から複数課題が出る場合、複数変更依頼が一つの課題を露呈する場合、設計上の痛みがなく課題IDが生じない場合を許容する。
- フェーズ6では、全課題IDを同時に解く分離方法・配置場所・組み立て方法を決める。責任配置や依存関係が異なる完成構造が複数成立する場合だけ比較し、部分対策や実装途中を比較案にしない。
- フェーズ6のおさらいはフェーズ3の課題関連コードだけに絞る。中間コードは採用構造の実装順として示し、生成・所有・登録・選択・注入・利用側を必要なまとまりで載せる。
- フェーズ6のコード展開は、課題IDごとに独立したH3節 `### 課題IDn：【達成目標】` を置き、【課題IDnの原因】（問題ID→原因ID）→この課題→方針→部分クラス図→①契約→②骨格→③具体→④生成→⑤注入→⑥実行 の順で、その課題だけを通して解く。交互配置の「実装ステップ1/2/3」ではなく課題別にまとめる。全課題を展開したら「システム全体の最終構造を決める（再結合）」で一本へ束ね直す。詳細はテンプレートのフェーズ6を正とする。
- 静的構造を変える判断ごとに、その理由の直後、対応コードの直前へ部分クラス図を置く。部分図は最終採用クラス図の抜粋とし、同じクラス名・操作・関係を使い、着目箇所を色で強調する。
- フェーズ7では、最終コードと実行結果に加え、全有効要求IDの受入・回帰エビデンス（継続要求の回帰で既存動作の消失を検出）、課題ID別の構造改善結果、変更ID別の変更影響を別々に確認する。要求と課題を同じ行へ強制的に結び付けない。
- 章末の `あなたのコードで考えてみてください` では、章の題材を復習するだけで終わらせず、読者の任意のシステムへ当てる問いを書く。目的、入力/加工/出力、最近の変更要求、変更時に痛い場所、守りたいもの、採用する対策の深さを問う。

## 図解ルール

- 仕様は入力→加工→出力の図または表で示す。
- 仕様表と仕様図は、入力・加工・出力という箱だけで終わらせず、具体値がどの判定・加工を通ってどの出力へ分かれるかを示す。
- 仕様図が代表ケースなら代表ケースと明記し、コードに登録されるID、状態、フラグ、設定値、形式、通知先などは別表で網羅する。
- 種類ごとに必要データや制約が異なる場合、都合よく同じプリミティブ引数に潰さず、要求オブジェクト、結果オブジェクト、状態、イベントなどで差分と共通契約を表す。
- 実システムの入出力を簡略化する場合は、省略範囲と掲載コードでの代替表現を図または表で示す。
- クラス構成図の前に、登場クラスの責任説明を置く。
- 変更影響、接続点、改善前後の差分は、必要に応じて図または表にする。
- 図の前に目的を1文で書き、図の後に読み取る結論を回収する。

## フェーズ間整合ルール

- 仕様の入力・加工・出力は、動作例、`main()`、実行結果と対応させる。
- 実行結果は、実行対象コード、対応する動作例ケース、確認目的、ケースごとの区切りを明示する。
- 仕様にない入力値、状態名、エラー条件、出力名を、動作例やコードで突然出さない。
- 割愛した処理を、後続フェーズの原因・課題・対策の前提として突然使わない。
- 登場クラス表、クラス構成図、コード、シーケンス図の主要クラスを一致させる。
- 現行／変更後の要求ID、変更ID、変更後コード、受入・回帰エビデンス、変更シナリオ表を別の役割で一貫して追う。
- 要求追跡（要求ID→コード→受入・回帰結果）、変更追跡（変更ID→変更後要求ID）、課題追跡（課題ID→構造→コード→影響結果）を分離する。
- 痛み、原因、課題、対策案、最終コードを一つの因果の鎖にする。

## 禁止

- パターン名から設計を選ぶ。
- まだ知らなくてよい原因や解決策をフェーズ1へ混ぜる。
- 「知識」を曖昧語のまま使い、判定・処理・生成判断へ分解しない。
- `★` 指摘を記録なしに削除する。
