# 全章再構築計画（2026-07-20 第1章確定版を正本化）

> **これがタスク管理の正本です。** 別のAIが引き継ぐ場合は、`AI_HANDOFF.md` → 本ファイルの順で読み、
> 「状態」列が「作業中」または「未」のタスクを上から順に実行すること。
> 第1章を思考の型の完成見本とし、0章・テンプレ・ルールを直してから各章へ展開する。
> 旧計画は`IMPLEMENTATION_PLAN.md`、指摘の履歴は`review-tasks.md`に残す。

## 今回確定した全章共通の思考の型

### フェーズ5：課題定義

1. フェーズ3の変更影響とフェーズ4の原因を受け取り、変更が波及する接続点を特定する。
2. 独立した変化軸へP1、P2、P3の連番を振る。
3. 4列の接続点表`| 課題ID・接続点 | 接続するデータ | 変わる側 | 守る側 |`だけで課題を確定する。
4. 最後にシステム全体の課題を、現在の波及と目指す状態として文章化する。
5. クラス名の新設、配置先、生成・注入方法などの対策は書かない。

フェーズ5では、同じ内容の「フェーズ6へ渡す課題」「受け入れ条件」「前フェーズの再整理」を別表・別節として繰り返さない。フェーズ5の接続点表と課題文そのものが、フェーズ6への入力である。

### フェーズ6：対策検討

1. フェーズ5の課題から直接始める。変更影響の再統合や受け入れ条件の再掲を挟まない。
2. **接続点を変える三つの観点**で、システム全体の構造を決める。
   - 分離方法：安定側から何を外し、境界へどの操作・値・イベントを残すか。
   - 配置場所：切り出した条件・式・状態・手順・生成判断を、どの実クラスへ置くか。
   - 組み立て方法：誰が生成・所有・登録・選択・注入し、安定側は何だけを呼ぶか。
3. P1〜Pnは別々の採用案ではなく、一つの完成システムをコードまで追うIDとして使う。
4. 分離・配置・組み立てから完成構造が一つに定まる場合、比較を置かない。
5. 責任配置や依存関係が異なる完成可能なシステムが複数残る場合だけ、目的・効果・コストで比較する。途中状態や部分実装は比較案にしない。
6. フェーズ1のクラス図を同じ粒度で更新し、着目クラスを着色する。`main()`は架空クラスにせず、生成場所として注記・対応表・コードへ明記する。
7. 変更前コードのおさらいは課題関連部分だけに絞る。
8. 採用済みのクラス図を、理解できる単位で段階的にコードへ反映する。試行錯誤の履歴として見せない。
9. P1〜Pnごとに「目指した状態→適用したクラス／メソッド→適用結果」を追跡するが、達成判定はシステム全体で一度だけ行う。

### フェーズ7：対策実施

1. フェーズ6で確定した構造を、実行可能な完成コードにする。
2. フェーズ6の採用後クラス図と同じMermaid定義を再掲し、設計と完成後の見た目を変えない。
3. P1〜Pnを完成コードのクラス・メソッド・生成場所・実行結果まで追跡する。
4. 改善後の変更影響は完成コードの後で確認する。

## 全章へ適用する編集ルール

- 「受け入れ条件」「システム全体の完了条件」を独立節として先に置かない。目指す状態はフェーズ5の課題文、達成結果はフェーズ6末尾とフェーズ7で回収する。
- 「決定」のような抽象見出しだけを使わず、「接続点を変える観点」「分離方法」「配置場所」「組み立て方法」と意味を明示する。
- 代替案は数をそろえるために作らない。比較目的と完成構造の差が説明できない案は載せない。
- 新設クラスは、生成・所有・登録・選択・注入・破棄までの実行経路をコードで示す。
- クラス図に出した実クラスはコードと一致させ、浮くクラスには利用者を注記する。やり取りラベルは依存の目的を補う説明として許容し、時間順はシーケンス図へ分ける。
- 対策検討と対策完成後のクラス図は同じ定義にする。
- 接続点コードを`...`や「同様」で略さない。関数本体はKindleで読める改行を使う。
- システムが自動で行うべき後続処理を、利用者の手動呼び出しにしない。
- 内部数値や状態が変わる場合、変更前→変更後が実行ログで確認できるようにする。
- 正常系仕様図へ、実装されていないエラー分岐を混ぜない。異常系は実コードと一致するエラー条件表で扱う。

## 実行順

### 土台

| ID | 内容 | 状態 |
|---|---|---|
| V3-01 | 第1章を新しい思考の型へ修正し、追加★を解消する | 完了（82edccb以降） |
| V3-02 | 本計画を第1章確定版へ更新する | 完了（本コミット） |
| V3-03 | 0章、テンプレ、CLAUDE.md、AI_HANDOFF.md、rules、validatorを第1章確定版へ更新する | 完了（本コミット） |

### 章別

| ID | 章 | 共通対応・章固有対応 | 状態 |
|---|---|---|---|
| C1 | ch01 | 追加★「決定の意味」を解消し、完成見本として最終確認する | 完了（本コミット） |
| C2 | ch02 | 重複する引き渡し・再統合・受け入れ条件を削除し、接続点表→三観点→単一の窓口構造へ直結。生成・所有・注入を追跡する | 完了（本コミット） |
| C3 | ch03 | 同じ直結フローへ変更。状態オブジェクトと待ち行列の生成・所有、自動昇格まで追跡する | 完了（本コミット） |
| C4 | ch04 | 直結フロー化。ファイル境界内でサンプルCSVの準備・読込を完結させ、`main()`を事前登録済みデータから開始。保存場所変更でアクセス方法も変わり得る前提へ修正 | 完了（本コミット） |
| C5 | ch05 | 直結フロー化。浮きクラス注記、Redo説明、仕様図分割、痛みの再定義、UIを変わりやすい端として扱う。残高を台帳で一元管理し、完成構造が分岐する場合だけ代替案比較 | 完了（本コミット） |
| C6 | ch06 | 直結フロー化。クラス図ラベル規約と表マークを統一。「完了条件」を廃止。Decorator完成構造とトッピングリスト完成構造を、小規模運用という目的で比較 | 完了（本コミット） |
| C7 | ch07 | 直結フロー化。通知先＝担当者、通知手段＝メール等を区別。在庫データの所有と`setStock()`の関係を説明。正常系仕様図、出力の空行、安定させるロジックの表現を修正 | 完了（本コミット） |
| C8 | ch08 | 直結フロー化。Processorの生成・Registry登録・利用側注入を追跡する | 完了（本コミット） |
| C9 | ch09_2 | 直結フロー化。状態と優先度の二軸を一つのシステムで組み立て、生成・所有・遷移を追跡する | 完了（本コミット） |
| C10 | ch10 | 直結フロー化。窓口・通知・生成の三軸を一つのシステムとして組み立てる | 完了（本コミット） |
| C11 | ch11 | 直結フロー化。骨格・装飾・操作履歴の生成順と所有を追跡する | 完了（本コミット） |
| C12 | ch12 | 直結フロー化。状態・通知・承認ルールの生成・登録・注入・実行を追跡する | 完了（本コミット） |

### 横断確認

| ID | 内容 | 状態 |
|---|---|---|
| X1 | 全章の正常系仕様図に、実コードにないエラー分岐が混ざっていないか確認する | 完了（本コミット） |
| X2 | 全章の複数ケース実行結果に、ケース間の空行があり読みやすいか確認する | 完了（本コミット） |
| X3 | 全章のクラス図に浮くクラスがある場合、利用者の注記があるか確認する | 完了（本コミット） |
| X4 | 全章の1行関数をKindle向けの複数行形式へ直す | 完了（本コミット） |
| X5 | 全章でフェーズ6採用図とフェーズ7完成図のMermaid定義を一致させる | 完了（本コミット） |

## 検証ゲート

章ごとに次を実行する。

- `python script/validate_book.py`
- `python script/check_execution_output.py`
- `python script/check_kindle.py`
- `python script/audit_book.py`（0件）
- `git diff --check`
- 変更したMermaid図のレンダリング

## 進捗の更新方法

着手時は「作業中」、完了時は「完了（コミットID）」へ更新する。★を消した内容は`review-tasks.md`へ記録し、検証後に対象ファイルだけをコミットして`main`へプッシュする。

---

# 第2ラウンド（2026-07-21 第9章の★指摘・全章定数化）

## 背景

第9章チケットシステムへの★指摘を精査した結果、以下が判明。著者承認済みの方針で対応する。

- 担当者割当は実装されておらず状態遷移のみ（1-1図のTicketRepositoryは実コードに不在）
- 優先度はチケットに保存されず毎回再計算（引き継がれない）
- ステータスがID単位で保存されない（第3章の予約水準に達していない）
- エスカレーション先の状態が未定義（状態機械はOpen/InProgress/Resolved/Pendingのみ）
- 直文字列がベタ書き（状態名・tier・優先度・イベント種別）

## 承認済み方針（著者回答 2026-07-21）

1. **第9章をチケット実体化**：`TicketRepository` にチケットID・状態・優先度・担当者を保存し、操作が保存済みチケットを更新。優先度は登録/再受付/エスカレーション時に再計算して保存し以降引き継ぐ。担当者はアサインで保存。第3章の予約と同水準の追跡可能なシステムにする。
2. **Escalated状態を新設**：InProgress→Escalated、Escalated→Resolved（解決）/InProgress（差し戻し）の遷移を定義。優先度High付与。状態図・状態遷移表・1-2・7-4を更新。
3. **全章を一括で定数化**：直文字列（状態名・ユーザー種別・優先度・イベント種別等）を名前付き定数（`constexpr const char*` / `const string` / 適所でenum）へ。実行結果照合を維持（出力は不変）。

## タスク

| ID | 内容 | 状態 |
|---|---|---|
| R2-9 | 第9章：チケット実体化（Repository保存）＋Escalated状態新設＋当章の定数化。1-1〜7-4を整合。実行結果を更新 | 完了（7-1実体化コード、TicketService/TicketRepository/StaffDirectory、Escalated状態＋状態遷移図、フェーズ6をTicketService設計へ再構成、enterprise→corporate/ティア→ユーザー種別、担当者ID=AGT系を分離。validator.ch09登録とenum class除外を更新。全ゲート通過） |
| R2-9b | 第9章：状態保存とEscalated(緊急対応中)は変更要求ではなく現状仕様、という指摘に対応。フェーズ1-5(before側)を状態保存あり・Escalated現状化へ整合 | 完了（1-4 before コードをTicketRepository(map)でID単位保存する実体ありへ作り替え＝優先度と状態遷移はupdateStatusの分岐に混在させ痛みは維持。1-1状態表にEscalated追加＋保存を現状仕様化、1-2動作例・1-2b遷移表/状態図をOpen/InProgress/Escalated/Resolvedへ、1-3クラス表/図にTicketRepository追加、1-5変更要求は状態追加(保留中)＋SLA改定に純化、2-5表・3-1変更試行・6変更前図/おさらいコード・7-1状態図注記を整合。実行結果更新。全ゲート通過） |
| R2-const-01 | ch01 会員種別 "Premium"/"Regular" → `MemberType`（matches分岐） | 完了 |
| R2-const-02 | ch02 直文字列の分岐なし（口座IDは記録データ・状態は非文字列）→対象外 | 対象外 |
| R2-const-03 | ch03 State object方式で文字列分岐なし（EVT系は記録データ）→対象外 | 対象外 |
| R2-const-04 | ch04 店舗形式ID "store"/"fc"/"ec" → `SchemaType`（登録キー＋schemaType()） | 完了 |
| R2-const-05 | ch05 Command object方式で文字列分岐なし（CAT系は記録データ）→対象外 | 対象外 |
| R2-const-06 | ch06 トッピングID "Milk"等 → `ToppingId`（生成分岐 create()） | 完了 |
| R2-const-07 | ch07 Observer object方式で文字列分岐なし（PRD系は記録データ）→対象外 | 対象外 |
| R2-const-08 | ch08 決済手段ID＋状態 "保留"/"失敗" → `PaymentMethod`/`PaymentStatus` | 完了 |
| R2-const-10 | ch10 具象オブジェクト分岐で文字列dispatch なし（PARTNER系は記録データ）→対象外 | 対象外 |
| R2-const-11 | ch11 テンプレートIDは記録データ・文字列分岐なし→対象外 | 対象外 |
| R2-const-12 | ch12 通知チャネル "email"/"chat" → `Channel`（supports分岐） | 完了 |
| R2-Z | 全章横断確認（定数の初出説明・Kindle80字・実行結果照合）＋方針判断の記録 | 完了（分岐・dispatchに使う直文字列のみ定数化。記録IDのseedデータ・出力メッセージは過剰化を避けデータのまま維持。全ゲート通過） |

### R2-const 方針判断（記録）

「直文字列を定数へ」は、**コードの分岐・dispatch判定に使う直文字列**（＝打ち間違いで挙動が壊れる磁石リテラル）を対象とした。対象は分岐キー（`== "..."`）と、レジストリ登録キーと型宣言が対で一致すべき識別子（ch04/ch08）。会員種別・店舗形式・トッピングID・決済手段/状態・通知チャネルが該当。一方、DBのseed用レコードID（口座ID・イベントID・商品ID・提携先ID・テンプレートID）や画面出力メッセージは、分岐に使われないデータ・表示であり、定数化するとかえって冗長になるため文字列のまま残した。State/Command/Observer/Factory-objectで既にオブジェクト分岐している章（ch02/03/05/07/10/11）は、そもそも文字列分岐が無く対象外。

## 定数化の方針（全章共通）

- 原則は名前付き定数（`constexpr const char*` または `const std::string`）。状態・種別など閉じた集合はenum classが望ましいが、既存の文字列比較と実行結果を壊さない範囲で選ぶ。
- 定数は各コードの先頭付近でまとめて定義し、初出時に日本語コメントで意味を説明する（Kindle読者向け）。
- 変換後も実行結果（`check_execution_output.py`）が完全一致することを必ず確認する。

# 第3ラウンド：システム仕様図と現状クラスの整合（2026-07-25）

## 第10章の★指摘から確定した共通基準

フェーズ1では、次の二つの図を役割で分ける。

1. **システム全体図**：利用者、対象システム、主要な保存データ、外部システムの間を、どのデータ・要求・結果が流れるかで示す。実装クラス名は使わない。
2. **システム内部図**：対象システムへ何を入力すると、どの業務処理がどの順序で行われ、何が出力・保存・通知されるかを示す。実装クラス名は使わない。

二つの図は重複ではない。全体図はシステム境界、内部図は対象システムの正常処理を示す。クラス名、メソッド名、結果型などの実装語彙は1-3以降で初めて対応づける。

1-3の登場クラス表・クラス図・1-4の現状コードは、次を満たす。

- `class`だけでなく、仕様上のデータを持つ`struct`も含め、現状コードに定義した全型を表と図へ載せる。
- 図の全型に、保持・生成・取得・引数・戻り値・呼び出しのいずれかの関係線を持たせる。
- 現状コードに存在しない型、変更要求で初めて追加する型、定義しただけで使わない型を現状の表・図へ置かない。
- 1-2には現状の動作だけを置き、1-5の変更要求後に初めて成立する動作例を混ぜない。

## 根本原因

第10章の主因は、変更要求に含まれない共通基盤を「変更前」から落とし、「変更後」だけへ置いたことだった。連携先追加・通知先変更・順次実行が今回の変更対象であり、送信結果の契約`DeliveryResult`と実行結果の保存`BatchLog`は変更対象ではない。それにもかかわらず、現状コードを`void`・保存なしへ変えたため、変更前後が同じシステムの比較ではなくなっていた。「変更後の要素が逆流した」のではなく、**変えない要素を変更前から欠落させたベースライン不一致**である。加えて、既存検査は`class`だけを対象にし、`struct`、クラス表、図の余分な型、関係線のない型、変更前後で維持すべき型・保存境界を検査していなかったため、不一致を検出できなかった。

## 全章監査結果とタスク

監査は、1-1の実装語彙、1-3の表・図、1-4の`class`／`struct`定義、図の関係線を照合した。これに加え、仕様変更の対象外である保存方法・既存の結果型・検証・履歴・外部境界が、変更前と変更後の両方に同じ基盤として存在するかを全章で照合する。第9章も2026-07-25時点の修正版を対象に含める。

| ID | 章・対象 | 監査で確認した修正点 | 状態 |
|---|---|---|---|
| R3-00 | 第0章 | システム全体図とシステム内部図の目的・読み分け、1-3まで実装語彙を出さない規則を説明する | 完了（本コミット） |
| R3-T | テンプレート・ルール | 二つの仕様図、現状動作例と変更後動作例の分離、全`class`／`struct`と関係線の一致、変更対象外の共通基盤を前後で維持する規則を正本化する | 完了（本コミット） |
| R3-B | 全章 | 1-5で「変える要素」と「変えない共通基盤」を分け、保存方法・結果型・検証・履歴・外部境界について、1-4と7-1の不必要な差分を監査する。差分がある場合は変更要求または課題IDで根拠を示し、根拠がなければ共通基盤へ戻す | 完了（本コミット） |
| R3-01 | ch01 | 1-1の`OrderProcessor`先取りを仕様語彙へ変更。システム全体図を追加し、`CheckoutResultRenderer`の利用関係を図へ追加する | 完了（本コミット） |
| R3-02 | ch02 | システム全体図を追加し、現状コードの`TransferRecord`を登場型表・クラス図へ追加する | 完了（本コミット） |
| R3-03 | ch03 | 1-1の`EventDatabase`／`TicketReservation`先取りを仕様語彙へ変更し、`EventInfo`を登場型表・クラス図へ追加する | 完了（本コミット） |
| R3-04 | ch04 | 1-1の`ImportResult`／`SchemaRegistry`先取りを仕様語彙へ変更。`SalesRow`／`ImportResult`／`ImportSchema`を表・図へ追加し、全型の関係線を示す | 完了（本コミット） |
| R3-05 | ch05 | 1-1の`UIButtons`先取りを仕様語彙へ変更し、`Category`を登場型表・クラス図へ追加する | 完了（本コミット） |
| R3-06 | ch06 | システム全体図を追加し、`MenuItem`を表・図へ追加。`MenuDatabase`と`CustomDrink`の利用関係を図へ示す | 完了（本コミット） |
| R3-07 | ch07 | 1-1の実装クラス名を仕様語彙へ変更し、`ProductInfo`を登場型表・クラス図へ追加する | 完了（本コミット） |
| R3-08 | ch08 | 1-1の実装クラス名を仕様語彙へ変更。要求・結果・設定・記録の各`struct`を表・図へ追加し、`PaymentLog`の利用関係を示す | 完了（本コミット） |
| R3-09 | ch09_2 | 1-1の`TicketManager`／`TicketRepository`先取りを仕様語彙へ変更し、`UserInfo`／`Ticket`を登場型表・クラス図へ追加する | 完了（本コミット） |
| R3-10 | ch10 | システム全体図と内部図へ分離し、現状動作例と変更後動作例を分離。`PartnerConfig`、`DeliveryResult`、`BatchRecord`、`BatchLog`を変更前後の共通基盤として表・図・コードへ揃え、変更差分を連携先・通知・順次実行へ限定する。全型の関係線を明示する | 完了（本コミット） |
| R3-11 | ch11 | 1-1の`ReportRenderingApi`先取りを仕様語彙へ変更。`SalesSummary`／`ReportTemplate`を表・図へ追加し、`ReportRenderingApi`／`TemplateRegistry`の利用関係を示す | 完了（本コミット） |
| R3-12 | ch12 | 1-1の実装クラス名を仕様語彙へ変更する。現状の表・図・コードにある型と関係線は一致済み | 完了（本コミット） |
| R3-V | validator | `struct`、表、余分な型、関係線、1-1の実装語彙、現状／変更後動作例の混在を段階導入で検査する | 完了（全12章へ適用・本コミット） |

## R3-B監査結果

1-4と7-1の型集合を照合し、仕様変更と無関係な保存方法・結果契約・検証・履歴・外部境界が消えていないかを確認した。各章の1-5には、次の共通基盤を「変更なし」として明記し、validatorでも前後の存在を継続検査する。

| 章 | 変更対象外として前後で維持する共通基盤 | 変更前から外れた型と根拠 |
|---|---|---|
| ch01 | `CustomerDatabase`、`CheckoutResultRenderer` | なし |
| ch02 | `AccountDatabase`、`TransferHistory` | なし |
| ch03 | `EventDatabase` | なし |
| ch04 | `ImportResult`、`SchemaRegistry` | `SampleFileStore`はP1の取得元・保存先境界を注入する課題に基づき、`ImportFileGateway`等へ責任を移した |
| ch05 | `CategoryDatabase` | `UIButtons`はP1の実行入口分離とP2の残高正本化に基づき、`BudgetApp`と台帳境界へ責任を移した |
| ch06 | `MenuDatabase`、`MenuItem` | `CustomDrink`はP1のトッピング接続点分離に基づき、基本商品と連結可能なトッピング部品へ分解した |
| ch07 | `ProductDatabase` | なし |
| ch08 | `PaymentRequest`、`PaymentResult`、`PaymentLog` | なし |
| ch09_2 | `Ticket`、`TicketRepository`、`UserDatabase` | `TicketManager`と`PriorityCalculator`はP1/P2の状態・優先度分離に基づき、Service・Phase・Ruleへ責任を移した |
| ch10 | `DeliveryResult`、`BatchRecord`、`BatchLog` | `NotificationService`はP2の通知差し替え要求に基づき、通知契約と通知実装へ責任を移した |
| ch11 | `DataReader`、`TemplateRegistry`、`ReportRenderingApi` | なし |
| ch12 | `WorkflowCaseRepository`、`ApproverDatabase`、`NotificationTargetRepository` | なし |

## 完了条件

- 全章の1-1を、システム全体図→システム内部図の同じ読み方で説明できる。
- 1-1と1-2に実装クラス名・メソッド名・変更後に初登場する型や動作がない。
- 1-3の登場型表、クラス図、1-4の現状コードの`class`／`struct`集合が一致する。
- クラス図の全型に、コードと一致する関係線がある。
- 1-5で変更対象と変更対象外を分け、変更対象外の保存方法・結果契約・検証・履歴・外部境界が1-4と7-1で一致する。差分を残す場合は変更要求または課題IDで根拠を説明する。
- 各章を修正した時点でvalidatorの適用対象へ追加し、全章適用後にR3-Vを完了とする。
