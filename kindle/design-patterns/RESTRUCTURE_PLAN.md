# 全章再構築計画（2026-07-20 ★指摘対応・フェーズ論理再定義）

> 著者承認済みの方向性（2026-07-20）に基づく実行計画。
> 旧計画は `IMPLEMENTATION_PLAN.md`（完了済み）。現行の指摘記録は `review-tasks.md`。
> 別のAIが引き継ぐ場合は、まず `AI_HANDOFF.md` → 本ファイルの順で読むこと。

## 背景（★指摘の要約）

2026-07-20のレビューでch01〜ch06に約30件の★が追加された。個別修正に留まらない全章テーマは次の6つ。

- **A. フェーズ5〜7の論理再構築**：現状はフェーズ5に対策（分離する責任・移す先）が漏れ込み、フェーズ6冒頭に構造の形をした「完了条件」が先に出る。課題定義＝接続点の洗い出し、対策検討＝分離・配置・組み立ての検討、対策実施＝コード完成、という流れに再定義する。
- **B. システム実在性**：プリントだけの「骨格」、実体のない処理を排除。仕様がやりたいことをコードが実際に実現し、状態が保存・追跡できること（第3章の予約システムが到達水準）。
- **C. 提示ルール統一**：接続点コードの略記禁止、関数スタイル基準、クラス図規約、データ・関数コメント。
- **D. 代替案の可視化**：採用しなかった構造のコードイメージを提示。「最終構造は一つに定まる」と安易に断定しない。
- **E. 0章への追記**：変わりやすい場所のヒューリスティック、共有ストア規約、フェーズ定義、図規約。
- **F. 章別の個別修正**（★の残り）。

## 承認済みの方向性（著者回答）

1. **フェーズ再定義で正本から改訂する**（文言調整ではなく本格改訂）。
   - フェーズ5 課題定義＝**接続点の洗い出しと定義**（どこに境界があるか／何が流れるか＝値・型・操作・イベント／どちら側が変わりどちら側を守るか）＋**受け入れ条件**（「変更要求Xを再適用したとき修正範囲がYに収まる」という**構造に依存しない影響範囲の条件**。対策の形は書かない）。
   - フェーズ6 対策検討＝接続点を**どう切るか（分離）／切り出したものをどこに置くか（配置）／誰が生成・所有・注入して繋ぎ直すか（組み立て）**を検討し、受け入れ条件で候補を評価して採用を決める。旧「システム全体の完了条件」（構造の形をした条件）は廃止し、フェーズ5の受け入れ条件を評価軸として参照する。
   - フェーズ7 対策実施＝決定した組み立てでコードを完成させ、受け入れ条件（変更シナリオ再適用）で検証する。
2. **第4章は実ファイルで本格化**（std::ofstream/ifstreamで実ファイルを作成・読込。事前データ登録でmainを簡素化。ヒアリングの前提「開閉手順は固定」を現実的に書き直す。題材は維持）。
3. **代替案は「本文で比較＋章後半コラムで詳細」**（フェーズ6本文で受け入れ条件により短く比較、コードイメージは章後半コラム。ch6のリスト方式のような読者が思いつく案も正直に扱う）。

## タスク一覧（実行順）

### 土台（正本・ルール・検証）

| ID | 内容 | 対象 | 状態 |
|---|---|---|---|
| F1 | フェーズ5/6/7の再定義を正本へ反映（5=接続点洗い出し+受け入れ条件、6=分離・配置・組み立て+採用、7=実施+検証）。「完了条件」→「受け入れ条件」への転換を含む | chapter00_2.md, templates/chapter-template.md, CLAUDE.md 7フェーズ表, rules/design-decision-guide.md, rules/connection-point-review.md, rules/phase-consistency-check.md, rules/checklist.md | 完了 |
| F2 | 0章への追記：変わりやすい場所のヒューリスティック（入力・保存など端は変わりやすい）、共有ストア注入規約（staticにしない理由・模擬DB/待ち行列の読み方）、クラス図規約（浮きクラス注記必須・依存ラベルの許容範囲） | chapter00_1.md / chapter00_2.md, rules/writing-rules.md | **作業中（次のAIはここから）** |
| F3 | validator改訂：新フェーズ5（接続点定義表+受け入れ条件）・新フェーズ6（分離・配置・組み立て+受け入れ条件参照）の検証。旧「完了条件」語彙の検出。接続点コード略記（「が続く」「同様に並ぶ」等）の検出 | script/validate_book.py | 未 |
| F4 | 提示ルール明文化：接続点コードは略さない／同型並列の極小関数は1行揃え・それ以外はブロック改行（Kindle80字と両立）／データ・関数コメント義務／浮きクラス注記 | rules/writing-rules.md, rules/checklist.md | 未 |

### 章別（各章共通の作業＝①新フェーズ5/6へ書き直し ②実在性監査（プリントだけの処理排除・状態追跡可能に） ③接続点コード完全記載 ④代替案コラム ⑤スタイル統一。以下は章固有の追加分）

| ID | 章 | 章固有の対応 | 状態 |
|---|---|---|---|
| C1 | ch01 | ★表破損の修復（6-2表）。「`int`→`int` の `apply()`」の不明瞭表現を平文へ。CustomerDatabase等の共有ストア注入化（データ保持の説明）。「他のelse ifブロックが続く」の完全展開。3案比較のコードイメージをコラム化 | 未 |
| C2 | ch02 | フェーズ5の「変わらない骨格がプリントだけ」箇所を実処理化。接続点表へ具体クラス・関数・データ名を記載。「verifyAccount/checkBalanceは変わる側では」の検証と明記。「銀行APIの手順が…続く」等の略記解消。課題定義表から対策記述を除去（新フェーズ5へ） | 未 |
| C3 | ch03 | 1-1仕様図のイベントID入力の一貫性（操作は全記載なのにIDが1つだけ）。1-5「短時間」の曖昧さ→Reservedは保留しない限り期限なしで維持されることを明示（または自動解放の運用に触れる）。0章の共有ストア規約への参照 | 未 |
| C4 | ch04 | **実ファイル化**：ofstream/ifstreamで実ファイル作成・読込、処理の実体化、事前データ登録でmain簡素化。ヒアリング「開閉手順は固定」の前提を現実的に是正（保存場所が変わればアクセス方法も変わり得ることを認め、この章の骨格が守る範囲を明確化） | 未 |
| C5 | ch05 | 残高を台帳クラスで一元管理（利用側で差分計算させない）。UIButtonsが残高を持つ責任の問題をフェーズ4で明示。3-1を「関数がない」ではなく実装試行の痛みへ書き直し。Redoの意味の説明追加。変更後仕様図を登録操作/履歴操作でブロック分割。UIも変わりやすい前提の明記。代替案（実行クラスへ残高を渡す形）の比較 | 未 |
| C6 | ch06 | リスト方式（トッピングをAddで積んで合計）を代替案として本文比較+コラム掲載。小規模ならリスト方式が妥当という評価も正直に書く。クラス図のデータやり取りラベルの扱いを0章規約に従い注記 | 未 |
| C7 | ch07 | 共通作業のみ（実在性は通知inbox実装済み。監査で確認） | 未 |
| C8 | ch08 | 共通作業のみ（Registry/Processor実装済み。監査で確認） | 未 |
| C9 | ch09_2 | 共通作業のみ（状態・優先度実装済み。監査で確認） | 未 |
| C10 | ch10 | 共通作業のみ（通知蓄積実装済み。監査で確認） | 未 |
| C11 | ch11 | 共通作業のみ（DataReader集計実装済み。監査で確認） | 未 |
| C12 | ch12 | 共通作業のみ（Repository実装済み。監査で確認） | 未 |

### 仕上げ

| ID | 内容 | 状態 |
|---|---|---|
| Z1 | 全章横断スタイル掃討（1行関数基準・コメント・浮きクラス注記・略記禁止の残り） | 未 |
| Z2 | 全ゲート（validate/exec/kindle/audit/diff/mermaid）+ review-tasks.md記録 + mainへプッシュ | 未 |

## 実行順序と進め方

1. F1→F2→F3→F4（正本を先に固める。以降の章作業はすべて新正本準拠）
2. C1〜C6（★が付いた章から。各章ごとに全ゲート→commit→push）
3. C7〜C12（共通作業+監査）
4. Z1→Z2

## 検証ゲート（章ごと）

`python3 script/validate_book.py` / `check_execution_output.py` / `check_kindle.py` / `audit_book.py`（0件）/ `git diff --check` / フェーズ6・7-3のmermaidレンダリング。

## 進捗の更新方法

本ファイルの「状態」列を（未／作業中／完了）で更新し、章ごとのコミットに含めること。完了時はreview-tasks.mdにも要約を記録する。
