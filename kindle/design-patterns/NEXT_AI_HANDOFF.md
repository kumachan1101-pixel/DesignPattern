# 次AIへの引き継ぎ ―― 現在地と残件

## 現在地

- **2026-08-13：最新コミット`a80dc09`で追加された本文の`★`10件を、AF-20260813-088〜097として登録済み。これが現在の最優先残件である。**詳細・完了条件は`author-feedback-20260813.md`、実施順と現在判定は`review-tasks.md`冒頭を正本とする。
  - 第0章前半1件：掲載C++で使う標準ライブラリの型・関数説明を全章から棚卸しする。
  - 第5章9件：フェーズ1の先取り、フェーズ2・確定変更表の章間不統一、フェーズ3・7の長大コード、原因ID追跡、Undo履歴操作、宣言と実装の提示意図を直す。
  - 横断指定のある項目は第5章だけで完了にせず、全12章、テンプレート、ルール、検証スクリプトまで確認する。
  - AF-20260813-094と097は同じUndoスタック操作の抜粋版・完成コード版である。同じ設計判断を両方へ反映し、成功・失敗時の履歴不変条件をテストする。
- 著者指摘AF-20260813-001〜087と本文整合タスク`CONS-001〜066`は完了済み。過去の全章ロジック監査152件も修正済みで、`logic-audit-20260812.md`と`review-tasks.md`の古い途中経過は履歴である。
- 追跡番号は`要求ID・変更ID・リスクID・問題ID・原因ID・課題ID`を用途別に使う。機能IDは設けない。
- 第1〜12章のフェーズ1は、代表実行結果→結果から読み取れる動き→対応する入力・`main()`→全体要約→詳細仕様の順で統一済み。
- 第1〜12章のフェーズ6は、抽象的な全体像→課題別の構造変更→生成・注入・所有・実行→設計トレースの深さで統一済み。
- 本文の編集用`★`は10件。タスク修正時に対応IDの判断・変更先・横断監査結果を残してから削除する。
- 過去の通常ゲートとreleaseゲートのPASSは、最新10件追加前の履歴である。10件を完了してゲートを再実行するまで本文readyと判定しない。
- タスク登録時の検証では`check_kindle.py`は15章PASS、`validate_book.py`は第5章541行の未対応`★`を検出してFAILした。これは現在の未対応状態を正しく示す結果であり、修正後にPASSへ戻す。
- 作業開始時点で`.obsidian/workspace.json`にユーザーの未コミット変更がある。編集・ステージ・コミットしない。

過去の`CONS-064〜066`指示書は完了記録であり、現在の作業指示ではない。`TASK.md`と`RESTRUCTURE_PLAN.md`も履歴である。

## 最初に読む順序

1. `AI_HANDOFF.md`
2. `AUTHOR_FEEDBACK_MASTER.md`
3. `templates/chapter-template.md`
4. `rules/checklist.md`
5. `rules/writing-rules.md`
6. `rules/phase-consistency-check.md`
7. 作業対象の章

現在の本文状態と過去監査は`review-tasks.md`、機械的な完了状態は`quality/completion-gate.json`を正とする。

## 現行の重要判断

### 現状システムの説明順

```text
代表的な実行結果
→ その結果から読み取れる利用者・入力・主要処理・状態変化・出力
→ 同じ結果を生む入力・main
→ 入力・処理・出力・掲載コードでの代替
→ 要求・データ・図・クラス・全コード
```

入力コードを代表実行結果より先に出さない。結果はデバッグ行一行ではなく、システムの中心動作を時系列でイメージできるエンドツーエンドケースにする。

### フェーズ間の接続

各フェーズ末のID・名詞・短い確定文を、次フェーズの入力として可能な限り同じ文言で使う。変更ID→問題ID→原因ID→課題IDと、要求ID→完成コード→受入・回帰結果は別の追跡線として扱う。要求から課題へ直接飛ばない。

### 対策検討

フェーズ6では、常に全体の構造変更、現在地、抽象的な設計操作、具体クラス・メソッドを対応させる。変更前コード、分離理由、着色した部分クラス図、採用後コード、生成・注入・所有・実行、具体入力での再結合を順に説明する。

### 著者指摘の扱い

著者指摘を文言どおり機械適用しない。現行仕様、前後フェーズ、完成コード、他の指摘と整合するか確認し、必要なら目的を両立する順序へ統合する。仕様変更と関係のない保存方式、API、ログ、検証、結果を変えない。

## 本文完成後の残件

本文の論理・コード・図の整合タスクとは別に、出版作業が残る。

| ID | 状態 | 内容 |
|---|---|---|
| PUBLISH-001 | 未着手 | EPUBまたはKPFを再生成できる出版ビルドを作る |
| PUBLISH-002 | PUBLISH-001待ち | Kindle Previewerで端末別表示を確認する |
| META-001 | ユーザー確認待ち | 書名・サブタイトル・著者名・紹介文・採用表紙を確定する |
| EDIT-001 | PUBLISH-001待ち | 生成成果物を初見読者として最終通読する |
| GATE-001 | 完了 | 本文ゲートと出版パッケージゲートを分けた。`run_completion_gate.py` 引数なし＝manuscript ready、`--package`＝KDP package ready。後者は `check_publish_package.py` が目次・成果物・結合順を検査する |

本文が`ready`でも、KDP入稿用成果物が完成したことを意味しない。

## 再確認コマンド

```powershell
python script/run_completion_gate.py --release
git diff --check
rg -n "★" output --glob "*.md"
git status --short --branch
```

本文を変更した場合は、releaseゲートを再実行してから完了を報告する。
